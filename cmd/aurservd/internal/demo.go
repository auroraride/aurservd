// Copyright (C) liasica. 2021-present.
//
// Created at 2021-12-14
// Based on aurservd by liasica, magicrolan@qq.com.

package internal

func Demo() {
	// items, _ := ent.Database.Person.Query().Where(person.Status(2)).All(context.Background())
	// for _, item := range items {
	// 	// 获取生日
	// 	birth := item.IDCardNumber[6:14]
	// 	birthday := carbon.Parse(birth).StdTime().AddDate(18, 0, 0)
	// 	if birthday.After(time.Now()) {
	// 		fmt.Printf("[%s] %d\t%s\t%s\n", item.AuthAt.Format("2006-01-02"), item.ID, item.Name, item.IDCardNumber)
	// 	}
	// }
}
