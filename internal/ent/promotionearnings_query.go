// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/order"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/promotioncommission"
	"github.com/auroraride/aurservd/internal/ent/promotionearnings"
	"github.com/auroraride/aurservd/internal/ent/promotionmember"
	"github.com/auroraride/aurservd/internal/ent/rider"
)

// PromotionEarningsQuery is the builder for querying PromotionEarnings entities.
type PromotionEarningsQuery struct {
	config
	ctx            *QueryContext
	order          []promotionearnings.OrderOption
	inters         []Interceptor
	predicates     []predicate.PromotionEarnings
	withCommission *PromotionCommissionQuery
	withMember     *PromotionMemberQuery
	withRider      *RiderQuery
	withOrder      *OrderQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PromotionEarningsQuery builder.
func (peq *PromotionEarningsQuery) Where(ps ...predicate.PromotionEarnings) *PromotionEarningsQuery {
	peq.predicates = append(peq.predicates, ps...)
	return peq
}

// Limit the number of records to be returned by this query.
func (peq *PromotionEarningsQuery) Limit(limit int) *PromotionEarningsQuery {
	peq.ctx.Limit = &limit
	return peq
}

// Offset to start from.
func (peq *PromotionEarningsQuery) Offset(offset int) *PromotionEarningsQuery {
	peq.ctx.Offset = &offset
	return peq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (peq *PromotionEarningsQuery) Unique(unique bool) *PromotionEarningsQuery {
	peq.ctx.Unique = &unique
	return peq
}

// Order specifies how the records should be ordered.
func (peq *PromotionEarningsQuery) Order(o ...promotionearnings.OrderOption) *PromotionEarningsQuery {
	peq.order = append(peq.order, o...)
	return peq
}

// QueryCommission chains the current query on the "commission" edge.
func (peq *PromotionEarningsQuery) QueryCommission() *PromotionCommissionQuery {
	query := (&PromotionCommissionClient{config: peq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := peq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := peq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionearnings.Table, promotionearnings.FieldID, selector),
			sqlgraph.To(promotioncommission.Table, promotioncommission.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, promotionearnings.CommissionTable, promotionearnings.CommissionColumn),
		)
		fromU = sqlgraph.SetNeighbors(peq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMember chains the current query on the "member" edge.
func (peq *PromotionEarningsQuery) QueryMember() *PromotionMemberQuery {
	query := (&PromotionMemberClient{config: peq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := peq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := peq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionearnings.Table, promotionearnings.FieldID, selector),
			sqlgraph.To(promotionmember.Table, promotionmember.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, promotionearnings.MemberTable, promotionearnings.MemberColumn),
		)
		fromU = sqlgraph.SetNeighbors(peq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRider chains the current query on the "rider" edge.
func (peq *PromotionEarningsQuery) QueryRider() *RiderQuery {
	query := (&RiderClient{config: peq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := peq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := peq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionearnings.Table, promotionearnings.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, promotionearnings.RiderTable, promotionearnings.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(peq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (peq *PromotionEarningsQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: peq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := peq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := peq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionearnings.Table, promotionearnings.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, promotionearnings.OrderTable, promotionearnings.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(peq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PromotionEarnings entity from the query.
// Returns a *NotFoundError when no PromotionEarnings was found.
func (peq *PromotionEarningsQuery) First(ctx context.Context) (*PromotionEarnings, error) {
	nodes, err := peq.Limit(1).All(setContextOp(ctx, peq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{promotionearnings.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (peq *PromotionEarningsQuery) FirstX(ctx context.Context) *PromotionEarnings {
	node, err := peq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PromotionEarnings ID from the query.
// Returns a *NotFoundError when no PromotionEarnings ID was found.
func (peq *PromotionEarningsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = peq.Limit(1).IDs(setContextOp(ctx, peq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{promotionearnings.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (peq *PromotionEarningsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := peq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PromotionEarnings entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PromotionEarnings entity is found.
// Returns a *NotFoundError when no PromotionEarnings entities are found.
func (peq *PromotionEarningsQuery) Only(ctx context.Context) (*PromotionEarnings, error) {
	nodes, err := peq.Limit(2).All(setContextOp(ctx, peq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{promotionearnings.Label}
	default:
		return nil, &NotSingularError{promotionearnings.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (peq *PromotionEarningsQuery) OnlyX(ctx context.Context) *PromotionEarnings {
	node, err := peq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PromotionEarnings ID in the query.
// Returns a *NotSingularError when more than one PromotionEarnings ID is found.
// Returns a *NotFoundError when no entities are found.
func (peq *PromotionEarningsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = peq.Limit(2).IDs(setContextOp(ctx, peq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{promotionearnings.Label}
	default:
		err = &NotSingularError{promotionearnings.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (peq *PromotionEarningsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := peq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PromotionEarningsSlice.
func (peq *PromotionEarningsQuery) All(ctx context.Context) ([]*PromotionEarnings, error) {
	ctx = setContextOp(ctx, peq.ctx, "All")
	if err := peq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PromotionEarnings, *PromotionEarningsQuery]()
	return withInterceptors[[]*PromotionEarnings](ctx, peq, qr, peq.inters)
}

// AllX is like All, but panics if an error occurs.
func (peq *PromotionEarningsQuery) AllX(ctx context.Context) []*PromotionEarnings {
	nodes, err := peq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PromotionEarnings IDs.
func (peq *PromotionEarningsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if peq.ctx.Unique == nil && peq.path != nil {
		peq.Unique(true)
	}
	ctx = setContextOp(ctx, peq.ctx, "IDs")
	if err = peq.Select(promotionearnings.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (peq *PromotionEarningsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := peq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (peq *PromotionEarningsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, peq.ctx, "Count")
	if err := peq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, peq, querierCount[*PromotionEarningsQuery](), peq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (peq *PromotionEarningsQuery) CountX(ctx context.Context) int {
	count, err := peq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (peq *PromotionEarningsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, peq.ctx, "Exist")
	switch _, err := peq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (peq *PromotionEarningsQuery) ExistX(ctx context.Context) bool {
	exist, err := peq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PromotionEarningsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (peq *PromotionEarningsQuery) Clone() *PromotionEarningsQuery {
	if peq == nil {
		return nil
	}
	return &PromotionEarningsQuery{
		config:         peq.config,
		ctx:            peq.ctx.Clone(),
		order:          append([]promotionearnings.OrderOption{}, peq.order...),
		inters:         append([]Interceptor{}, peq.inters...),
		predicates:     append([]predicate.PromotionEarnings{}, peq.predicates...),
		withCommission: peq.withCommission.Clone(),
		withMember:     peq.withMember.Clone(),
		withRider:      peq.withRider.Clone(),
		withOrder:      peq.withOrder.Clone(),
		// clone intermediate query.
		sql:  peq.sql.Clone(),
		path: peq.path,
	}
}

// WithCommission tells the query-builder to eager-load the nodes that are connected to
// the "commission" edge. The optional arguments are used to configure the query builder of the edge.
func (peq *PromotionEarningsQuery) WithCommission(opts ...func(*PromotionCommissionQuery)) *PromotionEarningsQuery {
	query := (&PromotionCommissionClient{config: peq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	peq.withCommission = query
	return peq
}

// WithMember tells the query-builder to eager-load the nodes that are connected to
// the "member" edge. The optional arguments are used to configure the query builder of the edge.
func (peq *PromotionEarningsQuery) WithMember(opts ...func(*PromotionMemberQuery)) *PromotionEarningsQuery {
	query := (&PromotionMemberClient{config: peq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	peq.withMember = query
	return peq
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (peq *PromotionEarningsQuery) WithRider(opts ...func(*RiderQuery)) *PromotionEarningsQuery {
	query := (&RiderClient{config: peq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	peq.withRider = query
	return peq
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (peq *PromotionEarningsQuery) WithOrder(opts ...func(*OrderQuery)) *PromotionEarningsQuery {
	query := (&OrderClient{config: peq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	peq.withOrder = query
	return peq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PromotionEarnings.Query().
//		GroupBy(promotionearnings.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (peq *PromotionEarningsQuery) GroupBy(field string, fields ...string) *PromotionEarningsGroupBy {
	peq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PromotionEarningsGroupBy{build: peq}
	grbuild.flds = &peq.ctx.Fields
	grbuild.label = promotionearnings.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PromotionEarnings.Query().
//		Select(promotionearnings.FieldCreatedAt).
//		Scan(ctx, &v)
func (peq *PromotionEarningsQuery) Select(fields ...string) *PromotionEarningsSelect {
	peq.ctx.Fields = append(peq.ctx.Fields, fields...)
	sbuild := &PromotionEarningsSelect{PromotionEarningsQuery: peq}
	sbuild.label = promotionearnings.Label
	sbuild.flds, sbuild.scan = &peq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PromotionEarningsSelect configured with the given aggregations.
func (peq *PromotionEarningsQuery) Aggregate(fns ...AggregateFunc) *PromotionEarningsSelect {
	return peq.Select().Aggregate(fns...)
}

func (peq *PromotionEarningsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range peq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, peq); err != nil {
				return err
			}
		}
	}
	for _, f := range peq.ctx.Fields {
		if !promotionearnings.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if peq.path != nil {
		prev, err := peq.path(ctx)
		if err != nil {
			return err
		}
		peq.sql = prev
	}
	return nil
}

func (peq *PromotionEarningsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PromotionEarnings, error) {
	var (
		nodes       = []*PromotionEarnings{}
		_spec       = peq.querySpec()
		loadedTypes = [4]bool{
			peq.withCommission != nil,
			peq.withMember != nil,
			peq.withRider != nil,
			peq.withOrder != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PromotionEarnings).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PromotionEarnings{config: peq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(peq.modifiers) > 0 {
		_spec.Modifiers = peq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, peq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := peq.withCommission; query != nil {
		if err := peq.loadCommission(ctx, query, nodes, nil,
			func(n *PromotionEarnings, e *PromotionCommission) { n.Edges.Commission = e }); err != nil {
			return nil, err
		}
	}
	if query := peq.withMember; query != nil {
		if err := peq.loadMember(ctx, query, nodes, nil,
			func(n *PromotionEarnings, e *PromotionMember) { n.Edges.Member = e }); err != nil {
			return nil, err
		}
	}
	if query := peq.withRider; query != nil {
		if err := peq.loadRider(ctx, query, nodes, nil,
			func(n *PromotionEarnings, e *Rider) { n.Edges.Rider = e }); err != nil {
			return nil, err
		}
	}
	if query := peq.withOrder; query != nil {
		if err := peq.loadOrder(ctx, query, nodes, nil,
			func(n *PromotionEarnings, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (peq *PromotionEarningsQuery) loadCommission(ctx context.Context, query *PromotionCommissionQuery, nodes []*PromotionEarnings, init func(*PromotionEarnings), assign func(*PromotionEarnings, *PromotionCommission)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionEarnings)
	for i := range nodes {
		fk := nodes[i].CommissionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(promotioncommission.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "commission_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (peq *PromotionEarningsQuery) loadMember(ctx context.Context, query *PromotionMemberQuery, nodes []*PromotionEarnings, init func(*PromotionEarnings), assign func(*PromotionEarnings, *PromotionMember)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionEarnings)
	for i := range nodes {
		fk := nodes[i].MemberID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(promotionmember.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "member_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (peq *PromotionEarningsQuery) loadRider(ctx context.Context, query *RiderQuery, nodes []*PromotionEarnings, init func(*PromotionEarnings), assign func(*PromotionEarnings, *Rider)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionEarnings)
	for i := range nodes {
		fk := nodes[i].RiderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rider.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (peq *PromotionEarningsQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*PromotionEarnings, init func(*PromotionEarnings), assign func(*PromotionEarnings, *Order)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionEarnings)
	for i := range nodes {
		if nodes[i].OrderID == nil {
			continue
		}
		fk := *nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (peq *PromotionEarningsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := peq.querySpec()
	if len(peq.modifiers) > 0 {
		_spec.Modifiers = peq.modifiers
	}
	_spec.Node.Columns = peq.ctx.Fields
	if len(peq.ctx.Fields) > 0 {
		_spec.Unique = peq.ctx.Unique != nil && *peq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, peq.driver, _spec)
}

func (peq *PromotionEarningsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(promotionearnings.Table, promotionearnings.Columns, sqlgraph.NewFieldSpec(promotionearnings.FieldID, field.TypeUint64))
	_spec.From = peq.sql
	if unique := peq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if peq.path != nil {
		_spec.Unique = true
	}
	if fields := peq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, promotionearnings.FieldID)
		for i := range fields {
			if fields[i] != promotionearnings.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if peq.withCommission != nil {
			_spec.Node.AddColumnOnce(promotionearnings.FieldCommissionID)
		}
		if peq.withMember != nil {
			_spec.Node.AddColumnOnce(promotionearnings.FieldMemberID)
		}
		if peq.withRider != nil {
			_spec.Node.AddColumnOnce(promotionearnings.FieldRiderID)
		}
		if peq.withOrder != nil {
			_spec.Node.AddColumnOnce(promotionearnings.FieldOrderID)
		}
	}
	if ps := peq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := peq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := peq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := peq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (peq *PromotionEarningsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(peq.driver.Dialect())
	t1 := builder.Table(promotionearnings.Table)
	columns := peq.ctx.Fields
	if len(columns) == 0 {
		columns = promotionearnings.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if peq.sql != nil {
		selector = peq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if peq.ctx.Unique != nil && *peq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range peq.modifiers {
		m(selector)
	}
	for _, p := range peq.predicates {
		p(selector)
	}
	for _, p := range peq.order {
		p(selector)
	}
	if offset := peq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := peq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (peq *PromotionEarningsQuery) Modify(modifiers ...func(s *sql.Selector)) *PromotionEarningsSelect {
	peq.modifiers = append(peq.modifiers, modifiers...)
	return peq.Select()
}

type PromotionEarningsQueryWith string

var (
	PromotionEarningsQueryWithCommission PromotionEarningsQueryWith = "Commission"
	PromotionEarningsQueryWithMember     PromotionEarningsQueryWith = "Member"
	PromotionEarningsQueryWithRider      PromotionEarningsQueryWith = "Rider"
	PromotionEarningsQueryWithOrder      PromotionEarningsQueryWith = "Order"
)

func (peq *PromotionEarningsQuery) With(withEdges ...PromotionEarningsQueryWith) *PromotionEarningsQuery {
	for _, v := range withEdges {
		switch v {
		case PromotionEarningsQueryWithCommission:
			peq.WithCommission()
		case PromotionEarningsQueryWithMember:
			peq.WithMember()
		case PromotionEarningsQueryWithRider:
			peq.WithRider()
		case PromotionEarningsQueryWithOrder:
			peq.WithOrder()
		}
	}
	return peq
}

// PromotionEarningsGroupBy is the group-by builder for PromotionEarnings entities.
type PromotionEarningsGroupBy struct {
	selector
	build *PromotionEarningsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pegb *PromotionEarningsGroupBy) Aggregate(fns ...AggregateFunc) *PromotionEarningsGroupBy {
	pegb.fns = append(pegb.fns, fns...)
	return pegb
}

// Scan applies the selector query and scans the result into the given value.
func (pegb *PromotionEarningsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pegb.build.ctx, "GroupBy")
	if err := pegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PromotionEarningsQuery, *PromotionEarningsGroupBy](ctx, pegb.build, pegb, pegb.build.inters, v)
}

func (pegb *PromotionEarningsGroupBy) sqlScan(ctx context.Context, root *PromotionEarningsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pegb.fns))
	for _, fn := range pegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pegb.flds)+len(pegb.fns))
		for _, f := range *pegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PromotionEarningsSelect is the builder for selecting fields of PromotionEarnings entities.
type PromotionEarningsSelect struct {
	*PromotionEarningsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pes *PromotionEarningsSelect) Aggregate(fns ...AggregateFunc) *PromotionEarningsSelect {
	pes.fns = append(pes.fns, fns...)
	return pes
}

// Scan applies the selector query and scans the result into the given value.
func (pes *PromotionEarningsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pes.ctx, "Select")
	if err := pes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PromotionEarningsQuery, *PromotionEarningsSelect](ctx, pes.PromotionEarningsQuery, pes, pes.inters, v)
}

func (pes *PromotionEarningsSelect) sqlScan(ctx context.Context, root *PromotionEarningsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pes.fns))
	for _, fn := range pes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pes *PromotionEarningsSelect) Modify(modifiers ...func(s *sql.Selector)) *PromotionEarningsSelect {
	pes.modifiers = append(pes.modifiers, modifiers...)
	return pes
}
