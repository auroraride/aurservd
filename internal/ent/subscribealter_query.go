// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/manager"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/rider"
	"github.com/auroraride/aurservd/internal/ent/subscribe"
	"github.com/auroraride/aurservd/internal/ent/subscribealter"
)

// SubscribeAlterQuery is the builder for querying SubscribeAlter entities.
type SubscribeAlterQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.SubscribeAlter
	// eager-loading edges.
	withRider     *RiderQuery
	withManager   *ManagerQuery
	withSubscribe *SubscribeQuery
	modifiers     []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SubscribeAlterQuery builder.
func (saq *SubscribeAlterQuery) Where(ps ...predicate.SubscribeAlter) *SubscribeAlterQuery {
	saq.predicates = append(saq.predicates, ps...)
	return saq
}

// Limit adds a limit step to the query.
func (saq *SubscribeAlterQuery) Limit(limit int) *SubscribeAlterQuery {
	saq.limit = &limit
	return saq
}

// Offset adds an offset step to the query.
func (saq *SubscribeAlterQuery) Offset(offset int) *SubscribeAlterQuery {
	saq.offset = &offset
	return saq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (saq *SubscribeAlterQuery) Unique(unique bool) *SubscribeAlterQuery {
	saq.unique = &unique
	return saq
}

// Order adds an order step to the query.
func (saq *SubscribeAlterQuery) Order(o ...OrderFunc) *SubscribeAlterQuery {
	saq.order = append(saq.order, o...)
	return saq
}

// QueryRider chains the current query on the "rider" edge.
func (saq *SubscribeAlterQuery) QueryRider() *RiderQuery {
	query := &RiderQuery{config: saq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := saq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := saq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(subscribealter.Table, subscribealter.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, subscribealter.RiderTable, subscribealter.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(saq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryManager chains the current query on the "manager" edge.
func (saq *SubscribeAlterQuery) QueryManager() *ManagerQuery {
	query := &ManagerQuery{config: saq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := saq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := saq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(subscribealter.Table, subscribealter.FieldID, selector),
			sqlgraph.To(manager.Table, manager.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, subscribealter.ManagerTable, subscribealter.ManagerColumn),
		)
		fromU = sqlgraph.SetNeighbors(saq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySubscribe chains the current query on the "subscribe" edge.
func (saq *SubscribeAlterQuery) QuerySubscribe() *SubscribeQuery {
	query := &SubscribeQuery{config: saq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := saq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := saq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(subscribealter.Table, subscribealter.FieldID, selector),
			sqlgraph.To(subscribe.Table, subscribe.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, subscribealter.SubscribeTable, subscribealter.SubscribeColumn),
		)
		fromU = sqlgraph.SetNeighbors(saq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SubscribeAlter entity from the query.
// Returns a *NotFoundError when no SubscribeAlter was found.
func (saq *SubscribeAlterQuery) First(ctx context.Context) (*SubscribeAlter, error) {
	nodes, err := saq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{subscribealter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (saq *SubscribeAlterQuery) FirstX(ctx context.Context) *SubscribeAlter {
	node, err := saq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SubscribeAlter ID from the query.
// Returns a *NotFoundError when no SubscribeAlter ID was found.
func (saq *SubscribeAlterQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = saq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{subscribealter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (saq *SubscribeAlterQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := saq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SubscribeAlter entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SubscribeAlter entity is found.
// Returns a *NotFoundError when no SubscribeAlter entities are found.
func (saq *SubscribeAlterQuery) Only(ctx context.Context) (*SubscribeAlter, error) {
	nodes, err := saq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{subscribealter.Label}
	default:
		return nil, &NotSingularError{subscribealter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (saq *SubscribeAlterQuery) OnlyX(ctx context.Context) *SubscribeAlter {
	node, err := saq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SubscribeAlter ID in the query.
// Returns a *NotSingularError when more than one SubscribeAlter ID is found.
// Returns a *NotFoundError when no entities are found.
func (saq *SubscribeAlterQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = saq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{subscribealter.Label}
	default:
		err = &NotSingularError{subscribealter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (saq *SubscribeAlterQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := saq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SubscribeAlters.
func (saq *SubscribeAlterQuery) All(ctx context.Context) ([]*SubscribeAlter, error) {
	if err := saq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return saq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (saq *SubscribeAlterQuery) AllX(ctx context.Context) []*SubscribeAlter {
	nodes, err := saq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SubscribeAlter IDs.
func (saq *SubscribeAlterQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	if err := saq.Select(subscribealter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (saq *SubscribeAlterQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := saq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (saq *SubscribeAlterQuery) Count(ctx context.Context) (int, error) {
	if err := saq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return saq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (saq *SubscribeAlterQuery) CountX(ctx context.Context) int {
	count, err := saq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (saq *SubscribeAlterQuery) Exist(ctx context.Context) (bool, error) {
	if err := saq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return saq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (saq *SubscribeAlterQuery) ExistX(ctx context.Context) bool {
	exist, err := saq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SubscribeAlterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (saq *SubscribeAlterQuery) Clone() *SubscribeAlterQuery {
	if saq == nil {
		return nil
	}
	return &SubscribeAlterQuery{
		config:        saq.config,
		limit:         saq.limit,
		offset:        saq.offset,
		order:         append([]OrderFunc{}, saq.order...),
		predicates:    append([]predicate.SubscribeAlter{}, saq.predicates...),
		withRider:     saq.withRider.Clone(),
		withManager:   saq.withManager.Clone(),
		withSubscribe: saq.withSubscribe.Clone(),
		// clone intermediate query.
		sql:    saq.sql.Clone(),
		path:   saq.path,
		unique: saq.unique,
	}
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (saq *SubscribeAlterQuery) WithRider(opts ...func(*RiderQuery)) *SubscribeAlterQuery {
	query := &RiderQuery{config: saq.config}
	for _, opt := range opts {
		opt(query)
	}
	saq.withRider = query
	return saq
}

// WithManager tells the query-builder to eager-load the nodes that are connected to
// the "manager" edge. The optional arguments are used to configure the query builder of the edge.
func (saq *SubscribeAlterQuery) WithManager(opts ...func(*ManagerQuery)) *SubscribeAlterQuery {
	query := &ManagerQuery{config: saq.config}
	for _, opt := range opts {
		opt(query)
	}
	saq.withManager = query
	return saq
}

// WithSubscribe tells the query-builder to eager-load the nodes that are connected to
// the "subscribe" edge. The optional arguments are used to configure the query builder of the edge.
func (saq *SubscribeAlterQuery) WithSubscribe(opts ...func(*SubscribeQuery)) *SubscribeAlterQuery {
	query := &SubscribeQuery{config: saq.config}
	for _, opt := range opts {
		opt(query)
	}
	saq.withSubscribe = query
	return saq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SubscribeAlter.Query().
//		GroupBy(subscribealter.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (saq *SubscribeAlterQuery) GroupBy(field string, fields ...string) *SubscribeAlterGroupBy {
	grbuild := &SubscribeAlterGroupBy{config: saq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := saq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return saq.sqlQuery(ctx), nil
	}
	grbuild.label = subscribealter.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SubscribeAlter.Query().
//		Select(subscribealter.FieldCreatedAt).
//		Scan(ctx, &v)
func (saq *SubscribeAlterQuery) Select(fields ...string) *SubscribeAlterSelect {
	saq.fields = append(saq.fields, fields...)
	selbuild := &SubscribeAlterSelect{SubscribeAlterQuery: saq}
	selbuild.label = subscribealter.Label
	selbuild.flds, selbuild.scan = &saq.fields, selbuild.Scan
	return selbuild
}

func (saq *SubscribeAlterQuery) prepareQuery(ctx context.Context) error {
	for _, f := range saq.fields {
		if !subscribealter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if saq.path != nil {
		prev, err := saq.path(ctx)
		if err != nil {
			return err
		}
		saq.sql = prev
	}
	return nil
}

func (saq *SubscribeAlterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SubscribeAlter, error) {
	var (
		nodes       = []*SubscribeAlter{}
		_spec       = saq.querySpec()
		loadedTypes = [3]bool{
			saq.withRider != nil,
			saq.withManager != nil,
			saq.withSubscribe != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*SubscribeAlter).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &SubscribeAlter{config: saq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(saq.modifiers) > 0 {
		_spec.Modifiers = saq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, saq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := saq.withRider; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*SubscribeAlter)
		for i := range nodes {
			fk := nodes[i].RiderID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(rider.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Rider = n
			}
		}
	}

	if query := saq.withManager; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*SubscribeAlter)
		for i := range nodes {
			fk := nodes[i].ManagerID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(manager.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "manager_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Manager = n
			}
		}
	}

	if query := saq.withSubscribe; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*SubscribeAlter)
		for i := range nodes {
			fk := nodes[i].SubscribeID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(subscribe.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "subscribe_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Subscribe = n
			}
		}
	}

	return nodes, nil
}

func (saq *SubscribeAlterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := saq.querySpec()
	if len(saq.modifiers) > 0 {
		_spec.Modifiers = saq.modifiers
	}
	_spec.Node.Columns = saq.fields
	if len(saq.fields) > 0 {
		_spec.Unique = saq.unique != nil && *saq.unique
	}
	return sqlgraph.CountNodes(ctx, saq.driver, _spec)
}

func (saq *SubscribeAlterQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := saq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (saq *SubscribeAlterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   subscribealter.Table,
			Columns: subscribealter.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: subscribealter.FieldID,
			},
		},
		From:   saq.sql,
		Unique: true,
	}
	if unique := saq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := saq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, subscribealter.FieldID)
		for i := range fields {
			if fields[i] != subscribealter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := saq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := saq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := saq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := saq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (saq *SubscribeAlterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(saq.driver.Dialect())
	t1 := builder.Table(subscribealter.Table)
	columns := saq.fields
	if len(columns) == 0 {
		columns = subscribealter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if saq.sql != nil {
		selector = saq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if saq.unique != nil && *saq.unique {
		selector.Distinct()
	}
	for _, m := range saq.modifiers {
		m(selector)
	}
	for _, p := range saq.predicates {
		p(selector)
	}
	for _, p := range saq.order {
		p(selector)
	}
	if offset := saq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := saq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (saq *SubscribeAlterQuery) Modify(modifiers ...func(s *sql.Selector)) *SubscribeAlterSelect {
	saq.modifiers = append(saq.modifiers, modifiers...)
	return saq.Select()
}

// SubscribeAlterGroupBy is the group-by builder for SubscribeAlter entities.
type SubscribeAlterGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sagb *SubscribeAlterGroupBy) Aggregate(fns ...AggregateFunc) *SubscribeAlterGroupBy {
	sagb.fns = append(sagb.fns, fns...)
	return sagb
}

// Scan applies the group-by query and scans the result into the given value.
func (sagb *SubscribeAlterGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := sagb.path(ctx)
	if err != nil {
		return err
	}
	sagb.sql = query
	return sagb.sqlScan(ctx, v)
}

func (sagb *SubscribeAlterGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range sagb.fields {
		if !subscribealter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := sagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (sagb *SubscribeAlterGroupBy) sqlQuery() *sql.Selector {
	selector := sagb.sql.Select()
	aggregation := make([]string, 0, len(sagb.fns))
	for _, fn := range sagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(sagb.fields)+len(sagb.fns))
		for _, f := range sagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(sagb.fields...)...)
}

// SubscribeAlterSelect is the builder for selecting fields of SubscribeAlter entities.
type SubscribeAlterSelect struct {
	*SubscribeAlterQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (sas *SubscribeAlterSelect) Scan(ctx context.Context, v interface{}) error {
	if err := sas.prepareQuery(ctx); err != nil {
		return err
	}
	sas.sql = sas.SubscribeAlterQuery.sqlQuery(ctx)
	return sas.sqlScan(ctx, v)
}

func (sas *SubscribeAlterSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := sas.sql.Query()
	if err := sas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sas *SubscribeAlterSelect) Modify(modifiers ...func(s *sql.Selector)) *SubscribeAlterSelect {
	sas.modifiers = append(sas.modifiers, modifiers...)
	return sas
}
