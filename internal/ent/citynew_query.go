// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/citynew"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// CityNewQuery is the builder for querying CityNew entities.
type CityNewQuery struct {
	config
	ctx        *QueryContext
	order      []citynew.OrderOption
	inters     []Interceptor
	predicates []predicate.CityNew
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CityNewQuery builder.
func (cnq *CityNewQuery) Where(ps ...predicate.CityNew) *CityNewQuery {
	cnq.predicates = append(cnq.predicates, ps...)
	return cnq
}

// Limit the number of records to be returned by this query.
func (cnq *CityNewQuery) Limit(limit int) *CityNewQuery {
	cnq.ctx.Limit = &limit
	return cnq
}

// Offset to start from.
func (cnq *CityNewQuery) Offset(offset int) *CityNewQuery {
	cnq.ctx.Offset = &offset
	return cnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cnq *CityNewQuery) Unique(unique bool) *CityNewQuery {
	cnq.ctx.Unique = &unique
	return cnq
}

// Order specifies how the records should be ordered.
func (cnq *CityNewQuery) Order(o ...citynew.OrderOption) *CityNewQuery {
	cnq.order = append(cnq.order, o...)
	return cnq
}

// First returns the first CityNew entity from the query.
// Returns a *NotFoundError when no CityNew was found.
func (cnq *CityNewQuery) First(ctx context.Context) (*CityNew, error) {
	nodes, err := cnq.Limit(1).All(setContextOp(ctx, cnq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{citynew.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cnq *CityNewQuery) FirstX(ctx context.Context) *CityNew {
	node, err := cnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CityNew ID from the query.
// Returns a *NotFoundError when no CityNew ID was found.
func (cnq *CityNewQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = cnq.Limit(1).IDs(setContextOp(ctx, cnq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{citynew.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cnq *CityNewQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := cnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CityNew entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CityNew entity is found.
// Returns a *NotFoundError when no CityNew entities are found.
func (cnq *CityNewQuery) Only(ctx context.Context) (*CityNew, error) {
	nodes, err := cnq.Limit(2).All(setContextOp(ctx, cnq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{citynew.Label}
	default:
		return nil, &NotSingularError{citynew.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cnq *CityNewQuery) OnlyX(ctx context.Context) *CityNew {
	node, err := cnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CityNew ID in the query.
// Returns a *NotSingularError when more than one CityNew ID is found.
// Returns a *NotFoundError when no entities are found.
func (cnq *CityNewQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = cnq.Limit(2).IDs(setContextOp(ctx, cnq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{citynew.Label}
	default:
		err = &NotSingularError{citynew.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cnq *CityNewQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := cnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CityNews.
func (cnq *CityNewQuery) All(ctx context.Context) ([]*CityNew, error) {
	ctx = setContextOp(ctx, cnq.ctx, "All")
	if err := cnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CityNew, *CityNewQuery]()
	return withInterceptors[[]*CityNew](ctx, cnq, qr, cnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cnq *CityNewQuery) AllX(ctx context.Context) []*CityNew {
	nodes, err := cnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CityNew IDs.
func (cnq *CityNewQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if cnq.ctx.Unique == nil && cnq.path != nil {
		cnq.Unique(true)
	}
	ctx = setContextOp(ctx, cnq.ctx, "IDs")
	if err = cnq.Select(citynew.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cnq *CityNewQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := cnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cnq *CityNewQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cnq.ctx, "Count")
	if err := cnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cnq, querierCount[*CityNewQuery](), cnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cnq *CityNewQuery) CountX(ctx context.Context) int {
	count, err := cnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cnq *CityNewQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cnq.ctx, "Exist")
	switch _, err := cnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cnq *CityNewQuery) ExistX(ctx context.Context) bool {
	exist, err := cnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CityNewQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cnq *CityNewQuery) Clone() *CityNewQuery {
	if cnq == nil {
		return nil
	}
	return &CityNewQuery{
		config:     cnq.config,
		ctx:        cnq.ctx.Clone(),
		order:      append([]citynew.OrderOption{}, cnq.order...),
		inters:     append([]Interceptor{}, cnq.inters...),
		predicates: append([]predicate.CityNew{}, cnq.predicates...),
		// clone intermediate query.
		sql:  cnq.sql.Clone(),
		path: cnq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CityNew.Query().
//		GroupBy(citynew.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cnq *CityNewQuery) GroupBy(field string, fields ...string) *CityNewGroupBy {
	cnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CityNewGroupBy{build: cnq}
	grbuild.flds = &cnq.ctx.Fields
	grbuild.label = citynew.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CityNew.Query().
//		Select(citynew.FieldCreatedAt).
//		Scan(ctx, &v)
func (cnq *CityNewQuery) Select(fields ...string) *CityNewSelect {
	cnq.ctx.Fields = append(cnq.ctx.Fields, fields...)
	sbuild := &CityNewSelect{CityNewQuery: cnq}
	sbuild.label = citynew.Label
	sbuild.flds, sbuild.scan = &cnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CityNewSelect configured with the given aggregations.
func (cnq *CityNewQuery) Aggregate(fns ...AggregateFunc) *CityNewSelect {
	return cnq.Select().Aggregate(fns...)
}

func (cnq *CityNewQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cnq); err != nil {
				return err
			}
		}
	}
	for _, f := range cnq.ctx.Fields {
		if !citynew.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cnq.path != nil {
		prev, err := cnq.path(ctx)
		if err != nil {
			return err
		}
		cnq.sql = prev
	}
	return nil
}

func (cnq *CityNewQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CityNew, error) {
	var (
		nodes = []*CityNew{}
		_spec = cnq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CityNew).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CityNew{config: cnq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(cnq.modifiers) > 0 {
		_spec.Modifiers = cnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cnq *CityNewQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cnq.querySpec()
	if len(cnq.modifiers) > 0 {
		_spec.Modifiers = cnq.modifiers
	}
	_spec.Node.Columns = cnq.ctx.Fields
	if len(cnq.ctx.Fields) > 0 {
		_spec.Unique = cnq.ctx.Unique != nil && *cnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cnq.driver, _spec)
}

func (cnq *CityNewQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(citynew.Table, citynew.Columns, sqlgraph.NewFieldSpec(citynew.FieldID, field.TypeUint64))
	_spec.From = cnq.sql
	if unique := cnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cnq.path != nil {
		_spec.Unique = true
	}
	if fields := cnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, citynew.FieldID)
		for i := range fields {
			if fields[i] != citynew.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cnq *CityNewQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cnq.driver.Dialect())
	t1 := builder.Table(citynew.Table)
	columns := cnq.ctx.Fields
	if len(columns) == 0 {
		columns = citynew.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cnq.sql != nil {
		selector = cnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cnq.ctx.Unique != nil && *cnq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range cnq.modifiers {
		m(selector)
	}
	for _, p := range cnq.predicates {
		p(selector)
	}
	for _, p := range cnq.order {
		p(selector)
	}
	if offset := cnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cnq *CityNewQuery) Modify(modifiers ...func(s *sql.Selector)) *CityNewSelect {
	cnq.modifiers = append(cnq.modifiers, modifiers...)
	return cnq.Select()
}

type CityNewQueryWith string

var ()

func (cnq *CityNewQuery) With(withEdges ...CityNewQueryWith) *CityNewQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return cnq
}

// CityNewGroupBy is the group-by builder for CityNew entities.
type CityNewGroupBy struct {
	selector
	build *CityNewQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cngb *CityNewGroupBy) Aggregate(fns ...AggregateFunc) *CityNewGroupBy {
	cngb.fns = append(cngb.fns, fns...)
	return cngb
}

// Scan applies the selector query and scans the result into the given value.
func (cngb *CityNewGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cngb.build.ctx, "GroupBy")
	if err := cngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CityNewQuery, *CityNewGroupBy](ctx, cngb.build, cngb, cngb.build.inters, v)
}

func (cngb *CityNewGroupBy) sqlScan(ctx context.Context, root *CityNewQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cngb.fns))
	for _, fn := range cngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cngb.flds)+len(cngb.fns))
		for _, f := range *cngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CityNewSelect is the builder for selecting fields of CityNew entities.
type CityNewSelect struct {
	*CityNewQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cns *CityNewSelect) Aggregate(fns ...AggregateFunc) *CityNewSelect {
	cns.fns = append(cns.fns, fns...)
	return cns
}

// Scan applies the selector query and scans the result into the given value.
func (cns *CityNewSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cns.ctx, "Select")
	if err := cns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CityNewQuery, *CityNewSelect](ctx, cns.CityNewQuery, cns, cns.inters, v)
}

func (cns *CityNewSelect) sqlScan(ctx context.Context, root *CityNewQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cns.fns))
	for _, fn := range cns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cns *CityNewSelect) Modify(modifiers ...func(s *sql.Selector)) *CityNewSelect {
	cns.modifiers = append(cns.modifiers, modifiers...)
	return cns
}
