// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/enterprise"
	"github.com/auroraride/aurservd/internal/ent/enterprisecontract"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// EnterpriseContractQuery is the builder for querying EnterpriseContract entities.
type EnterpriseContractQuery struct {
	config
	ctx            *QueryContext
	order          []enterprisecontract.OrderOption
	inters         []Interceptor
	predicates     []predicate.EnterpriseContract
	withEnterprise *EnterpriseQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EnterpriseContractQuery builder.
func (ecq *EnterpriseContractQuery) Where(ps ...predicate.EnterpriseContract) *EnterpriseContractQuery {
	ecq.predicates = append(ecq.predicates, ps...)
	return ecq
}

// Limit the number of records to be returned by this query.
func (ecq *EnterpriseContractQuery) Limit(limit int) *EnterpriseContractQuery {
	ecq.ctx.Limit = &limit
	return ecq
}

// Offset to start from.
func (ecq *EnterpriseContractQuery) Offset(offset int) *EnterpriseContractQuery {
	ecq.ctx.Offset = &offset
	return ecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ecq *EnterpriseContractQuery) Unique(unique bool) *EnterpriseContractQuery {
	ecq.ctx.Unique = &unique
	return ecq
}

// Order specifies how the records should be ordered.
func (ecq *EnterpriseContractQuery) Order(o ...enterprisecontract.OrderOption) *EnterpriseContractQuery {
	ecq.order = append(ecq.order, o...)
	return ecq
}

// QueryEnterprise chains the current query on the "enterprise" edge.
func (ecq *EnterpriseContractQuery) QueryEnterprise() *EnterpriseQuery {
	query := (&EnterpriseClient{config: ecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enterprisecontract.Table, enterprisecontract.FieldID, selector),
			sqlgraph.To(enterprise.Table, enterprise.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, enterprisecontract.EnterpriseTable, enterprisecontract.EnterpriseColumn),
		)
		fromU = sqlgraph.SetNeighbors(ecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EnterpriseContract entity from the query.
// Returns a *NotFoundError when no EnterpriseContract was found.
func (ecq *EnterpriseContractQuery) First(ctx context.Context) (*EnterpriseContract, error) {
	nodes, err := ecq.Limit(1).All(setContextOp(ctx, ecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{enterprisecontract.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) FirstX(ctx context.Context) *EnterpriseContract {
	node, err := ecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EnterpriseContract ID from the query.
// Returns a *NotFoundError when no EnterpriseContract ID was found.
func (ecq *EnterpriseContractQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ecq.Limit(1).IDs(setContextOp(ctx, ecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{enterprisecontract.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := ecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EnterpriseContract entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EnterpriseContract entity is found.
// Returns a *NotFoundError when no EnterpriseContract entities are found.
func (ecq *EnterpriseContractQuery) Only(ctx context.Context) (*EnterpriseContract, error) {
	nodes, err := ecq.Limit(2).All(setContextOp(ctx, ecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{enterprisecontract.Label}
	default:
		return nil, &NotSingularError{enterprisecontract.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) OnlyX(ctx context.Context) *EnterpriseContract {
	node, err := ecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EnterpriseContract ID in the query.
// Returns a *NotSingularError when more than one EnterpriseContract ID is found.
// Returns a *NotFoundError when no entities are found.
func (ecq *EnterpriseContractQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ecq.Limit(2).IDs(setContextOp(ctx, ecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{enterprisecontract.Label}
	default:
		err = &NotSingularError{enterprisecontract.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := ecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EnterpriseContracts.
func (ecq *EnterpriseContractQuery) All(ctx context.Context) ([]*EnterpriseContract, error) {
	ctx = setContextOp(ctx, ecq.ctx, "All")
	if err := ecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EnterpriseContract, *EnterpriseContractQuery]()
	return withInterceptors[[]*EnterpriseContract](ctx, ecq, qr, ecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) AllX(ctx context.Context) []*EnterpriseContract {
	nodes, err := ecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EnterpriseContract IDs.
func (ecq *EnterpriseContractQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if ecq.ctx.Unique == nil && ecq.path != nil {
		ecq.Unique(true)
	}
	ctx = setContextOp(ctx, ecq.ctx, "IDs")
	if err = ecq.Select(enterprisecontract.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := ecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ecq *EnterpriseContractQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ecq.ctx, "Count")
	if err := ecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ecq, querierCount[*EnterpriseContractQuery](), ecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) CountX(ctx context.Context) int {
	count, err := ecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ecq *EnterpriseContractQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ecq.ctx, "Exist")
	switch _, err := ecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ecq *EnterpriseContractQuery) ExistX(ctx context.Context) bool {
	exist, err := ecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EnterpriseContractQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ecq *EnterpriseContractQuery) Clone() *EnterpriseContractQuery {
	if ecq == nil {
		return nil
	}
	return &EnterpriseContractQuery{
		config:         ecq.config,
		ctx:            ecq.ctx.Clone(),
		order:          append([]enterprisecontract.OrderOption{}, ecq.order...),
		inters:         append([]Interceptor{}, ecq.inters...),
		predicates:     append([]predicate.EnterpriseContract{}, ecq.predicates...),
		withEnterprise: ecq.withEnterprise.Clone(),
		// clone intermediate query.
		sql:  ecq.sql.Clone(),
		path: ecq.path,
	}
}

// WithEnterprise tells the query-builder to eager-load the nodes that are connected to
// the "enterprise" edge. The optional arguments are used to configure the query builder of the edge.
func (ecq *EnterpriseContractQuery) WithEnterprise(opts ...func(*EnterpriseQuery)) *EnterpriseContractQuery {
	query := (&EnterpriseClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ecq.withEnterprise = query
	return ecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EnterpriseContract.Query().
//		GroupBy(enterprisecontract.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ecq *EnterpriseContractQuery) GroupBy(field string, fields ...string) *EnterpriseContractGroupBy {
	ecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EnterpriseContractGroupBy{build: ecq}
	grbuild.flds = &ecq.ctx.Fields
	grbuild.label = enterprisecontract.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EnterpriseContract.Query().
//		Select(enterprisecontract.FieldCreatedAt).
//		Scan(ctx, &v)
func (ecq *EnterpriseContractQuery) Select(fields ...string) *EnterpriseContractSelect {
	ecq.ctx.Fields = append(ecq.ctx.Fields, fields...)
	sbuild := &EnterpriseContractSelect{EnterpriseContractQuery: ecq}
	sbuild.label = enterprisecontract.Label
	sbuild.flds, sbuild.scan = &ecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EnterpriseContractSelect configured with the given aggregations.
func (ecq *EnterpriseContractQuery) Aggregate(fns ...AggregateFunc) *EnterpriseContractSelect {
	return ecq.Select().Aggregate(fns...)
}

func (ecq *EnterpriseContractQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ecq); err != nil {
				return err
			}
		}
	}
	for _, f := range ecq.ctx.Fields {
		if !enterprisecontract.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ecq.path != nil {
		prev, err := ecq.path(ctx)
		if err != nil {
			return err
		}
		ecq.sql = prev
	}
	return nil
}

func (ecq *EnterpriseContractQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EnterpriseContract, error) {
	var (
		nodes       = []*EnterpriseContract{}
		_spec       = ecq.querySpec()
		loadedTypes = [1]bool{
			ecq.withEnterprise != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EnterpriseContract).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EnterpriseContract{config: ecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ecq.modifiers) > 0 {
		_spec.Modifiers = ecq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ecq.withEnterprise; query != nil {
		if err := ecq.loadEnterprise(ctx, query, nodes, nil,
			func(n *EnterpriseContract, e *Enterprise) { n.Edges.Enterprise = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ecq *EnterpriseContractQuery) loadEnterprise(ctx context.Context, query *EnterpriseQuery, nodes []*EnterpriseContract, init func(*EnterpriseContract), assign func(*EnterpriseContract, *Enterprise)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*EnterpriseContract)
	for i := range nodes {
		fk := nodes[i].EnterpriseID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(enterprise.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "enterprise_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ecq *EnterpriseContractQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ecq.querySpec()
	if len(ecq.modifiers) > 0 {
		_spec.Modifiers = ecq.modifiers
	}
	_spec.Node.Columns = ecq.ctx.Fields
	if len(ecq.ctx.Fields) > 0 {
		_spec.Unique = ecq.ctx.Unique != nil && *ecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ecq.driver, _spec)
}

func (ecq *EnterpriseContractQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(enterprisecontract.Table, enterprisecontract.Columns, sqlgraph.NewFieldSpec(enterprisecontract.FieldID, field.TypeUint64))
	_spec.From = ecq.sql
	if unique := ecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ecq.path != nil {
		_spec.Unique = true
	}
	if fields := ecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, enterprisecontract.FieldID)
		for i := range fields {
			if fields[i] != enterprisecontract.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ecq.withEnterprise != nil {
			_spec.Node.AddColumnOnce(enterprisecontract.FieldEnterpriseID)
		}
	}
	if ps := ecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ecq *EnterpriseContractQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ecq.driver.Dialect())
	t1 := builder.Table(enterprisecontract.Table)
	columns := ecq.ctx.Fields
	if len(columns) == 0 {
		columns = enterprisecontract.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ecq.sql != nil {
		selector = ecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ecq.ctx.Unique != nil && *ecq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ecq.modifiers {
		m(selector)
	}
	for _, p := range ecq.predicates {
		p(selector)
	}
	for _, p := range ecq.order {
		p(selector)
	}
	if offset := ecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ecq *EnterpriseContractQuery) Modify(modifiers ...func(s *sql.Selector)) *EnterpriseContractSelect {
	ecq.modifiers = append(ecq.modifiers, modifiers...)
	return ecq.Select()
}

type EnterpriseContractQueryWith string

var (
	EnterpriseContractQueryWithEnterprise EnterpriseContractQueryWith = "Enterprise"
)

func (ecq *EnterpriseContractQuery) With(withEdges ...EnterpriseContractQueryWith) *EnterpriseContractQuery {
	for _, v := range withEdges {
		switch v {
		case EnterpriseContractQueryWithEnterprise:
			ecq.WithEnterprise()
		}
	}
	return ecq
}

// EnterpriseContractGroupBy is the group-by builder for EnterpriseContract entities.
type EnterpriseContractGroupBy struct {
	selector
	build *EnterpriseContractQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ecgb *EnterpriseContractGroupBy) Aggregate(fns ...AggregateFunc) *EnterpriseContractGroupBy {
	ecgb.fns = append(ecgb.fns, fns...)
	return ecgb
}

// Scan applies the selector query and scans the result into the given value.
func (ecgb *EnterpriseContractGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ecgb.build.ctx, "GroupBy")
	if err := ecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnterpriseContractQuery, *EnterpriseContractGroupBy](ctx, ecgb.build, ecgb, ecgb.build.inters, v)
}

func (ecgb *EnterpriseContractGroupBy) sqlScan(ctx context.Context, root *EnterpriseContractQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ecgb.fns))
	for _, fn := range ecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ecgb.flds)+len(ecgb.fns))
		for _, f := range *ecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EnterpriseContractSelect is the builder for selecting fields of EnterpriseContract entities.
type EnterpriseContractSelect struct {
	*EnterpriseContractQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ecs *EnterpriseContractSelect) Aggregate(fns ...AggregateFunc) *EnterpriseContractSelect {
	ecs.fns = append(ecs.fns, fns...)
	return ecs
}

// Scan applies the selector query and scans the result into the given value.
func (ecs *EnterpriseContractSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ecs.ctx, "Select")
	if err := ecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EnterpriseContractQuery, *EnterpriseContractSelect](ctx, ecs.EnterpriseContractQuery, ecs, ecs.inters, v)
}

func (ecs *EnterpriseContractSelect) sqlScan(ctx context.Context, root *EnterpriseContractQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ecs.fns))
	for _, fn := range ecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ecs *EnterpriseContractSelect) Modify(modifiers ...func(s *sql.Selector)) *EnterpriseContractSelect {
	ecs.modifiers = append(ecs.modifiers, modifiers...)
	return ecs
}
