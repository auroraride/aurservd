// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/riderphonedevice"
)

// RiderPhoneDeviceQuery is the builder for querying RiderPhoneDevice entities.
type RiderPhoneDeviceQuery struct {
	config
	ctx        *QueryContext
	order      []riderphonedevice.OrderOption
	inters     []Interceptor
	predicates []predicate.RiderPhoneDevice
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RiderPhoneDeviceQuery builder.
func (rpdq *RiderPhoneDeviceQuery) Where(ps ...predicate.RiderPhoneDevice) *RiderPhoneDeviceQuery {
	rpdq.predicates = append(rpdq.predicates, ps...)
	return rpdq
}

// Limit the number of records to be returned by this query.
func (rpdq *RiderPhoneDeviceQuery) Limit(limit int) *RiderPhoneDeviceQuery {
	rpdq.ctx.Limit = &limit
	return rpdq
}

// Offset to start from.
func (rpdq *RiderPhoneDeviceQuery) Offset(offset int) *RiderPhoneDeviceQuery {
	rpdq.ctx.Offset = &offset
	return rpdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpdq *RiderPhoneDeviceQuery) Unique(unique bool) *RiderPhoneDeviceQuery {
	rpdq.ctx.Unique = &unique
	return rpdq
}

// Order specifies how the records should be ordered.
func (rpdq *RiderPhoneDeviceQuery) Order(o ...riderphonedevice.OrderOption) *RiderPhoneDeviceQuery {
	rpdq.order = append(rpdq.order, o...)
	return rpdq
}

// First returns the first RiderPhoneDevice entity from the query.
// Returns a *NotFoundError when no RiderPhoneDevice was found.
func (rpdq *RiderPhoneDeviceQuery) First(ctx context.Context) (*RiderPhoneDevice, error) {
	nodes, err := rpdq.Limit(1).All(setContextOp(ctx, rpdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{riderphonedevice.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) FirstX(ctx context.Context) *RiderPhoneDevice {
	node, err := rpdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RiderPhoneDevice ID from the query.
// Returns a *NotFoundError when no RiderPhoneDevice ID was found.
func (rpdq *RiderPhoneDeviceQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rpdq.Limit(1).IDs(setContextOp(ctx, rpdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{riderphonedevice.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := rpdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RiderPhoneDevice entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RiderPhoneDevice entity is found.
// Returns a *NotFoundError when no RiderPhoneDevice entities are found.
func (rpdq *RiderPhoneDeviceQuery) Only(ctx context.Context) (*RiderPhoneDevice, error) {
	nodes, err := rpdq.Limit(2).All(setContextOp(ctx, rpdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{riderphonedevice.Label}
	default:
		return nil, &NotSingularError{riderphonedevice.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) OnlyX(ctx context.Context) *RiderPhoneDevice {
	node, err := rpdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RiderPhoneDevice ID in the query.
// Returns a *NotSingularError when more than one RiderPhoneDevice ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpdq *RiderPhoneDeviceQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rpdq.Limit(2).IDs(setContextOp(ctx, rpdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{riderphonedevice.Label}
	default:
		err = &NotSingularError{riderphonedevice.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := rpdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RiderPhoneDevices.
func (rpdq *RiderPhoneDeviceQuery) All(ctx context.Context) ([]*RiderPhoneDevice, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "All")
	if err := rpdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RiderPhoneDevice, *RiderPhoneDeviceQuery]()
	return withInterceptors[[]*RiderPhoneDevice](ctx, rpdq, qr, rpdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) AllX(ctx context.Context) []*RiderPhoneDevice {
	nodes, err := rpdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RiderPhoneDevice IDs.
func (rpdq *RiderPhoneDeviceQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if rpdq.ctx.Unique == nil && rpdq.path != nil {
		rpdq.Unique(true)
	}
	ctx = setContextOp(ctx, rpdq.ctx, "IDs")
	if err = rpdq.Select(riderphonedevice.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := rpdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpdq *RiderPhoneDeviceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "Count")
	if err := rpdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpdq, querierCount[*RiderPhoneDeviceQuery](), rpdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) CountX(ctx context.Context) int {
	count, err := rpdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpdq *RiderPhoneDeviceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpdq.ctx, "Exist")
	switch _, err := rpdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpdq *RiderPhoneDeviceQuery) ExistX(ctx context.Context) bool {
	exist, err := rpdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RiderPhoneDeviceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpdq *RiderPhoneDeviceQuery) Clone() *RiderPhoneDeviceQuery {
	if rpdq == nil {
		return nil
	}
	return &RiderPhoneDeviceQuery{
		config:     rpdq.config,
		ctx:        rpdq.ctx.Clone(),
		order:      append([]riderphonedevice.OrderOption{}, rpdq.order...),
		inters:     append([]Interceptor{}, rpdq.inters...),
		predicates: append([]predicate.RiderPhoneDevice{}, rpdq.predicates...),
		// clone intermediate query.
		sql:  rpdq.sql.Clone(),
		path: rpdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RiderPhoneDevice.Query().
//		GroupBy(riderphonedevice.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpdq *RiderPhoneDeviceQuery) GroupBy(field string, fields ...string) *RiderPhoneDeviceGroupBy {
	rpdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RiderPhoneDeviceGroupBy{build: rpdq}
	grbuild.flds = &rpdq.ctx.Fields
	grbuild.label = riderphonedevice.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RiderPhoneDevice.Query().
//		Select(riderphonedevice.FieldCreatedAt).
//		Scan(ctx, &v)
func (rpdq *RiderPhoneDeviceQuery) Select(fields ...string) *RiderPhoneDeviceSelect {
	rpdq.ctx.Fields = append(rpdq.ctx.Fields, fields...)
	sbuild := &RiderPhoneDeviceSelect{RiderPhoneDeviceQuery: rpdq}
	sbuild.label = riderphonedevice.Label
	sbuild.flds, sbuild.scan = &rpdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RiderPhoneDeviceSelect configured with the given aggregations.
func (rpdq *RiderPhoneDeviceQuery) Aggregate(fns ...AggregateFunc) *RiderPhoneDeviceSelect {
	return rpdq.Select().Aggregate(fns...)
}

func (rpdq *RiderPhoneDeviceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpdq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpdq.ctx.Fields {
		if !riderphonedevice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpdq.path != nil {
		prev, err := rpdq.path(ctx)
		if err != nil {
			return err
		}
		rpdq.sql = prev
	}
	return nil
}

func (rpdq *RiderPhoneDeviceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RiderPhoneDevice, error) {
	var (
		nodes = []*RiderPhoneDevice{}
		_spec = rpdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RiderPhoneDevice).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RiderPhoneDevice{config: rpdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(rpdq.modifiers) > 0 {
		_spec.Modifiers = rpdq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rpdq *RiderPhoneDeviceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpdq.querySpec()
	if len(rpdq.modifiers) > 0 {
		_spec.Modifiers = rpdq.modifiers
	}
	_spec.Node.Columns = rpdq.ctx.Fields
	if len(rpdq.ctx.Fields) > 0 {
		_spec.Unique = rpdq.ctx.Unique != nil && *rpdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpdq.driver, _spec)
}

func (rpdq *RiderPhoneDeviceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(riderphonedevice.Table, riderphonedevice.Columns, sqlgraph.NewFieldSpec(riderphonedevice.FieldID, field.TypeUint64))
	_spec.From = rpdq.sql
	if unique := rpdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpdq.path != nil {
		_spec.Unique = true
	}
	if fields := rpdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, riderphonedevice.FieldID)
		for i := range fields {
			if fields[i] != riderphonedevice.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rpdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpdq *RiderPhoneDeviceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpdq.driver.Dialect())
	t1 := builder.Table(riderphonedevice.Table)
	columns := rpdq.ctx.Fields
	if len(columns) == 0 {
		columns = riderphonedevice.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpdq.sql != nil {
		selector = rpdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpdq.ctx.Unique != nil && *rpdq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rpdq.modifiers {
		m(selector)
	}
	for _, p := range rpdq.predicates {
		p(selector)
	}
	for _, p := range rpdq.order {
		p(selector)
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rpdq *RiderPhoneDeviceQuery) Modify(modifiers ...func(s *sql.Selector)) *RiderPhoneDeviceSelect {
	rpdq.modifiers = append(rpdq.modifiers, modifiers...)
	return rpdq.Select()
}

type RiderPhoneDeviceQueryWith string

var ()

func (rpdq *RiderPhoneDeviceQuery) With(withEdges ...RiderPhoneDeviceQueryWith) *RiderPhoneDeviceQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return rpdq
}

// RiderPhoneDeviceGroupBy is the group-by builder for RiderPhoneDevice entities.
type RiderPhoneDeviceGroupBy struct {
	selector
	build *RiderPhoneDeviceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpdgb *RiderPhoneDeviceGroupBy) Aggregate(fns ...AggregateFunc) *RiderPhoneDeviceGroupBy {
	rpdgb.fns = append(rpdgb.fns, fns...)
	return rpdgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpdgb *RiderPhoneDeviceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpdgb.build.ctx, "GroupBy")
	if err := rpdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiderPhoneDeviceQuery, *RiderPhoneDeviceGroupBy](ctx, rpdgb.build, rpdgb, rpdgb.build.inters, v)
}

func (rpdgb *RiderPhoneDeviceGroupBy) sqlScan(ctx context.Context, root *RiderPhoneDeviceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpdgb.fns))
	for _, fn := range rpdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpdgb.flds)+len(rpdgb.fns))
		for _, f := range *rpdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RiderPhoneDeviceSelect is the builder for selecting fields of RiderPhoneDevice entities.
type RiderPhoneDeviceSelect struct {
	*RiderPhoneDeviceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rpds *RiderPhoneDeviceSelect) Aggregate(fns ...AggregateFunc) *RiderPhoneDeviceSelect {
	rpds.fns = append(rpds.fns, fns...)
	return rpds
}

// Scan applies the selector query and scans the result into the given value.
func (rpds *RiderPhoneDeviceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpds.ctx, "Select")
	if err := rpds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiderPhoneDeviceQuery, *RiderPhoneDeviceSelect](ctx, rpds.RiderPhoneDeviceQuery, rpds, rpds.inters, v)
}

func (rpds *RiderPhoneDeviceSelect) sqlScan(ctx context.Context, root *RiderPhoneDeviceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rpds.fns))
	for _, fn := range rpds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rpds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rpds *RiderPhoneDeviceSelect) Modify(modifiers ...func(s *sql.Selector)) *RiderPhoneDeviceSelect {
	rpds.modifiers = append(rpds.modifiers, modifiers...)
	return rpds
}
