// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/batterymodelnew"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// BatteryModelNewQuery is the builder for querying BatteryModelNew entities.
type BatteryModelNewQuery struct {
	config
	ctx        *QueryContext
	order      []batterymodelnew.OrderOption
	inters     []Interceptor
	predicates []predicate.BatteryModelNew
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BatteryModelNewQuery builder.
func (bmnq *BatteryModelNewQuery) Where(ps ...predicate.BatteryModelNew) *BatteryModelNewQuery {
	bmnq.predicates = append(bmnq.predicates, ps...)
	return bmnq
}

// Limit the number of records to be returned by this query.
func (bmnq *BatteryModelNewQuery) Limit(limit int) *BatteryModelNewQuery {
	bmnq.ctx.Limit = &limit
	return bmnq
}

// Offset to start from.
func (bmnq *BatteryModelNewQuery) Offset(offset int) *BatteryModelNewQuery {
	bmnq.ctx.Offset = &offset
	return bmnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bmnq *BatteryModelNewQuery) Unique(unique bool) *BatteryModelNewQuery {
	bmnq.ctx.Unique = &unique
	return bmnq
}

// Order specifies how the records should be ordered.
func (bmnq *BatteryModelNewQuery) Order(o ...batterymodelnew.OrderOption) *BatteryModelNewQuery {
	bmnq.order = append(bmnq.order, o...)
	return bmnq
}

// First returns the first BatteryModelNew entity from the query.
// Returns a *NotFoundError when no BatteryModelNew was found.
func (bmnq *BatteryModelNewQuery) First(ctx context.Context) (*BatteryModelNew, error) {
	nodes, err := bmnq.Limit(1).All(setContextOp(ctx, bmnq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{batterymodelnew.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) FirstX(ctx context.Context) *BatteryModelNew {
	node, err := bmnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BatteryModelNew ID from the query.
// Returns a *NotFoundError when no BatteryModelNew ID was found.
func (bmnq *BatteryModelNewQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = bmnq.Limit(1).IDs(setContextOp(ctx, bmnq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{batterymodelnew.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := bmnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BatteryModelNew entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BatteryModelNew entity is found.
// Returns a *NotFoundError when no BatteryModelNew entities are found.
func (bmnq *BatteryModelNewQuery) Only(ctx context.Context) (*BatteryModelNew, error) {
	nodes, err := bmnq.Limit(2).All(setContextOp(ctx, bmnq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{batterymodelnew.Label}
	default:
		return nil, &NotSingularError{batterymodelnew.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) OnlyX(ctx context.Context) *BatteryModelNew {
	node, err := bmnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BatteryModelNew ID in the query.
// Returns a *NotSingularError when more than one BatteryModelNew ID is found.
// Returns a *NotFoundError when no entities are found.
func (bmnq *BatteryModelNewQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = bmnq.Limit(2).IDs(setContextOp(ctx, bmnq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{batterymodelnew.Label}
	default:
		err = &NotSingularError{batterymodelnew.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := bmnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BatteryModelNews.
func (bmnq *BatteryModelNewQuery) All(ctx context.Context) ([]*BatteryModelNew, error) {
	ctx = setContextOp(ctx, bmnq.ctx, "All")
	if err := bmnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BatteryModelNew, *BatteryModelNewQuery]()
	return withInterceptors[[]*BatteryModelNew](ctx, bmnq, qr, bmnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) AllX(ctx context.Context) []*BatteryModelNew {
	nodes, err := bmnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BatteryModelNew IDs.
func (bmnq *BatteryModelNewQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if bmnq.ctx.Unique == nil && bmnq.path != nil {
		bmnq.Unique(true)
	}
	ctx = setContextOp(ctx, bmnq.ctx, "IDs")
	if err = bmnq.Select(batterymodelnew.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := bmnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bmnq *BatteryModelNewQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bmnq.ctx, "Count")
	if err := bmnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bmnq, querierCount[*BatteryModelNewQuery](), bmnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) CountX(ctx context.Context) int {
	count, err := bmnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bmnq *BatteryModelNewQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bmnq.ctx, "Exist")
	switch _, err := bmnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bmnq *BatteryModelNewQuery) ExistX(ctx context.Context) bool {
	exist, err := bmnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BatteryModelNewQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bmnq *BatteryModelNewQuery) Clone() *BatteryModelNewQuery {
	if bmnq == nil {
		return nil
	}
	return &BatteryModelNewQuery{
		config:     bmnq.config,
		ctx:        bmnq.ctx.Clone(),
		order:      append([]batterymodelnew.OrderOption{}, bmnq.order...),
		inters:     append([]Interceptor{}, bmnq.inters...),
		predicates: append([]predicate.BatteryModelNew{}, bmnq.predicates...),
		// clone intermediate query.
		sql:  bmnq.sql.Clone(),
		path: bmnq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BatteryModelNew.Query().
//		GroupBy(batterymodelnew.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bmnq *BatteryModelNewQuery) GroupBy(field string, fields ...string) *BatteryModelNewGroupBy {
	bmnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BatteryModelNewGroupBy{build: bmnq}
	grbuild.flds = &bmnq.ctx.Fields
	grbuild.label = batterymodelnew.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.BatteryModelNew.Query().
//		Select(batterymodelnew.FieldCreatedAt).
//		Scan(ctx, &v)
func (bmnq *BatteryModelNewQuery) Select(fields ...string) *BatteryModelNewSelect {
	bmnq.ctx.Fields = append(bmnq.ctx.Fields, fields...)
	sbuild := &BatteryModelNewSelect{BatteryModelNewQuery: bmnq}
	sbuild.label = batterymodelnew.Label
	sbuild.flds, sbuild.scan = &bmnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BatteryModelNewSelect configured with the given aggregations.
func (bmnq *BatteryModelNewQuery) Aggregate(fns ...AggregateFunc) *BatteryModelNewSelect {
	return bmnq.Select().Aggregate(fns...)
}

func (bmnq *BatteryModelNewQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bmnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bmnq); err != nil {
				return err
			}
		}
	}
	for _, f := range bmnq.ctx.Fields {
		if !batterymodelnew.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bmnq.path != nil {
		prev, err := bmnq.path(ctx)
		if err != nil {
			return err
		}
		bmnq.sql = prev
	}
	return nil
}

func (bmnq *BatteryModelNewQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BatteryModelNew, error) {
	var (
		nodes = []*BatteryModelNew{}
		_spec = bmnq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BatteryModelNew).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BatteryModelNew{config: bmnq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(bmnq.modifiers) > 0 {
		_spec.Modifiers = bmnq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bmnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bmnq *BatteryModelNewQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bmnq.querySpec()
	if len(bmnq.modifiers) > 0 {
		_spec.Modifiers = bmnq.modifiers
	}
	_spec.Node.Columns = bmnq.ctx.Fields
	if len(bmnq.ctx.Fields) > 0 {
		_spec.Unique = bmnq.ctx.Unique != nil && *bmnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bmnq.driver, _spec)
}

func (bmnq *BatteryModelNewQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(batterymodelnew.Table, batterymodelnew.Columns, sqlgraph.NewFieldSpec(batterymodelnew.FieldID, field.TypeUint64))
	_spec.From = bmnq.sql
	if unique := bmnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bmnq.path != nil {
		_spec.Unique = true
	}
	if fields := bmnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, batterymodelnew.FieldID)
		for i := range fields {
			if fields[i] != batterymodelnew.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bmnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bmnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bmnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bmnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bmnq *BatteryModelNewQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bmnq.driver.Dialect())
	t1 := builder.Table(batterymodelnew.Table)
	columns := bmnq.ctx.Fields
	if len(columns) == 0 {
		columns = batterymodelnew.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bmnq.sql != nil {
		selector = bmnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bmnq.ctx.Unique != nil && *bmnq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range bmnq.modifiers {
		m(selector)
	}
	for _, p := range bmnq.predicates {
		p(selector)
	}
	for _, p := range bmnq.order {
		p(selector)
	}
	if offset := bmnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bmnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (bmnq *BatteryModelNewQuery) Modify(modifiers ...func(s *sql.Selector)) *BatteryModelNewSelect {
	bmnq.modifiers = append(bmnq.modifiers, modifiers...)
	return bmnq.Select()
}

type BatteryModelNewQueryWith string

var ()

func (bmnq *BatteryModelNewQuery) With(withEdges ...BatteryModelNewQueryWith) *BatteryModelNewQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return bmnq
}

// BatteryModelNewGroupBy is the group-by builder for BatteryModelNew entities.
type BatteryModelNewGroupBy struct {
	selector
	build *BatteryModelNewQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bmngb *BatteryModelNewGroupBy) Aggregate(fns ...AggregateFunc) *BatteryModelNewGroupBy {
	bmngb.fns = append(bmngb.fns, fns...)
	return bmngb
}

// Scan applies the selector query and scans the result into the given value.
func (bmngb *BatteryModelNewGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bmngb.build.ctx, "GroupBy")
	if err := bmngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BatteryModelNewQuery, *BatteryModelNewGroupBy](ctx, bmngb.build, bmngb, bmngb.build.inters, v)
}

func (bmngb *BatteryModelNewGroupBy) sqlScan(ctx context.Context, root *BatteryModelNewQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bmngb.fns))
	for _, fn := range bmngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bmngb.flds)+len(bmngb.fns))
		for _, f := range *bmngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bmngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bmngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BatteryModelNewSelect is the builder for selecting fields of BatteryModelNew entities.
type BatteryModelNewSelect struct {
	*BatteryModelNewQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bmns *BatteryModelNewSelect) Aggregate(fns ...AggregateFunc) *BatteryModelNewSelect {
	bmns.fns = append(bmns.fns, fns...)
	return bmns
}

// Scan applies the selector query and scans the result into the given value.
func (bmns *BatteryModelNewSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bmns.ctx, "Select")
	if err := bmns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BatteryModelNewQuery, *BatteryModelNewSelect](ctx, bmns.BatteryModelNewQuery, bmns, bmns.inters, v)
}

func (bmns *BatteryModelNewSelect) sqlScan(ctx context.Context, root *BatteryModelNewQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bmns.fns))
	for _, fn := range bmns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bmns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bmns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (bmns *BatteryModelNewSelect) Modify(modifiers ...func(s *sql.Selector)) *BatteryModelNewSelect {
	bmns.modifiers = append(bmns.modifiers, modifiers...)
	return bmns
}
