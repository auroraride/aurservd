// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/riderbelongs"
)

// RiderBelongsQuery is the builder for querying RiderBelongs entities.
type RiderBelongsQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.RiderBelongs
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RiderBelongsQuery builder.
func (rbq *RiderBelongsQuery) Where(ps ...predicate.RiderBelongs) *RiderBelongsQuery {
	rbq.predicates = append(rbq.predicates, ps...)
	return rbq
}

// Limit the number of records to be returned by this query.
func (rbq *RiderBelongsQuery) Limit(limit int) *RiderBelongsQuery {
	rbq.ctx.Limit = &limit
	return rbq
}

// Offset to start from.
func (rbq *RiderBelongsQuery) Offset(offset int) *RiderBelongsQuery {
	rbq.ctx.Offset = &offset
	return rbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rbq *RiderBelongsQuery) Unique(unique bool) *RiderBelongsQuery {
	rbq.ctx.Unique = &unique
	return rbq
}

// Order specifies how the records should be ordered.
func (rbq *RiderBelongsQuery) Order(o ...OrderFunc) *RiderBelongsQuery {
	rbq.order = append(rbq.order, o...)
	return rbq
}

// First returns the first RiderBelongs entity from the query.
// Returns a *NotFoundError when no RiderBelongs was found.
func (rbq *RiderBelongsQuery) First(ctx context.Context) (*RiderBelongs, error) {
	nodes, err := rbq.Limit(1).All(setContextOp(ctx, rbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{riderbelongs.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rbq *RiderBelongsQuery) FirstX(ctx context.Context) *RiderBelongs {
	node, err := rbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RiderBelongs ID from the query.
// Returns a *NotFoundError when no RiderBelongs ID was found.
func (rbq *RiderBelongsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rbq.Limit(1).IDs(setContextOp(ctx, rbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{riderbelongs.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rbq *RiderBelongsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := rbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RiderBelongs entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RiderBelongs entity is found.
// Returns a *NotFoundError when no RiderBelongs entities are found.
func (rbq *RiderBelongsQuery) Only(ctx context.Context) (*RiderBelongs, error) {
	nodes, err := rbq.Limit(2).All(setContextOp(ctx, rbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{riderbelongs.Label}
	default:
		return nil, &NotSingularError{riderbelongs.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rbq *RiderBelongsQuery) OnlyX(ctx context.Context) *RiderBelongs {
	node, err := rbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RiderBelongs ID in the query.
// Returns a *NotSingularError when more than one RiderBelongs ID is found.
// Returns a *NotFoundError when no entities are found.
func (rbq *RiderBelongsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rbq.Limit(2).IDs(setContextOp(ctx, rbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{riderbelongs.Label}
	default:
		err = &NotSingularError{riderbelongs.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rbq *RiderBelongsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := rbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RiderBelongsSlice.
func (rbq *RiderBelongsQuery) All(ctx context.Context) ([]*RiderBelongs, error) {
	ctx = setContextOp(ctx, rbq.ctx, "All")
	if err := rbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RiderBelongs, *RiderBelongsQuery]()
	return withInterceptors[[]*RiderBelongs](ctx, rbq, qr, rbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rbq *RiderBelongsQuery) AllX(ctx context.Context) []*RiderBelongs {
	nodes, err := rbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RiderBelongs IDs.
func (rbq *RiderBelongsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if rbq.ctx.Unique == nil && rbq.path != nil {
		rbq.Unique(true)
	}
	ctx = setContextOp(ctx, rbq.ctx, "IDs")
	if err = rbq.Select(riderbelongs.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rbq *RiderBelongsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := rbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rbq *RiderBelongsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rbq.ctx, "Count")
	if err := rbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rbq, querierCount[*RiderBelongsQuery](), rbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rbq *RiderBelongsQuery) CountX(ctx context.Context) int {
	count, err := rbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rbq *RiderBelongsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rbq.ctx, "Exist")
	switch _, err := rbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rbq *RiderBelongsQuery) ExistX(ctx context.Context) bool {
	exist, err := rbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RiderBelongsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rbq *RiderBelongsQuery) Clone() *RiderBelongsQuery {
	if rbq == nil {
		return nil
	}
	return &RiderBelongsQuery{
		config:     rbq.config,
		ctx:        rbq.ctx.Clone(),
		order:      append([]OrderFunc{}, rbq.order...),
		inters:     append([]Interceptor{}, rbq.inters...),
		predicates: append([]predicate.RiderBelongs{}, rbq.predicates...),
		// clone intermediate query.
		sql:  rbq.sql.Clone(),
		path: rbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RiderBelongs.Query().
//		GroupBy(riderbelongs.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rbq *RiderBelongsQuery) GroupBy(field string, fields ...string) *RiderBelongsGroupBy {
	rbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RiderBelongsGroupBy{build: rbq}
	grbuild.flds = &rbq.ctx.Fields
	grbuild.label = riderbelongs.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.RiderBelongs.Query().
//		Select(riderbelongs.FieldCreatedAt).
//		Scan(ctx, &v)
func (rbq *RiderBelongsQuery) Select(fields ...string) *RiderBelongsSelect {
	rbq.ctx.Fields = append(rbq.ctx.Fields, fields...)
	sbuild := &RiderBelongsSelect{RiderBelongsQuery: rbq}
	sbuild.label = riderbelongs.Label
	sbuild.flds, sbuild.scan = &rbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RiderBelongsSelect configured with the given aggregations.
func (rbq *RiderBelongsQuery) Aggregate(fns ...AggregateFunc) *RiderBelongsSelect {
	return rbq.Select().Aggregate(fns...)
}

func (rbq *RiderBelongsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rbq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rbq); err != nil {
				return err
			}
		}
	}
	for _, f := range rbq.ctx.Fields {
		if !riderbelongs.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rbq.path != nil {
		prev, err := rbq.path(ctx)
		if err != nil {
			return err
		}
		rbq.sql = prev
	}
	return nil
}

func (rbq *RiderBelongsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RiderBelongs, error) {
	var (
		nodes = []*RiderBelongs{}
		_spec = rbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RiderBelongs).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RiderBelongs{config: rbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(rbq.modifiers) > 0 {
		_spec.Modifiers = rbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rbq *RiderBelongsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rbq.querySpec()
	if len(rbq.modifiers) > 0 {
		_spec.Modifiers = rbq.modifiers
	}
	_spec.Node.Columns = rbq.ctx.Fields
	if len(rbq.ctx.Fields) > 0 {
		_spec.Unique = rbq.ctx.Unique != nil && *rbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rbq.driver, _spec)
}

func (rbq *RiderBelongsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(riderbelongs.Table, riderbelongs.Columns, sqlgraph.NewFieldSpec(riderbelongs.FieldID, field.TypeUint64))
	_spec.From = rbq.sql
	if unique := rbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rbq.path != nil {
		_spec.Unique = true
	}
	if fields := rbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, riderbelongs.FieldID)
		for i := range fields {
			if fields[i] != riderbelongs.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rbq *RiderBelongsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rbq.driver.Dialect())
	t1 := builder.Table(riderbelongs.Table)
	columns := rbq.ctx.Fields
	if len(columns) == 0 {
		columns = riderbelongs.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rbq.sql != nil {
		selector = rbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rbq.ctx.Unique != nil && *rbq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rbq.modifiers {
		m(selector)
	}
	for _, p := range rbq.predicates {
		p(selector)
	}
	for _, p := range rbq.order {
		p(selector)
	}
	if offset := rbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rbq *RiderBelongsQuery) Modify(modifiers ...func(s *sql.Selector)) *RiderBelongsSelect {
	rbq.modifiers = append(rbq.modifiers, modifiers...)
	return rbq.Select()
}

type RiderBelongsQueryWith string

var ()

func (rbq *RiderBelongsQuery) With(withEdges ...RiderBelongsQueryWith) *RiderBelongsQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return rbq
}

// RiderBelongsGroupBy is the group-by builder for RiderBelongs entities.
type RiderBelongsGroupBy struct {
	selector
	build *RiderBelongsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rbgb *RiderBelongsGroupBy) Aggregate(fns ...AggregateFunc) *RiderBelongsGroupBy {
	rbgb.fns = append(rbgb.fns, fns...)
	return rbgb
}

// Scan applies the selector query and scans the result into the given value.
func (rbgb *RiderBelongsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rbgb.build.ctx, "GroupBy")
	if err := rbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiderBelongsQuery, *RiderBelongsGroupBy](ctx, rbgb.build, rbgb, rbgb.build.inters, v)
}

func (rbgb *RiderBelongsGroupBy) sqlScan(ctx context.Context, root *RiderBelongsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rbgb.fns))
	for _, fn := range rbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rbgb.flds)+len(rbgb.fns))
		for _, f := range *rbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RiderBelongsSelect is the builder for selecting fields of RiderBelongs entities.
type RiderBelongsSelect struct {
	*RiderBelongsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rbs *RiderBelongsSelect) Aggregate(fns ...AggregateFunc) *RiderBelongsSelect {
	rbs.fns = append(rbs.fns, fns...)
	return rbs
}

// Scan applies the selector query and scans the result into the given value.
func (rbs *RiderBelongsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rbs.ctx, "Select")
	if err := rbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RiderBelongsQuery, *RiderBelongsSelect](ctx, rbs.RiderBelongsQuery, rbs, rbs.inters, v)
}

func (rbs *RiderBelongsSelect) sqlScan(ctx context.Context, root *RiderBelongsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rbs.fns))
	for _, fn := range rbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rbs *RiderBelongsSelect) Modify(modifiers ...func(s *sql.Selector)) *RiderBelongsSelect {
	rbs.modifiers = append(rbs.modifiers, modifiers...)
	return rbs
}
