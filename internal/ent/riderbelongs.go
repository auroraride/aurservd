// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/auroraride/aurservd/app/model"
	"github.com/auroraride/aurservd/internal/ent/riderbelongs"
)

// RiderBelongs is the model entity for the RiderBelongs schema.
type RiderBelongs struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// 创建人
	Creator *model.Modifier `json:"creator,omitempty"`
	// 最后修改人
	LastModifier *model.Modifier `json:"last_modifier,omitempty"`
	// 管理员改动原因/备注
	Remark string `json:"remark,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RiderBelongs) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case riderbelongs.FieldCreator, riderbelongs.FieldLastModifier:
			values[i] = new([]byte)
		case riderbelongs.FieldID:
			values[i] = new(sql.NullInt64)
		case riderbelongs.FieldRemark:
			values[i] = new(sql.NullString)
		case riderbelongs.FieldCreatedAt, riderbelongs.FieldUpdatedAt, riderbelongs.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type RiderBelongs", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RiderBelongs fields.
func (rb *RiderBelongs) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case riderbelongs.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rb.ID = uint64(value.Int64)
		case riderbelongs.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rb.CreatedAt = value.Time
			}
		case riderbelongs.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rb.UpdatedAt = value.Time
			}
		case riderbelongs.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				rb.DeletedAt = new(time.Time)
				*rb.DeletedAt = value.Time
			}
		case riderbelongs.FieldCreator:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field creator", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &rb.Creator); err != nil {
					return fmt.Errorf("unmarshal field creator: %w", err)
				}
			}
		case riderbelongs.FieldLastModifier:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field last_modifier", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &rb.LastModifier); err != nil {
					return fmt.Errorf("unmarshal field last_modifier: %w", err)
				}
			}
		case riderbelongs.FieldRemark:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field remark", values[i])
			} else if value.Valid {
				rb.Remark = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this RiderBelongs.
// Note that you need to call RiderBelongs.Unwrap() before calling this method if this RiderBelongs
// was returned from a transaction, and the transaction was committed or rolled back.
func (rb *RiderBelongs) Update() *RiderBelongsUpdateOne {
	return NewRiderBelongsClient(rb.config).UpdateOne(rb)
}

// Unwrap unwraps the RiderBelongs entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rb *RiderBelongs) Unwrap() *RiderBelongs {
	_tx, ok := rb.config.driver.(*txDriver)
	if !ok {
		panic("ent: RiderBelongs is not a transactional entity")
	}
	rb.config.driver = _tx.drv
	return rb
}

// String implements the fmt.Stringer.
func (rb *RiderBelongs) String() string {
	var builder strings.Builder
	builder.WriteString("RiderBelongs(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rb.ID))
	builder.WriteString("created_at=")
	builder.WriteString(rb.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rb.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := rb.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("creator=")
	builder.WriteString(fmt.Sprintf("%v", rb.Creator))
	builder.WriteString(", ")
	builder.WriteString("last_modifier=")
	builder.WriteString(fmt.Sprintf("%v", rb.LastModifier))
	builder.WriteString(", ")
	builder.WriteString("remark=")
	builder.WriteString(rb.Remark)
	builder.WriteByte(')')
	return builder.String()
}

// RiderBelongsSlice is a parsable slice of RiderBelongs.
type RiderBelongsSlice []*RiderBelongs
