// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/assetexport"
	"github.com/auroraride/aurservd/internal/ent/assetmanager"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// AssetExportQuery is the builder for querying AssetExport entities.
type AssetExportQuery struct {
	config
	ctx              *QueryContext
	order            []assetexport.OrderOption
	inters           []Interceptor
	predicates       []predicate.AssetExport
	withAssetManager *AssetManagerQuery
	modifiers        []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AssetExportQuery builder.
func (aeq *AssetExportQuery) Where(ps ...predicate.AssetExport) *AssetExportQuery {
	aeq.predicates = append(aeq.predicates, ps...)
	return aeq
}

// Limit the number of records to be returned by this query.
func (aeq *AssetExportQuery) Limit(limit int) *AssetExportQuery {
	aeq.ctx.Limit = &limit
	return aeq
}

// Offset to start from.
func (aeq *AssetExportQuery) Offset(offset int) *AssetExportQuery {
	aeq.ctx.Offset = &offset
	return aeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aeq *AssetExportQuery) Unique(unique bool) *AssetExportQuery {
	aeq.ctx.Unique = &unique
	return aeq
}

// Order specifies how the records should be ordered.
func (aeq *AssetExportQuery) Order(o ...assetexport.OrderOption) *AssetExportQuery {
	aeq.order = append(aeq.order, o...)
	return aeq
}

// QueryAssetManager chains the current query on the "asset_manager" edge.
func (aeq *AssetExportQuery) QueryAssetManager() *AssetManagerQuery {
	query := (&AssetManagerClient{config: aeq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aeq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aeq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assetexport.Table, assetexport.FieldID, selector),
			sqlgraph.To(assetmanager.Table, assetmanager.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, assetexport.AssetManagerTable, assetexport.AssetManagerColumn),
		)
		fromU = sqlgraph.SetNeighbors(aeq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AssetExport entity from the query.
// Returns a *NotFoundError when no AssetExport was found.
func (aeq *AssetExportQuery) First(ctx context.Context) (*AssetExport, error) {
	nodes, err := aeq.Limit(1).All(setContextOp(ctx, aeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{assetexport.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aeq *AssetExportQuery) FirstX(ctx context.Context) *AssetExport {
	node, err := aeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AssetExport ID from the query.
// Returns a *NotFoundError when no AssetExport ID was found.
func (aeq *AssetExportQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aeq.Limit(1).IDs(setContextOp(ctx, aeq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{assetexport.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aeq *AssetExportQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := aeq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AssetExport entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AssetExport entity is found.
// Returns a *NotFoundError when no AssetExport entities are found.
func (aeq *AssetExportQuery) Only(ctx context.Context) (*AssetExport, error) {
	nodes, err := aeq.Limit(2).All(setContextOp(ctx, aeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{assetexport.Label}
	default:
		return nil, &NotSingularError{assetexport.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aeq *AssetExportQuery) OnlyX(ctx context.Context) *AssetExport {
	node, err := aeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AssetExport ID in the query.
// Returns a *NotSingularError when more than one AssetExport ID is found.
// Returns a *NotFoundError when no entities are found.
func (aeq *AssetExportQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = aeq.Limit(2).IDs(setContextOp(ctx, aeq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{assetexport.Label}
	default:
		err = &NotSingularError{assetexport.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aeq *AssetExportQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := aeq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AssetExports.
func (aeq *AssetExportQuery) All(ctx context.Context) ([]*AssetExport, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryAll)
	if err := aeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AssetExport, *AssetExportQuery]()
	return withInterceptors[[]*AssetExport](ctx, aeq, qr, aeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aeq *AssetExportQuery) AllX(ctx context.Context) []*AssetExport {
	nodes, err := aeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AssetExport IDs.
func (aeq *AssetExportQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if aeq.ctx.Unique == nil && aeq.path != nil {
		aeq.Unique(true)
	}
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryIDs)
	if err = aeq.Select(assetexport.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aeq *AssetExportQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := aeq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aeq *AssetExportQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryCount)
	if err := aeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aeq, querierCount[*AssetExportQuery](), aeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aeq *AssetExportQuery) CountX(ctx context.Context) int {
	count, err := aeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aeq *AssetExportQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aeq.ctx, ent.OpQueryExist)
	switch _, err := aeq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aeq *AssetExportQuery) ExistX(ctx context.Context) bool {
	exist, err := aeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AssetExportQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aeq *AssetExportQuery) Clone() *AssetExportQuery {
	if aeq == nil {
		return nil
	}
	return &AssetExportQuery{
		config:           aeq.config,
		ctx:              aeq.ctx.Clone(),
		order:            append([]assetexport.OrderOption{}, aeq.order...),
		inters:           append([]Interceptor{}, aeq.inters...),
		predicates:       append([]predicate.AssetExport{}, aeq.predicates...),
		withAssetManager: aeq.withAssetManager.Clone(),
		// clone intermediate query.
		sql:       aeq.sql.Clone(),
		path:      aeq.path,
		modifiers: append([]func(*sql.Selector){}, aeq.modifiers...),
	}
}

// WithAssetManager tells the query-builder to eager-load the nodes that are connected to
// the "asset_manager" edge. The optional arguments are used to configure the query builder of the edge.
func (aeq *AssetExportQuery) WithAssetManager(opts ...func(*AssetManagerQuery)) *AssetExportQuery {
	query := (&AssetManagerClient{config: aeq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aeq.withAssetManager = query
	return aeq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AssetExport.Query().
//		GroupBy(assetexport.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (aeq *AssetExportQuery) GroupBy(field string, fields ...string) *AssetExportGroupBy {
	aeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AssetExportGroupBy{build: aeq}
	grbuild.flds = &aeq.ctx.Fields
	grbuild.label = assetexport.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AssetExport.Query().
//		Select(assetexport.FieldCreatedAt).
//		Scan(ctx, &v)
func (aeq *AssetExportQuery) Select(fields ...string) *AssetExportSelect {
	aeq.ctx.Fields = append(aeq.ctx.Fields, fields...)
	sbuild := &AssetExportSelect{AssetExportQuery: aeq}
	sbuild.label = assetexport.Label
	sbuild.flds, sbuild.scan = &aeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AssetExportSelect configured with the given aggregations.
func (aeq *AssetExportQuery) Aggregate(fns ...AggregateFunc) *AssetExportSelect {
	return aeq.Select().Aggregate(fns...)
}

func (aeq *AssetExportQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aeq); err != nil {
				return err
			}
		}
	}
	for _, f := range aeq.ctx.Fields {
		if !assetexport.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aeq.path != nil {
		prev, err := aeq.path(ctx)
		if err != nil {
			return err
		}
		aeq.sql = prev
	}
	return nil
}

func (aeq *AssetExportQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AssetExport, error) {
	var (
		nodes       = []*AssetExport{}
		_spec       = aeq.querySpec()
		loadedTypes = [1]bool{
			aeq.withAssetManager != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AssetExport).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AssetExport{config: aeq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(aeq.modifiers) > 0 {
		_spec.Modifiers = aeq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aeq.withAssetManager; query != nil {
		if err := aeq.loadAssetManager(ctx, query, nodes, nil,
			func(n *AssetExport, e *AssetManager) { n.Edges.AssetManager = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aeq *AssetExportQuery) loadAssetManager(ctx context.Context, query *AssetManagerQuery, nodes []*AssetExport, init func(*AssetExport), assign func(*AssetExport, *AssetManager)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*AssetExport)
	for i := range nodes {
		fk := nodes[i].AssetManagerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(assetmanager.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "asset_manager_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (aeq *AssetExportQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aeq.querySpec()
	if len(aeq.modifiers) > 0 {
		_spec.Modifiers = aeq.modifiers
	}
	_spec.Node.Columns = aeq.ctx.Fields
	if len(aeq.ctx.Fields) > 0 {
		_spec.Unique = aeq.ctx.Unique != nil && *aeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aeq.driver, _spec)
}

func (aeq *AssetExportQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(assetexport.Table, assetexport.Columns, sqlgraph.NewFieldSpec(assetexport.FieldID, field.TypeUint64))
	_spec.From = aeq.sql
	if unique := aeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aeq.path != nil {
		_spec.Unique = true
	}
	if fields := aeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, assetexport.FieldID)
		for i := range fields {
			if fields[i] != assetexport.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if aeq.withAssetManager != nil {
			_spec.Node.AddColumnOnce(assetexport.FieldAssetManagerID)
		}
	}
	if ps := aeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aeq *AssetExportQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aeq.driver.Dialect())
	t1 := builder.Table(assetexport.Table)
	columns := aeq.ctx.Fields
	if len(columns) == 0 {
		columns = assetexport.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aeq.sql != nil {
		selector = aeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aeq.ctx.Unique != nil && *aeq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range aeq.modifiers {
		m(selector)
	}
	for _, p := range aeq.predicates {
		p(selector)
	}
	for _, p := range aeq.order {
		p(selector)
	}
	if offset := aeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aeq *AssetExportQuery) Modify(modifiers ...func(s *sql.Selector)) *AssetExportSelect {
	aeq.modifiers = append(aeq.modifiers, modifiers...)
	return aeq.Select()
}

type AssetExportQueryWith string

var (
	AssetExportQueryWithAssetManager AssetExportQueryWith = "AssetManager"
)

func (aeq *AssetExportQuery) With(withEdges ...AssetExportQueryWith) *AssetExportQuery {
	for _, v := range withEdges {
		switch v {
		case AssetExportQueryWithAssetManager:
			aeq.WithAssetManager()
		}
	}
	return aeq
}

// AssetExportGroupBy is the group-by builder for AssetExport entities.
type AssetExportGroupBy struct {
	selector
	build *AssetExportQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aegb *AssetExportGroupBy) Aggregate(fns ...AggregateFunc) *AssetExportGroupBy {
	aegb.fns = append(aegb.fns, fns...)
	return aegb
}

// Scan applies the selector query and scans the result into the given value.
func (aegb *AssetExportGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aegb.build.ctx, ent.OpQueryGroupBy)
	if err := aegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetExportQuery, *AssetExportGroupBy](ctx, aegb.build, aegb, aegb.build.inters, v)
}

func (aegb *AssetExportGroupBy) sqlScan(ctx context.Context, root *AssetExportQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(aegb.fns))
	for _, fn := range aegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*aegb.flds)+len(aegb.fns))
		for _, f := range *aegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*aegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AssetExportSelect is the builder for selecting fields of AssetExport entities.
type AssetExportSelect struct {
	*AssetExportQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aes *AssetExportSelect) Aggregate(fns ...AggregateFunc) *AssetExportSelect {
	aes.fns = append(aes.fns, fns...)
	return aes
}

// Scan applies the selector query and scans the result into the given value.
func (aes *AssetExportSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aes.ctx, ent.OpQuerySelect)
	if err := aes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetExportQuery, *AssetExportSelect](ctx, aes.AssetExportQuery, aes, aes.inters, v)
}

func (aes *AssetExportSelect) sqlScan(ctx context.Context, root *AssetExportQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aes.fns))
	for _, fn := range aes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aes *AssetExportSelect) Modify(modifiers ...func(s *sql.Selector)) *AssetExportSelect {
	aes.modifiers = append(aes.modifiers, modifiers...)
	return aes
}
