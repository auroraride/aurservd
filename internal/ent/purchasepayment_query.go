// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/goods"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/purchaseorder"
	"github.com/auroraride/aurservd/internal/ent/purchasepayment"
	"github.com/auroraride/aurservd/internal/ent/rider"
)

// PurchasePaymentQuery is the builder for querying PurchasePayment entities.
type PurchasePaymentQuery struct {
	config
	ctx        *QueryContext
	order      []purchasepayment.OrderOption
	inters     []Interceptor
	predicates []predicate.PurchasePayment
	withRider  *RiderQuery
	withGoods  *GoodsQuery
	withOrder  *PurchaseOrderQuery
	withFKs    bool
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PurchasePaymentQuery builder.
func (ppq *PurchasePaymentQuery) Where(ps ...predicate.PurchasePayment) *PurchasePaymentQuery {
	ppq.predicates = append(ppq.predicates, ps...)
	return ppq
}

// Limit the number of records to be returned by this query.
func (ppq *PurchasePaymentQuery) Limit(limit int) *PurchasePaymentQuery {
	ppq.ctx.Limit = &limit
	return ppq
}

// Offset to start from.
func (ppq *PurchasePaymentQuery) Offset(offset int) *PurchasePaymentQuery {
	ppq.ctx.Offset = &offset
	return ppq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppq *PurchasePaymentQuery) Unique(unique bool) *PurchasePaymentQuery {
	ppq.ctx.Unique = &unique
	return ppq
}

// Order specifies how the records should be ordered.
func (ppq *PurchasePaymentQuery) Order(o ...purchasepayment.OrderOption) *PurchasePaymentQuery {
	ppq.order = append(ppq.order, o...)
	return ppq
}

// QueryRider chains the current query on the "rider" edge.
func (ppq *PurchasePaymentQuery) QueryRider() *RiderQuery {
	query := (&RiderClient{config: ppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(purchasepayment.Table, purchasepayment.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, purchasepayment.RiderTable, purchasepayment.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGoods chains the current query on the "goods" edge.
func (ppq *PurchasePaymentQuery) QueryGoods() *GoodsQuery {
	query := (&GoodsClient{config: ppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(purchasepayment.Table, purchasepayment.FieldID, selector),
			sqlgraph.To(goods.Table, goods.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, purchasepayment.GoodsTable, purchasepayment.GoodsColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (ppq *PurchasePaymentQuery) QueryOrder() *PurchaseOrderQuery {
	query := (&PurchaseOrderClient{config: ppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(purchasepayment.Table, purchasepayment.FieldID, selector),
			sqlgraph.To(purchaseorder.Table, purchaseorder.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, purchasepayment.OrderTable, purchasepayment.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PurchasePayment entity from the query.
// Returns a *NotFoundError when no PurchasePayment was found.
func (ppq *PurchasePaymentQuery) First(ctx context.Context) (*PurchasePayment, error) {
	nodes, err := ppq.Limit(1).All(setContextOp(ctx, ppq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{purchasepayment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) FirstX(ctx context.Context) *PurchasePayment {
	node, err := ppq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PurchasePayment ID from the query.
// Returns a *NotFoundError when no PurchasePayment ID was found.
func (ppq *PurchasePaymentQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ppq.Limit(1).IDs(setContextOp(ctx, ppq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{purchasepayment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := ppq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PurchasePayment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PurchasePayment entity is found.
// Returns a *NotFoundError when no PurchasePayment entities are found.
func (ppq *PurchasePaymentQuery) Only(ctx context.Context) (*PurchasePayment, error) {
	nodes, err := ppq.Limit(2).All(setContextOp(ctx, ppq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{purchasepayment.Label}
	default:
		return nil, &NotSingularError{purchasepayment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) OnlyX(ctx context.Context) *PurchasePayment {
	node, err := ppq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PurchasePayment ID in the query.
// Returns a *NotSingularError when more than one PurchasePayment ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppq *PurchasePaymentQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ppq.Limit(2).IDs(setContextOp(ctx, ppq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{purchasepayment.Label}
	default:
		err = &NotSingularError{purchasepayment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := ppq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PurchasePayments.
func (ppq *PurchasePaymentQuery) All(ctx context.Context) ([]*PurchasePayment, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryAll)
	if err := ppq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PurchasePayment, *PurchasePaymentQuery]()
	return withInterceptors[[]*PurchasePayment](ctx, ppq, qr, ppq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) AllX(ctx context.Context) []*PurchasePayment {
	nodes, err := ppq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PurchasePayment IDs.
func (ppq *PurchasePaymentQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if ppq.ctx.Unique == nil && ppq.path != nil {
		ppq.Unique(true)
	}
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryIDs)
	if err = ppq.Select(purchasepayment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := ppq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppq *PurchasePaymentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryCount)
	if err := ppq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppq, querierCount[*PurchasePaymentQuery](), ppq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) CountX(ctx context.Context) int {
	count, err := ppq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppq *PurchasePaymentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppq.ctx, ent.OpQueryExist)
	switch _, err := ppq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppq *PurchasePaymentQuery) ExistX(ctx context.Context) bool {
	exist, err := ppq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PurchasePaymentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppq *PurchasePaymentQuery) Clone() *PurchasePaymentQuery {
	if ppq == nil {
		return nil
	}
	return &PurchasePaymentQuery{
		config:     ppq.config,
		ctx:        ppq.ctx.Clone(),
		order:      append([]purchasepayment.OrderOption{}, ppq.order...),
		inters:     append([]Interceptor{}, ppq.inters...),
		predicates: append([]predicate.PurchasePayment{}, ppq.predicates...),
		withRider:  ppq.withRider.Clone(),
		withGoods:  ppq.withGoods.Clone(),
		withOrder:  ppq.withOrder.Clone(),
		// clone intermediate query.
		sql:       ppq.sql.Clone(),
		path:      ppq.path,
		modifiers: append([]func(*sql.Selector){}, ppq.modifiers...),
	}
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (ppq *PurchasePaymentQuery) WithRider(opts ...func(*RiderQuery)) *PurchasePaymentQuery {
	query := (&RiderClient{config: ppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppq.withRider = query
	return ppq
}

// WithGoods tells the query-builder to eager-load the nodes that are connected to
// the "goods" edge. The optional arguments are used to configure the query builder of the edge.
func (ppq *PurchasePaymentQuery) WithGoods(opts ...func(*GoodsQuery)) *PurchasePaymentQuery {
	query := (&GoodsClient{config: ppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppq.withGoods = query
	return ppq
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (ppq *PurchasePaymentQuery) WithOrder(opts ...func(*PurchaseOrderQuery)) *PurchasePaymentQuery {
	query := (&PurchaseOrderClient{config: ppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppq.withOrder = query
	return ppq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PurchasePayment.Query().
//		GroupBy(purchasepayment.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppq *PurchasePaymentQuery) GroupBy(field string, fields ...string) *PurchasePaymentGroupBy {
	ppq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PurchasePaymentGroupBy{build: ppq}
	grbuild.flds = &ppq.ctx.Fields
	grbuild.label = purchasepayment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PurchasePayment.Query().
//		Select(purchasepayment.FieldCreatedAt).
//		Scan(ctx, &v)
func (ppq *PurchasePaymentQuery) Select(fields ...string) *PurchasePaymentSelect {
	ppq.ctx.Fields = append(ppq.ctx.Fields, fields...)
	sbuild := &PurchasePaymentSelect{PurchasePaymentQuery: ppq}
	sbuild.label = purchasepayment.Label
	sbuild.flds, sbuild.scan = &ppq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PurchasePaymentSelect configured with the given aggregations.
func (ppq *PurchasePaymentQuery) Aggregate(fns ...AggregateFunc) *PurchasePaymentSelect {
	return ppq.Select().Aggregate(fns...)
}

func (ppq *PurchasePaymentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppq.ctx.Fields {
		if !purchasepayment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppq.path != nil {
		prev, err := ppq.path(ctx)
		if err != nil {
			return err
		}
		ppq.sql = prev
	}
	return nil
}

func (ppq *PurchasePaymentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PurchasePayment, error) {
	var (
		nodes       = []*PurchasePayment{}
		withFKs     = ppq.withFKs
		_spec       = ppq.querySpec()
		loadedTypes = [3]bool{
			ppq.withRider != nil,
			ppq.withGoods != nil,
			ppq.withOrder != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, purchasepayment.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PurchasePayment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PurchasePayment{config: ppq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ppq.modifiers) > 0 {
		_spec.Modifiers = ppq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ppq.withRider; query != nil {
		if err := ppq.loadRider(ctx, query, nodes, nil,
			func(n *PurchasePayment, e *Rider) { n.Edges.Rider = e }); err != nil {
			return nil, err
		}
	}
	if query := ppq.withGoods; query != nil {
		if err := ppq.loadGoods(ctx, query, nodes, nil,
			func(n *PurchasePayment, e *Goods) { n.Edges.Goods = e }); err != nil {
			return nil, err
		}
	}
	if query := ppq.withOrder; query != nil {
		if err := ppq.loadOrder(ctx, query, nodes, nil,
			func(n *PurchasePayment, e *PurchaseOrder) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ppq *PurchasePaymentQuery) loadRider(ctx context.Context, query *RiderQuery, nodes []*PurchasePayment, init func(*PurchasePayment), assign func(*PurchasePayment, *Rider)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PurchasePayment)
	for i := range nodes {
		fk := nodes[i].RiderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rider.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ppq *PurchasePaymentQuery) loadGoods(ctx context.Context, query *GoodsQuery, nodes []*PurchasePayment, init func(*PurchasePayment), assign func(*PurchasePayment, *Goods)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PurchasePayment)
	for i := range nodes {
		fk := nodes[i].GoodsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(goods.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "goods_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ppq *PurchasePaymentQuery) loadOrder(ctx context.Context, query *PurchaseOrderQuery, nodes []*PurchasePayment, init func(*PurchasePayment), assign func(*PurchasePayment, *PurchaseOrder)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PurchasePayment)
	for i := range nodes {
		fk := nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(purchaseorder.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ppq *PurchasePaymentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppq.querySpec()
	if len(ppq.modifiers) > 0 {
		_spec.Modifiers = ppq.modifiers
	}
	_spec.Node.Columns = ppq.ctx.Fields
	if len(ppq.ctx.Fields) > 0 {
		_spec.Unique = ppq.ctx.Unique != nil && *ppq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppq.driver, _spec)
}

func (ppq *PurchasePaymentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(purchasepayment.Table, purchasepayment.Columns, sqlgraph.NewFieldSpec(purchasepayment.FieldID, field.TypeUint64))
	_spec.From = ppq.sql
	if unique := ppq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppq.path != nil {
		_spec.Unique = true
	}
	if fields := ppq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, purchasepayment.FieldID)
		for i := range fields {
			if fields[i] != purchasepayment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ppq.withRider != nil {
			_spec.Node.AddColumnOnce(purchasepayment.FieldRiderID)
		}
		if ppq.withGoods != nil {
			_spec.Node.AddColumnOnce(purchasepayment.FieldGoodsID)
		}
		if ppq.withOrder != nil {
			_spec.Node.AddColumnOnce(purchasepayment.FieldOrderID)
		}
	}
	if ps := ppq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppq *PurchasePaymentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppq.driver.Dialect())
	t1 := builder.Table(purchasepayment.Table)
	columns := ppq.ctx.Fields
	if len(columns) == 0 {
		columns = purchasepayment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppq.sql != nil {
		selector = ppq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppq.ctx.Unique != nil && *ppq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ppq.modifiers {
		m(selector)
	}
	for _, p := range ppq.predicates {
		p(selector)
	}
	for _, p := range ppq.order {
		p(selector)
	}
	if offset := ppq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ppq *PurchasePaymentQuery) Modify(modifiers ...func(s *sql.Selector)) *PurchasePaymentSelect {
	ppq.modifiers = append(ppq.modifiers, modifiers...)
	return ppq.Select()
}

type PurchasePaymentQueryWith string

var (
	PurchasePaymentQueryWithRider PurchasePaymentQueryWith = "Rider"
	PurchasePaymentQueryWithGoods PurchasePaymentQueryWith = "Goods"
	PurchasePaymentQueryWithOrder PurchasePaymentQueryWith = "Order"
)

func (ppq *PurchasePaymentQuery) With(withEdges ...PurchasePaymentQueryWith) *PurchasePaymentQuery {
	for _, v := range withEdges {
		switch v {
		case PurchasePaymentQueryWithRider:
			ppq.WithRider()
		case PurchasePaymentQueryWithGoods:
			ppq.WithGoods()
		case PurchasePaymentQueryWithOrder:
			ppq.WithOrder()
		}
	}
	return ppq
}

// PurchasePaymentGroupBy is the group-by builder for PurchasePayment entities.
type PurchasePaymentGroupBy struct {
	selector
	build *PurchasePaymentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppgb *PurchasePaymentGroupBy) Aggregate(fns ...AggregateFunc) *PurchasePaymentGroupBy {
	ppgb.fns = append(ppgb.fns, fns...)
	return ppgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppgb *PurchasePaymentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppgb.build.ctx, ent.OpQueryGroupBy)
	if err := ppgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PurchasePaymentQuery, *PurchasePaymentGroupBy](ctx, ppgb.build, ppgb, ppgb.build.inters, v)
}

func (ppgb *PurchasePaymentGroupBy) sqlScan(ctx context.Context, root *PurchasePaymentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppgb.fns))
	for _, fn := range ppgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppgb.flds)+len(ppgb.fns))
		for _, f := range *ppgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PurchasePaymentSelect is the builder for selecting fields of PurchasePayment entities.
type PurchasePaymentSelect struct {
	*PurchasePaymentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pps *PurchasePaymentSelect) Aggregate(fns ...AggregateFunc) *PurchasePaymentSelect {
	pps.fns = append(pps.fns, fns...)
	return pps
}

// Scan applies the selector query and scans the result into the given value.
func (pps *PurchasePaymentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pps.ctx, ent.OpQuerySelect)
	if err := pps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PurchasePaymentQuery, *PurchasePaymentSelect](ctx, pps.PurchasePaymentQuery, pps, pps.inters, v)
}

func (pps *PurchasePaymentSelect) sqlScan(ctx context.Context, root *PurchasePaymentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pps.fns))
	for _, fn := range pps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pps *PurchasePaymentSelect) Modify(modifiers ...func(s *sql.Selector)) *PurchasePaymentSelect {
	pps.modifiers = append(pps.modifiers, modifiers...)
	return pps
}
