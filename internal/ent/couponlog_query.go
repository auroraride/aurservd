// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/couponlog"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// CouponLogQuery is the builder for querying CouponLog entities.
type CouponLogQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.CouponLog
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CouponLogQuery builder.
func (clq *CouponLogQuery) Where(ps ...predicate.CouponLog) *CouponLogQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit adds a limit step to the query.
func (clq *CouponLogQuery) Limit(limit int) *CouponLogQuery {
	clq.limit = &limit
	return clq
}

// Offset adds an offset step to the query.
func (clq *CouponLogQuery) Offset(offset int) *CouponLogQuery {
	clq.offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CouponLogQuery) Unique(unique bool) *CouponLogQuery {
	clq.unique = &unique
	return clq
}

// Order adds an order step to the query.
func (clq *CouponLogQuery) Order(o ...OrderFunc) *CouponLogQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// First returns the first CouponLog entity from the query.
// Returns a *NotFoundError when no CouponLog was found.
func (clq *CouponLogQuery) First(ctx context.Context) (*CouponLog, error) {
	nodes, err := clq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{couponlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CouponLogQuery) FirstX(ctx context.Context) *CouponLog {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CouponLog ID from the query.
// Returns a *NotFoundError when no CouponLog ID was found.
func (clq *CouponLogQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = clq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{couponlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CouponLogQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CouponLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CouponLog entity is found.
// Returns a *NotFoundError when no CouponLog entities are found.
func (clq *CouponLogQuery) Only(ctx context.Context) (*CouponLog, error) {
	nodes, err := clq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{couponlog.Label}
	default:
		return nil, &NotSingularError{couponlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CouponLogQuery) OnlyX(ctx context.Context) *CouponLog {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CouponLog ID in the query.
// Returns a *NotSingularError when more than one CouponLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CouponLogQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = clq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{couponlog.Label}
	default:
		err = &NotSingularError{couponlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CouponLogQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CouponLogs.
func (clq *CouponLogQuery) All(ctx context.Context) ([]*CouponLog, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return clq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (clq *CouponLogQuery) AllX(ctx context.Context) []*CouponLog {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CouponLog IDs.
func (clq *CouponLogQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	if err := clq.Select(couponlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CouponLogQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CouponLogQuery) Count(ctx context.Context) (int, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return clq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CouponLogQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CouponLogQuery) Exist(ctx context.Context) (bool, error) {
	if err := clq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return clq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CouponLogQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CouponLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CouponLogQuery) Clone() *CouponLogQuery {
	if clq == nil {
		return nil
	}
	return &CouponLogQuery{
		config:     clq.config,
		limit:      clq.limit,
		offset:     clq.offset,
		order:      append([]OrderFunc{}, clq.order...),
		predicates: append([]predicate.CouponLog{}, clq.predicates...),
		// clone intermediate query.
		sql:    clq.sql.Clone(),
		path:   clq.path,
		unique: clq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CouponLog.Query().
//		GroupBy(couponlog.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CouponLogQuery) GroupBy(field string, fields ...string) *CouponLogGroupBy {
	grbuild := &CouponLogGroupBy{config: clq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := clq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return clq.sqlQuery(ctx), nil
	}
	grbuild.label = couponlog.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CouponLog.Query().
//		Select(couponlog.FieldCreatedAt).
//		Scan(ctx, &v)
func (clq *CouponLogQuery) Select(fields ...string) *CouponLogSelect {
	clq.fields = append(clq.fields, fields...)
	selbuild := &CouponLogSelect{CouponLogQuery: clq}
	selbuild.label = couponlog.Label
	selbuild.flds, selbuild.scan = &clq.fields, selbuild.Scan
	return selbuild
}

func (clq *CouponLogQuery) prepareQuery(ctx context.Context) error {
	for _, f := range clq.fields {
		if !couponlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CouponLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CouponLog, error) {
	var (
		nodes = []*CouponLog{}
		_spec = clq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CouponLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CouponLog{config: clq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (clq *CouponLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	_spec.Node.Columns = clq.fields
	if len(clq.fields) > 0 {
		_spec.Unique = clq.unique != nil && *clq.unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CouponLogQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (clq *CouponLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   couponlog.Table,
			Columns: couponlog.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: couponlog.FieldID,
			},
		},
		From:   clq.sql,
		Unique: true,
	}
	if unique := clq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := clq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, couponlog.FieldID)
		for i := range fields {
			if fields[i] != couponlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CouponLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(couponlog.Table)
	columns := clq.fields
	if len(columns) == 0 {
		columns = couponlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.unique != nil && *clq.unique {
		selector.Distinct()
	}
	for _, m := range clq.modifiers {
		m(selector)
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (clq *CouponLogQuery) Modify(modifiers ...func(s *sql.Selector)) *CouponLogSelect {
	clq.modifiers = append(clq.modifiers, modifiers...)
	return clq.Select()
}

// CouponLogGroupBy is the group-by builder for CouponLog entities.
type CouponLogGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CouponLogGroupBy) Aggregate(fns ...AggregateFunc) *CouponLogGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the group-by query and scans the result into the given value.
func (clgb *CouponLogGroupBy) Scan(ctx context.Context, v any) error {
	query, err := clgb.path(ctx)
	if err != nil {
		return err
	}
	clgb.sql = query
	return clgb.sqlScan(ctx, v)
}

func (clgb *CouponLogGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range clgb.fields {
		if !couponlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := clgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (clgb *CouponLogGroupBy) sqlQuery() *sql.Selector {
	selector := clgb.sql.Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(clgb.fields)+len(clgb.fns))
		for _, f := range clgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(clgb.fields...)...)
}

// CouponLogSelect is the builder for selecting fields of CouponLog entities.
type CouponLogSelect struct {
	*CouponLogQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CouponLogSelect) Scan(ctx context.Context, v any) error {
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	cls.sql = cls.CouponLogQuery.sqlQuery(ctx)
	return cls.sqlScan(ctx, v)
}

func (cls *CouponLogSelect) sqlScan(ctx context.Context, v any) error {
	rows := &sql.Rows{}
	query, args := cls.sql.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cls *CouponLogSelect) Modify(modifiers ...func(s *sql.Selector)) *CouponLogSelect {
	cls.modifiers = append(cls.modifiers, modifiers...)
	return cls
}
