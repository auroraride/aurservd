// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/ebikebrand"
	"github.com/auroraride/aurservd/internal/ent/ebikebrandattribute"
	"github.com/auroraride/aurservd/internal/ent/plan"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// EbikeBrandQuery is the builder for querying EbikeBrand entities.
type EbikeBrandQuery struct {
	config
	ctx                *QueryContext
	order              []ebikebrand.OrderOption
	inters             []Interceptor
	predicates         []predicate.EbikeBrand
	withBrandAttribute *EbikeBrandAttributeQuery
	withPlans          *PlanQuery
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EbikeBrandQuery builder.
func (ebq *EbikeBrandQuery) Where(ps ...predicate.EbikeBrand) *EbikeBrandQuery {
	ebq.predicates = append(ebq.predicates, ps...)
	return ebq
}

// Limit the number of records to be returned by this query.
func (ebq *EbikeBrandQuery) Limit(limit int) *EbikeBrandQuery {
	ebq.ctx.Limit = &limit
	return ebq
}

// Offset to start from.
func (ebq *EbikeBrandQuery) Offset(offset int) *EbikeBrandQuery {
	ebq.ctx.Offset = &offset
	return ebq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ebq *EbikeBrandQuery) Unique(unique bool) *EbikeBrandQuery {
	ebq.ctx.Unique = &unique
	return ebq
}

// Order specifies how the records should be ordered.
func (ebq *EbikeBrandQuery) Order(o ...ebikebrand.OrderOption) *EbikeBrandQuery {
	ebq.order = append(ebq.order, o...)
	return ebq
}

// QueryBrandAttribute chains the current query on the "brand_attribute" edge.
func (ebq *EbikeBrandQuery) QueryBrandAttribute() *EbikeBrandAttributeQuery {
	query := (&EbikeBrandAttributeClient{config: ebq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ebq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ebq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ebikebrand.Table, ebikebrand.FieldID, selector),
			sqlgraph.To(ebikebrandattribute.Table, ebikebrandattribute.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, ebikebrand.BrandAttributeTable, ebikebrand.BrandAttributeColumn),
		)
		fromU = sqlgraph.SetNeighbors(ebq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPlans chains the current query on the "plans" edge.
func (ebq *EbikeBrandQuery) QueryPlans() *PlanQuery {
	query := (&PlanClient{config: ebq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ebq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ebq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ebikebrand.Table, ebikebrand.FieldID, selector),
			sqlgraph.To(plan.Table, plan.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, ebikebrand.PlansTable, ebikebrand.PlansColumn),
		)
		fromU = sqlgraph.SetNeighbors(ebq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EbikeBrand entity from the query.
// Returns a *NotFoundError when no EbikeBrand was found.
func (ebq *EbikeBrandQuery) First(ctx context.Context) (*EbikeBrand, error) {
	nodes, err := ebq.Limit(1).All(setContextOp(ctx, ebq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ebikebrand.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ebq *EbikeBrandQuery) FirstX(ctx context.Context) *EbikeBrand {
	node, err := ebq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EbikeBrand ID from the query.
// Returns a *NotFoundError when no EbikeBrand ID was found.
func (ebq *EbikeBrandQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ebq.Limit(1).IDs(setContextOp(ctx, ebq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ebikebrand.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ebq *EbikeBrandQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := ebq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EbikeBrand entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EbikeBrand entity is found.
// Returns a *NotFoundError when no EbikeBrand entities are found.
func (ebq *EbikeBrandQuery) Only(ctx context.Context) (*EbikeBrand, error) {
	nodes, err := ebq.Limit(2).All(setContextOp(ctx, ebq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ebikebrand.Label}
	default:
		return nil, &NotSingularError{ebikebrand.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ebq *EbikeBrandQuery) OnlyX(ctx context.Context) *EbikeBrand {
	node, err := ebq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EbikeBrand ID in the query.
// Returns a *NotSingularError when more than one EbikeBrand ID is found.
// Returns a *NotFoundError when no entities are found.
func (ebq *EbikeBrandQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = ebq.Limit(2).IDs(setContextOp(ctx, ebq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ebikebrand.Label}
	default:
		err = &NotSingularError{ebikebrand.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ebq *EbikeBrandQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := ebq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EbikeBrands.
func (ebq *EbikeBrandQuery) All(ctx context.Context) ([]*EbikeBrand, error) {
	ctx = setContextOp(ctx, ebq.ctx, ent.OpQueryAll)
	if err := ebq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EbikeBrand, *EbikeBrandQuery]()
	return withInterceptors[[]*EbikeBrand](ctx, ebq, qr, ebq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ebq *EbikeBrandQuery) AllX(ctx context.Context) []*EbikeBrand {
	nodes, err := ebq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EbikeBrand IDs.
func (ebq *EbikeBrandQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if ebq.ctx.Unique == nil && ebq.path != nil {
		ebq.Unique(true)
	}
	ctx = setContextOp(ctx, ebq.ctx, ent.OpQueryIDs)
	if err = ebq.Select(ebikebrand.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ebq *EbikeBrandQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := ebq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ebq *EbikeBrandQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ebq.ctx, ent.OpQueryCount)
	if err := ebq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ebq, querierCount[*EbikeBrandQuery](), ebq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ebq *EbikeBrandQuery) CountX(ctx context.Context) int {
	count, err := ebq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ebq *EbikeBrandQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ebq.ctx, ent.OpQueryExist)
	switch _, err := ebq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ebq *EbikeBrandQuery) ExistX(ctx context.Context) bool {
	exist, err := ebq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EbikeBrandQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ebq *EbikeBrandQuery) Clone() *EbikeBrandQuery {
	if ebq == nil {
		return nil
	}
	return &EbikeBrandQuery{
		config:             ebq.config,
		ctx:                ebq.ctx.Clone(),
		order:              append([]ebikebrand.OrderOption{}, ebq.order...),
		inters:             append([]Interceptor{}, ebq.inters...),
		predicates:         append([]predicate.EbikeBrand{}, ebq.predicates...),
		withBrandAttribute: ebq.withBrandAttribute.Clone(),
		withPlans:          ebq.withPlans.Clone(),
		// clone intermediate query.
		sql:       ebq.sql.Clone(),
		path:      ebq.path,
		modifiers: append([]func(*sql.Selector){}, ebq.modifiers...),
	}
}

// WithBrandAttribute tells the query-builder to eager-load the nodes that are connected to
// the "brand_attribute" edge. The optional arguments are used to configure the query builder of the edge.
func (ebq *EbikeBrandQuery) WithBrandAttribute(opts ...func(*EbikeBrandAttributeQuery)) *EbikeBrandQuery {
	query := (&EbikeBrandAttributeClient{config: ebq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ebq.withBrandAttribute = query
	return ebq
}

// WithPlans tells the query-builder to eager-load the nodes that are connected to
// the "plans" edge. The optional arguments are used to configure the query builder of the edge.
func (ebq *EbikeBrandQuery) WithPlans(opts ...func(*PlanQuery)) *EbikeBrandQuery {
	query := (&PlanClient{config: ebq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ebq.withPlans = query
	return ebq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EbikeBrand.Query().
//		GroupBy(ebikebrand.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ebq *EbikeBrandQuery) GroupBy(field string, fields ...string) *EbikeBrandGroupBy {
	ebq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EbikeBrandGroupBy{build: ebq}
	grbuild.flds = &ebq.ctx.Fields
	grbuild.label = ebikebrand.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EbikeBrand.Query().
//		Select(ebikebrand.FieldCreatedAt).
//		Scan(ctx, &v)
func (ebq *EbikeBrandQuery) Select(fields ...string) *EbikeBrandSelect {
	ebq.ctx.Fields = append(ebq.ctx.Fields, fields...)
	sbuild := &EbikeBrandSelect{EbikeBrandQuery: ebq}
	sbuild.label = ebikebrand.Label
	sbuild.flds, sbuild.scan = &ebq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EbikeBrandSelect configured with the given aggregations.
func (ebq *EbikeBrandQuery) Aggregate(fns ...AggregateFunc) *EbikeBrandSelect {
	return ebq.Select().Aggregate(fns...)
}

func (ebq *EbikeBrandQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ebq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ebq); err != nil {
				return err
			}
		}
	}
	for _, f := range ebq.ctx.Fields {
		if !ebikebrand.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ebq.path != nil {
		prev, err := ebq.path(ctx)
		if err != nil {
			return err
		}
		ebq.sql = prev
	}
	return nil
}

func (ebq *EbikeBrandQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EbikeBrand, error) {
	var (
		nodes       = []*EbikeBrand{}
		_spec       = ebq.querySpec()
		loadedTypes = [2]bool{
			ebq.withBrandAttribute != nil,
			ebq.withPlans != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EbikeBrand).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EbikeBrand{config: ebq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ebq.modifiers) > 0 {
		_spec.Modifiers = ebq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ebq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ebq.withBrandAttribute; query != nil {
		if err := ebq.loadBrandAttribute(ctx, query, nodes,
			func(n *EbikeBrand) { n.Edges.BrandAttribute = []*EbikeBrandAttribute{} },
			func(n *EbikeBrand, e *EbikeBrandAttribute) {
				n.Edges.BrandAttribute = append(n.Edges.BrandAttribute, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := ebq.withPlans; query != nil {
		if err := ebq.loadPlans(ctx, query, nodes,
			func(n *EbikeBrand) { n.Edges.Plans = []*Plan{} },
			func(n *EbikeBrand, e *Plan) { n.Edges.Plans = append(n.Edges.Plans, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ebq *EbikeBrandQuery) loadBrandAttribute(ctx context.Context, query *EbikeBrandAttributeQuery, nodes []*EbikeBrand, init func(*EbikeBrand), assign func(*EbikeBrand, *EbikeBrandAttribute)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*EbikeBrand)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(ebikebrandattribute.FieldBrandID)
	}
	query.Where(predicate.EbikeBrandAttribute(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(ebikebrand.BrandAttributeColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BrandID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "brand_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (ebq *EbikeBrandQuery) loadPlans(ctx context.Context, query *PlanQuery, nodes []*EbikeBrand, init func(*EbikeBrand), assign func(*EbikeBrand, *Plan)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uint64]*EbikeBrand)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(plan.FieldBrandID)
	}
	query.Where(predicate.Plan(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(ebikebrand.PlansColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BrandID
		if fk == nil {
			return fmt.Errorf(`foreign-key "brand_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "brand_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (ebq *EbikeBrandQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ebq.querySpec()
	if len(ebq.modifiers) > 0 {
		_spec.Modifiers = ebq.modifiers
	}
	_spec.Node.Columns = ebq.ctx.Fields
	if len(ebq.ctx.Fields) > 0 {
		_spec.Unique = ebq.ctx.Unique != nil && *ebq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ebq.driver, _spec)
}

func (ebq *EbikeBrandQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ebikebrand.Table, ebikebrand.Columns, sqlgraph.NewFieldSpec(ebikebrand.FieldID, field.TypeUint64))
	_spec.From = ebq.sql
	if unique := ebq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ebq.path != nil {
		_spec.Unique = true
	}
	if fields := ebq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ebikebrand.FieldID)
		for i := range fields {
			if fields[i] != ebikebrand.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ebq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ebq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ebq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ebq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ebq *EbikeBrandQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ebq.driver.Dialect())
	t1 := builder.Table(ebikebrand.Table)
	columns := ebq.ctx.Fields
	if len(columns) == 0 {
		columns = ebikebrand.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ebq.sql != nil {
		selector = ebq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ebq.ctx.Unique != nil && *ebq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ebq.modifiers {
		m(selector)
	}
	for _, p := range ebq.predicates {
		p(selector)
	}
	for _, p := range ebq.order {
		p(selector)
	}
	if offset := ebq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ebq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ebq *EbikeBrandQuery) Modify(modifiers ...func(s *sql.Selector)) *EbikeBrandSelect {
	ebq.modifiers = append(ebq.modifiers, modifiers...)
	return ebq.Select()
}

type EbikeBrandQueryWith string

var (
	EbikeBrandQueryWithBrandAttribute EbikeBrandQueryWith = "BrandAttribute"
	EbikeBrandQueryWithPlans          EbikeBrandQueryWith = "Plans"
)

func (ebq *EbikeBrandQuery) With(withEdges ...EbikeBrandQueryWith) *EbikeBrandQuery {
	for _, v := range withEdges {
		switch v {
		case EbikeBrandQueryWithBrandAttribute:
			ebq.WithBrandAttribute()
		case EbikeBrandQueryWithPlans:
			ebq.WithPlans()
		}
	}
	return ebq
}

// EbikeBrandGroupBy is the group-by builder for EbikeBrand entities.
type EbikeBrandGroupBy struct {
	selector
	build *EbikeBrandQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ebgb *EbikeBrandGroupBy) Aggregate(fns ...AggregateFunc) *EbikeBrandGroupBy {
	ebgb.fns = append(ebgb.fns, fns...)
	return ebgb
}

// Scan applies the selector query and scans the result into the given value.
func (ebgb *EbikeBrandGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ebgb.build.ctx, ent.OpQueryGroupBy)
	if err := ebgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EbikeBrandQuery, *EbikeBrandGroupBy](ctx, ebgb.build, ebgb, ebgb.build.inters, v)
}

func (ebgb *EbikeBrandGroupBy) sqlScan(ctx context.Context, root *EbikeBrandQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ebgb.fns))
	for _, fn := range ebgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ebgb.flds)+len(ebgb.fns))
		for _, f := range *ebgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ebgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ebgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EbikeBrandSelect is the builder for selecting fields of EbikeBrand entities.
type EbikeBrandSelect struct {
	*EbikeBrandQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ebs *EbikeBrandSelect) Aggregate(fns ...AggregateFunc) *EbikeBrandSelect {
	ebs.fns = append(ebs.fns, fns...)
	return ebs
}

// Scan applies the selector query and scans the result into the given value.
func (ebs *EbikeBrandSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ebs.ctx, ent.OpQuerySelect)
	if err := ebs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EbikeBrandQuery, *EbikeBrandSelect](ctx, ebs.EbikeBrandQuery, ebs, ebs.inters, v)
}

func (ebs *EbikeBrandSelect) sqlScan(ctx context.Context, root *EbikeBrandQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ebs.fns))
	for _, fn := range ebs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ebs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ebs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ebs *EbikeBrandSelect) Modify(modifiers ...func(s *sql.Selector)) *EbikeBrandSelect {
	ebs.modifiers = append(ebs.modifiers, modifiers...)
	return ebs
}
