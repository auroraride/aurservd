// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/enterprise"
	"github.com/auroraride/aurservd/internal/ent/enterpriseinvoice"
	"github.com/auroraride/aurservd/internal/ent/enterprisestatement"
	"github.com/auroraride/aurservd/internal/ent/enterprisestation"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/rider"
)

// EnterpriseInvoiceQuery is the builder for querying EnterpriseInvoice entities.
type EnterpriseInvoiceQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.EnterpriseInvoice
	// eager-loading edges.
	withStation    *EnterpriseStationQuery
	withEnterprise *EnterpriseQuery
	withRider      *RiderQuery
	withStatement  *EnterpriseStatementQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EnterpriseInvoiceQuery builder.
func (eiq *EnterpriseInvoiceQuery) Where(ps ...predicate.EnterpriseInvoice) *EnterpriseInvoiceQuery {
	eiq.predicates = append(eiq.predicates, ps...)
	return eiq
}

// Limit adds a limit step to the query.
func (eiq *EnterpriseInvoiceQuery) Limit(limit int) *EnterpriseInvoiceQuery {
	eiq.limit = &limit
	return eiq
}

// Offset adds an offset step to the query.
func (eiq *EnterpriseInvoiceQuery) Offset(offset int) *EnterpriseInvoiceQuery {
	eiq.offset = &offset
	return eiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eiq *EnterpriseInvoiceQuery) Unique(unique bool) *EnterpriseInvoiceQuery {
	eiq.unique = &unique
	return eiq
}

// Order adds an order step to the query.
func (eiq *EnterpriseInvoiceQuery) Order(o ...OrderFunc) *EnterpriseInvoiceQuery {
	eiq.order = append(eiq.order, o...)
	return eiq
}

// QueryStation chains the current query on the "station" edge.
func (eiq *EnterpriseInvoiceQuery) QueryStation() *EnterpriseStationQuery {
	query := &EnterpriseStationQuery{config: eiq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enterpriseinvoice.Table, enterpriseinvoice.FieldID, selector),
			sqlgraph.To(enterprisestation.Table, enterprisestation.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, enterpriseinvoice.StationTable, enterpriseinvoice.StationColumn),
		)
		fromU = sqlgraph.SetNeighbors(eiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEnterprise chains the current query on the "enterprise" edge.
func (eiq *EnterpriseInvoiceQuery) QueryEnterprise() *EnterpriseQuery {
	query := &EnterpriseQuery{config: eiq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enterpriseinvoice.Table, enterpriseinvoice.FieldID, selector),
			sqlgraph.To(enterprise.Table, enterprise.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, enterpriseinvoice.EnterpriseTable, enterpriseinvoice.EnterpriseColumn),
		)
		fromU = sqlgraph.SetNeighbors(eiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRider chains the current query on the "rider" edge.
func (eiq *EnterpriseInvoiceQuery) QueryRider() *RiderQuery {
	query := &RiderQuery{config: eiq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enterpriseinvoice.Table, enterpriseinvoice.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, enterpriseinvoice.RiderTable, enterpriseinvoice.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(eiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStatement chains the current query on the "statement" edge.
func (eiq *EnterpriseInvoiceQuery) QueryStatement() *EnterpriseStatementQuery {
	query := &EnterpriseStatementQuery{config: eiq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enterpriseinvoice.Table, enterpriseinvoice.FieldID, selector),
			sqlgraph.To(enterprisestatement.Table, enterprisestatement.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, enterpriseinvoice.StatementTable, enterpriseinvoice.StatementColumn),
		)
		fromU = sqlgraph.SetNeighbors(eiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EnterpriseInvoice entity from the query.
// Returns a *NotFoundError when no EnterpriseInvoice was found.
func (eiq *EnterpriseInvoiceQuery) First(ctx context.Context) (*EnterpriseInvoice, error) {
	nodes, err := eiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{enterpriseinvoice.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) FirstX(ctx context.Context) *EnterpriseInvoice {
	node, err := eiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EnterpriseInvoice ID from the query.
// Returns a *NotFoundError when no EnterpriseInvoice ID was found.
func (eiq *EnterpriseInvoiceQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{enterpriseinvoice.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := eiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EnterpriseInvoice entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EnterpriseInvoice entity is found.
// Returns a *NotFoundError when no EnterpriseInvoice entities are found.
func (eiq *EnterpriseInvoiceQuery) Only(ctx context.Context) (*EnterpriseInvoice, error) {
	nodes, err := eiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{enterpriseinvoice.Label}
	default:
		return nil, &NotSingularError{enterpriseinvoice.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) OnlyX(ctx context.Context) *EnterpriseInvoice {
	node, err := eiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EnterpriseInvoice ID in the query.
// Returns a *NotSingularError when more than one EnterpriseInvoice ID is found.
// Returns a *NotFoundError when no entities are found.
func (eiq *EnterpriseInvoiceQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = eiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{enterpriseinvoice.Label}
	default:
		err = &NotSingularError{enterpriseinvoice.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := eiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EnterpriseInvoices.
func (eiq *EnterpriseInvoiceQuery) All(ctx context.Context) ([]*EnterpriseInvoice, error) {
	if err := eiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return eiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) AllX(ctx context.Context) []*EnterpriseInvoice {
	nodes, err := eiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EnterpriseInvoice IDs.
func (eiq *EnterpriseInvoiceQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	if err := eiq.Select(enterpriseinvoice.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := eiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eiq *EnterpriseInvoiceQuery) Count(ctx context.Context) (int, error) {
	if err := eiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return eiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) CountX(ctx context.Context) int {
	count, err := eiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eiq *EnterpriseInvoiceQuery) Exist(ctx context.Context) (bool, error) {
	if err := eiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return eiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (eiq *EnterpriseInvoiceQuery) ExistX(ctx context.Context) bool {
	exist, err := eiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EnterpriseInvoiceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eiq *EnterpriseInvoiceQuery) Clone() *EnterpriseInvoiceQuery {
	if eiq == nil {
		return nil
	}
	return &EnterpriseInvoiceQuery{
		config:         eiq.config,
		limit:          eiq.limit,
		offset:         eiq.offset,
		order:          append([]OrderFunc{}, eiq.order...),
		predicates:     append([]predicate.EnterpriseInvoice{}, eiq.predicates...),
		withStation:    eiq.withStation.Clone(),
		withEnterprise: eiq.withEnterprise.Clone(),
		withRider:      eiq.withRider.Clone(),
		withStatement:  eiq.withStatement.Clone(),
		// clone intermediate query.
		sql:    eiq.sql.Clone(),
		path:   eiq.path,
		unique: eiq.unique,
	}
}

// WithStation tells the query-builder to eager-load the nodes that are connected to
// the "station" edge. The optional arguments are used to configure the query builder of the edge.
func (eiq *EnterpriseInvoiceQuery) WithStation(opts ...func(*EnterpriseStationQuery)) *EnterpriseInvoiceQuery {
	query := &EnterpriseStationQuery{config: eiq.config}
	for _, opt := range opts {
		opt(query)
	}
	eiq.withStation = query
	return eiq
}

// WithEnterprise tells the query-builder to eager-load the nodes that are connected to
// the "enterprise" edge. The optional arguments are used to configure the query builder of the edge.
func (eiq *EnterpriseInvoiceQuery) WithEnterprise(opts ...func(*EnterpriseQuery)) *EnterpriseInvoiceQuery {
	query := &EnterpriseQuery{config: eiq.config}
	for _, opt := range opts {
		opt(query)
	}
	eiq.withEnterprise = query
	return eiq
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (eiq *EnterpriseInvoiceQuery) WithRider(opts ...func(*RiderQuery)) *EnterpriseInvoiceQuery {
	query := &RiderQuery{config: eiq.config}
	for _, opt := range opts {
		opt(query)
	}
	eiq.withRider = query
	return eiq
}

// WithStatement tells the query-builder to eager-load the nodes that are connected to
// the "statement" edge. The optional arguments are used to configure the query builder of the edge.
func (eiq *EnterpriseInvoiceQuery) WithStatement(opts ...func(*EnterpriseStatementQuery)) *EnterpriseInvoiceQuery {
	query := &EnterpriseStatementQuery{config: eiq.config}
	for _, opt := range opts {
		opt(query)
	}
	eiq.withStatement = query
	return eiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EnterpriseInvoice.Query().
//		GroupBy(enterpriseinvoice.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (eiq *EnterpriseInvoiceQuery) GroupBy(field string, fields ...string) *EnterpriseInvoiceGroupBy {
	grbuild := &EnterpriseInvoiceGroupBy{config: eiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := eiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return eiq.sqlQuery(ctx), nil
	}
	grbuild.label = enterpriseinvoice.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.EnterpriseInvoice.Query().
//		Select(enterpriseinvoice.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (eiq *EnterpriseInvoiceQuery) Select(fields ...string) *EnterpriseInvoiceSelect {
	eiq.fields = append(eiq.fields, fields...)
	selbuild := &EnterpriseInvoiceSelect{EnterpriseInvoiceQuery: eiq}
	selbuild.label = enterpriseinvoice.Label
	selbuild.flds, selbuild.scan = &eiq.fields, selbuild.Scan
	return selbuild
}

func (eiq *EnterpriseInvoiceQuery) prepareQuery(ctx context.Context) error {
	for _, f := range eiq.fields {
		if !enterpriseinvoice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eiq.path != nil {
		prev, err := eiq.path(ctx)
		if err != nil {
			return err
		}
		eiq.sql = prev
	}
	return nil
}

func (eiq *EnterpriseInvoiceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EnterpriseInvoice, error) {
	var (
		nodes       = []*EnterpriseInvoice{}
		_spec       = eiq.querySpec()
		loadedTypes = [4]bool{
			eiq.withStation != nil,
			eiq.withEnterprise != nil,
			eiq.withRider != nil,
			eiq.withStatement != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*EnterpriseInvoice).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &EnterpriseInvoice{config: eiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(eiq.modifiers) > 0 {
		_spec.Modifiers = eiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := eiq.withStation; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*EnterpriseInvoice)
		for i := range nodes {
			fk := nodes[i].StationID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(enterprisestation.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "station_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Station = n
			}
		}
	}

	if query := eiq.withEnterprise; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*EnterpriseInvoice)
		for i := range nodes {
			fk := nodes[i].EnterpriseID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(enterprise.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "enterprise_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Enterprise = n
			}
		}
	}

	if query := eiq.withRider; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*EnterpriseInvoice)
		for i := range nodes {
			fk := nodes[i].RiderID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(rider.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Rider = n
			}
		}
	}

	if query := eiq.withStatement; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*EnterpriseInvoice)
		for i := range nodes {
			fk := nodes[i].StatementID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(enterprisestatement.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "statement_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Statement = n
			}
		}
	}

	return nodes, nil
}

func (eiq *EnterpriseInvoiceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eiq.querySpec()
	if len(eiq.modifiers) > 0 {
		_spec.Modifiers = eiq.modifiers
	}
	_spec.Node.Columns = eiq.fields
	if len(eiq.fields) > 0 {
		_spec.Unique = eiq.unique != nil && *eiq.unique
	}
	return sqlgraph.CountNodes(ctx, eiq.driver, _spec)
}

func (eiq *EnterpriseInvoiceQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := eiq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (eiq *EnterpriseInvoiceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   enterpriseinvoice.Table,
			Columns: enterpriseinvoice.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: enterpriseinvoice.FieldID,
			},
		},
		From:   eiq.sql,
		Unique: true,
	}
	if unique := eiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := eiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, enterpriseinvoice.FieldID)
		for i := range fields {
			if fields[i] != enterpriseinvoice.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eiq *EnterpriseInvoiceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eiq.driver.Dialect())
	t1 := builder.Table(enterpriseinvoice.Table)
	columns := eiq.fields
	if len(columns) == 0 {
		columns = enterpriseinvoice.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eiq.sql != nil {
		selector = eiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eiq.unique != nil && *eiq.unique {
		selector.Distinct()
	}
	for _, m := range eiq.modifiers {
		m(selector)
	}
	for _, p := range eiq.predicates {
		p(selector)
	}
	for _, p := range eiq.order {
		p(selector)
	}
	if offset := eiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eiq *EnterpriseInvoiceQuery) Modify(modifiers ...func(s *sql.Selector)) *EnterpriseInvoiceSelect {
	eiq.modifiers = append(eiq.modifiers, modifiers...)
	return eiq.Select()
}

// EnterpriseInvoiceGroupBy is the group-by builder for EnterpriseInvoice entities.
type EnterpriseInvoiceGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eigb *EnterpriseInvoiceGroupBy) Aggregate(fns ...AggregateFunc) *EnterpriseInvoiceGroupBy {
	eigb.fns = append(eigb.fns, fns...)
	return eigb
}

// Scan applies the group-by query and scans the result into the given value.
func (eigb *EnterpriseInvoiceGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := eigb.path(ctx)
	if err != nil {
		return err
	}
	eigb.sql = query
	return eigb.sqlScan(ctx, v)
}

func (eigb *EnterpriseInvoiceGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range eigb.fields {
		if !enterpriseinvoice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := eigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (eigb *EnterpriseInvoiceGroupBy) sqlQuery() *sql.Selector {
	selector := eigb.sql.Select()
	aggregation := make([]string, 0, len(eigb.fns))
	for _, fn := range eigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(eigb.fields)+len(eigb.fns))
		for _, f := range eigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(eigb.fields...)...)
}

// EnterpriseInvoiceSelect is the builder for selecting fields of EnterpriseInvoice entities.
type EnterpriseInvoiceSelect struct {
	*EnterpriseInvoiceQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (eis *EnterpriseInvoiceSelect) Scan(ctx context.Context, v interface{}) error {
	if err := eis.prepareQuery(ctx); err != nil {
		return err
	}
	eis.sql = eis.EnterpriseInvoiceQuery.sqlQuery(ctx)
	return eis.sqlScan(ctx, v)
}

func (eis *EnterpriseInvoiceSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := eis.sql.Query()
	if err := eis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (eis *EnterpriseInvoiceSelect) Modify(modifiers ...func(s *sql.Selector)) *EnterpriseInvoiceSelect {
	eis.modifiers = append(eis.modifiers, modifiers...)
	return eis
}
