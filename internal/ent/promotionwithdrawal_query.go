// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/promotionbankcard"
	"github.com/auroraride/aurservd/internal/ent/promotionmember"
	"github.com/auroraride/aurservd/internal/ent/promotionwithdrawal"
)

// PromotionWithdrawalQuery is the builder for querying PromotionWithdrawal entities.
type PromotionWithdrawalQuery struct {
	config
	ctx        *QueryContext
	order      []promotionwithdrawal.OrderOption
	inters     []Interceptor
	predicates []predicate.PromotionWithdrawal
	withMember *PromotionMemberQuery
	withCards  *PromotionBankCardQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PromotionWithdrawalQuery builder.
func (pwq *PromotionWithdrawalQuery) Where(ps ...predicate.PromotionWithdrawal) *PromotionWithdrawalQuery {
	pwq.predicates = append(pwq.predicates, ps...)
	return pwq
}

// Limit the number of records to be returned by this query.
func (pwq *PromotionWithdrawalQuery) Limit(limit int) *PromotionWithdrawalQuery {
	pwq.ctx.Limit = &limit
	return pwq
}

// Offset to start from.
func (pwq *PromotionWithdrawalQuery) Offset(offset int) *PromotionWithdrawalQuery {
	pwq.ctx.Offset = &offset
	return pwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pwq *PromotionWithdrawalQuery) Unique(unique bool) *PromotionWithdrawalQuery {
	pwq.ctx.Unique = &unique
	return pwq
}

// Order specifies how the records should be ordered.
func (pwq *PromotionWithdrawalQuery) Order(o ...promotionwithdrawal.OrderOption) *PromotionWithdrawalQuery {
	pwq.order = append(pwq.order, o...)
	return pwq
}

// QueryMember chains the current query on the "member" edge.
func (pwq *PromotionWithdrawalQuery) QueryMember() *PromotionMemberQuery {
	query := (&PromotionMemberClient{config: pwq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pwq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pwq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionwithdrawal.Table, promotionwithdrawal.FieldID, selector),
			sqlgraph.To(promotionmember.Table, promotionmember.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, promotionwithdrawal.MemberTable, promotionwithdrawal.MemberColumn),
		)
		fromU = sqlgraph.SetNeighbors(pwq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCards chains the current query on the "cards" edge.
func (pwq *PromotionWithdrawalQuery) QueryCards() *PromotionBankCardQuery {
	query := (&PromotionBankCardClient{config: pwq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pwq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pwq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(promotionwithdrawal.Table, promotionwithdrawal.FieldID, selector),
			sqlgraph.To(promotionbankcard.Table, promotionbankcard.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, promotionwithdrawal.CardsTable, promotionwithdrawal.CardsColumn),
		)
		fromU = sqlgraph.SetNeighbors(pwq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PromotionWithdrawal entity from the query.
// Returns a *NotFoundError when no PromotionWithdrawal was found.
func (pwq *PromotionWithdrawalQuery) First(ctx context.Context) (*PromotionWithdrawal, error) {
	nodes, err := pwq.Limit(1).All(setContextOp(ctx, pwq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{promotionwithdrawal.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) FirstX(ctx context.Context) *PromotionWithdrawal {
	node, err := pwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PromotionWithdrawal ID from the query.
// Returns a *NotFoundError when no PromotionWithdrawal ID was found.
func (pwq *PromotionWithdrawalQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = pwq.Limit(1).IDs(setContextOp(ctx, pwq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{promotionwithdrawal.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := pwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PromotionWithdrawal entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PromotionWithdrawal entity is found.
// Returns a *NotFoundError when no PromotionWithdrawal entities are found.
func (pwq *PromotionWithdrawalQuery) Only(ctx context.Context) (*PromotionWithdrawal, error) {
	nodes, err := pwq.Limit(2).All(setContextOp(ctx, pwq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{promotionwithdrawal.Label}
	default:
		return nil, &NotSingularError{promotionwithdrawal.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) OnlyX(ctx context.Context) *PromotionWithdrawal {
	node, err := pwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PromotionWithdrawal ID in the query.
// Returns a *NotSingularError when more than one PromotionWithdrawal ID is found.
// Returns a *NotFoundError when no entities are found.
func (pwq *PromotionWithdrawalQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = pwq.Limit(2).IDs(setContextOp(ctx, pwq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{promotionwithdrawal.Label}
	default:
		err = &NotSingularError{promotionwithdrawal.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := pwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PromotionWithdrawals.
func (pwq *PromotionWithdrawalQuery) All(ctx context.Context) ([]*PromotionWithdrawal, error) {
	ctx = setContextOp(ctx, pwq.ctx, "All")
	if err := pwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PromotionWithdrawal, *PromotionWithdrawalQuery]()
	return withInterceptors[[]*PromotionWithdrawal](ctx, pwq, qr, pwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) AllX(ctx context.Context) []*PromotionWithdrawal {
	nodes, err := pwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PromotionWithdrawal IDs.
func (pwq *PromotionWithdrawalQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if pwq.ctx.Unique == nil && pwq.path != nil {
		pwq.Unique(true)
	}
	ctx = setContextOp(ctx, pwq.ctx, "IDs")
	if err = pwq.Select(promotionwithdrawal.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := pwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pwq *PromotionWithdrawalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pwq.ctx, "Count")
	if err := pwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pwq, querierCount[*PromotionWithdrawalQuery](), pwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) CountX(ctx context.Context) int {
	count, err := pwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pwq *PromotionWithdrawalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pwq.ctx, "Exist")
	switch _, err := pwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pwq *PromotionWithdrawalQuery) ExistX(ctx context.Context) bool {
	exist, err := pwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PromotionWithdrawalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pwq *PromotionWithdrawalQuery) Clone() *PromotionWithdrawalQuery {
	if pwq == nil {
		return nil
	}
	return &PromotionWithdrawalQuery{
		config:     pwq.config,
		ctx:        pwq.ctx.Clone(),
		order:      append([]promotionwithdrawal.OrderOption{}, pwq.order...),
		inters:     append([]Interceptor{}, pwq.inters...),
		predicates: append([]predicate.PromotionWithdrawal{}, pwq.predicates...),
		withMember: pwq.withMember.Clone(),
		withCards:  pwq.withCards.Clone(),
		// clone intermediate query.
		sql:  pwq.sql.Clone(),
		path: pwq.path,
	}
}

// WithMember tells the query-builder to eager-load the nodes that are connected to
// the "member" edge. The optional arguments are used to configure the query builder of the edge.
func (pwq *PromotionWithdrawalQuery) WithMember(opts ...func(*PromotionMemberQuery)) *PromotionWithdrawalQuery {
	query := (&PromotionMemberClient{config: pwq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pwq.withMember = query
	return pwq
}

// WithCards tells the query-builder to eager-load the nodes that are connected to
// the "cards" edge. The optional arguments are used to configure the query builder of the edge.
func (pwq *PromotionWithdrawalQuery) WithCards(opts ...func(*PromotionBankCardQuery)) *PromotionWithdrawalQuery {
	query := (&PromotionBankCardClient{config: pwq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pwq.withCards = query
	return pwq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PromotionWithdrawal.Query().
//		GroupBy(promotionwithdrawal.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pwq *PromotionWithdrawalQuery) GroupBy(field string, fields ...string) *PromotionWithdrawalGroupBy {
	pwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PromotionWithdrawalGroupBy{build: pwq}
	grbuild.flds = &pwq.ctx.Fields
	grbuild.label = promotionwithdrawal.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PromotionWithdrawal.Query().
//		Select(promotionwithdrawal.FieldCreatedAt).
//		Scan(ctx, &v)
func (pwq *PromotionWithdrawalQuery) Select(fields ...string) *PromotionWithdrawalSelect {
	pwq.ctx.Fields = append(pwq.ctx.Fields, fields...)
	sbuild := &PromotionWithdrawalSelect{PromotionWithdrawalQuery: pwq}
	sbuild.label = promotionwithdrawal.Label
	sbuild.flds, sbuild.scan = &pwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PromotionWithdrawalSelect configured with the given aggregations.
func (pwq *PromotionWithdrawalQuery) Aggregate(fns ...AggregateFunc) *PromotionWithdrawalSelect {
	return pwq.Select().Aggregate(fns...)
}

func (pwq *PromotionWithdrawalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pwq); err != nil {
				return err
			}
		}
	}
	for _, f := range pwq.ctx.Fields {
		if !promotionwithdrawal.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pwq.path != nil {
		prev, err := pwq.path(ctx)
		if err != nil {
			return err
		}
		pwq.sql = prev
	}
	return nil
}

func (pwq *PromotionWithdrawalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PromotionWithdrawal, error) {
	var (
		nodes       = []*PromotionWithdrawal{}
		_spec       = pwq.querySpec()
		loadedTypes = [2]bool{
			pwq.withMember != nil,
			pwq.withCards != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PromotionWithdrawal).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PromotionWithdrawal{config: pwq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pwq.modifiers) > 0 {
		_spec.Modifiers = pwq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pwq.withMember; query != nil {
		if err := pwq.loadMember(ctx, query, nodes, nil,
			func(n *PromotionWithdrawal, e *PromotionMember) { n.Edges.Member = e }); err != nil {
			return nil, err
		}
	}
	if query := pwq.withCards; query != nil {
		if err := pwq.loadCards(ctx, query, nodes, nil,
			func(n *PromotionWithdrawal, e *PromotionBankCard) { n.Edges.Cards = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pwq *PromotionWithdrawalQuery) loadMember(ctx context.Context, query *PromotionMemberQuery, nodes []*PromotionWithdrawal, init func(*PromotionWithdrawal), assign func(*PromotionWithdrawal, *PromotionMember)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionWithdrawal)
	for i := range nodes {
		fk := nodes[i].MemberID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(promotionmember.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "member_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pwq *PromotionWithdrawalQuery) loadCards(ctx context.Context, query *PromotionBankCardQuery, nodes []*PromotionWithdrawal, init func(*PromotionWithdrawal), assign func(*PromotionWithdrawal, *PromotionBankCard)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PromotionWithdrawal)
	for i := range nodes {
		fk := nodes[i].AccountID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(promotionbankcard.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "account_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pwq *PromotionWithdrawalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pwq.querySpec()
	if len(pwq.modifiers) > 0 {
		_spec.Modifiers = pwq.modifiers
	}
	_spec.Node.Columns = pwq.ctx.Fields
	if len(pwq.ctx.Fields) > 0 {
		_spec.Unique = pwq.ctx.Unique != nil && *pwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pwq.driver, _spec)
}

func (pwq *PromotionWithdrawalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(promotionwithdrawal.Table, promotionwithdrawal.Columns, sqlgraph.NewFieldSpec(promotionwithdrawal.FieldID, field.TypeUint64))
	_spec.From = pwq.sql
	if unique := pwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pwq.path != nil {
		_spec.Unique = true
	}
	if fields := pwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, promotionwithdrawal.FieldID)
		for i := range fields {
			if fields[i] != promotionwithdrawal.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pwq.withMember != nil {
			_spec.Node.AddColumnOnce(promotionwithdrawal.FieldMemberID)
		}
		if pwq.withCards != nil {
			_spec.Node.AddColumnOnce(promotionwithdrawal.FieldAccountID)
		}
	}
	if ps := pwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pwq *PromotionWithdrawalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pwq.driver.Dialect())
	t1 := builder.Table(promotionwithdrawal.Table)
	columns := pwq.ctx.Fields
	if len(columns) == 0 {
		columns = promotionwithdrawal.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pwq.sql != nil {
		selector = pwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pwq.ctx.Unique != nil && *pwq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pwq.modifiers {
		m(selector)
	}
	for _, p := range pwq.predicates {
		p(selector)
	}
	for _, p := range pwq.order {
		p(selector)
	}
	if offset := pwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pwq *PromotionWithdrawalQuery) Modify(modifiers ...func(s *sql.Selector)) *PromotionWithdrawalSelect {
	pwq.modifiers = append(pwq.modifiers, modifiers...)
	return pwq.Select()
}

type PromotionWithdrawalQueryWith string

var (
	PromotionWithdrawalQueryWithMember PromotionWithdrawalQueryWith = "Member"
	PromotionWithdrawalQueryWithCards  PromotionWithdrawalQueryWith = "Cards"
)

func (pwq *PromotionWithdrawalQuery) With(withEdges ...PromotionWithdrawalQueryWith) *PromotionWithdrawalQuery {
	for _, v := range withEdges {
		switch v {
		case PromotionWithdrawalQueryWithMember:
			pwq.WithMember()
		case PromotionWithdrawalQueryWithCards:
			pwq.WithCards()
		}
	}
	return pwq
}

// PromotionWithdrawalGroupBy is the group-by builder for PromotionWithdrawal entities.
type PromotionWithdrawalGroupBy struct {
	selector
	build *PromotionWithdrawalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pwgb *PromotionWithdrawalGroupBy) Aggregate(fns ...AggregateFunc) *PromotionWithdrawalGroupBy {
	pwgb.fns = append(pwgb.fns, fns...)
	return pwgb
}

// Scan applies the selector query and scans the result into the given value.
func (pwgb *PromotionWithdrawalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pwgb.build.ctx, "GroupBy")
	if err := pwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PromotionWithdrawalQuery, *PromotionWithdrawalGroupBy](ctx, pwgb.build, pwgb, pwgb.build.inters, v)
}

func (pwgb *PromotionWithdrawalGroupBy) sqlScan(ctx context.Context, root *PromotionWithdrawalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pwgb.fns))
	for _, fn := range pwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pwgb.flds)+len(pwgb.fns))
		for _, f := range *pwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PromotionWithdrawalSelect is the builder for selecting fields of PromotionWithdrawal entities.
type PromotionWithdrawalSelect struct {
	*PromotionWithdrawalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pws *PromotionWithdrawalSelect) Aggregate(fns ...AggregateFunc) *PromotionWithdrawalSelect {
	pws.fns = append(pws.fns, fns...)
	return pws
}

// Scan applies the selector query and scans the result into the given value.
func (pws *PromotionWithdrawalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pws.ctx, "Select")
	if err := pws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PromotionWithdrawalQuery, *PromotionWithdrawalSelect](ctx, pws.PromotionWithdrawalQuery, pws, pws.inters, v)
}

func (pws *PromotionWithdrawalSelect) sqlScan(ctx context.Context, root *PromotionWithdrawalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pws.fns))
	for _, fn := range pws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pws *PromotionWithdrawalSelect) Modify(modifiers ...func(s *sql.Selector)) *PromotionWithdrawalSelect {
	pws.modifiers = append(pws.modifiers, modifiers...)
	return pws
}
