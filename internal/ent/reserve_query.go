// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/business"
	"github.com/auroraride/aurservd/internal/ent/cabinet"
	"github.com/auroraride/aurservd/internal/ent/city"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/reserve"
	"github.com/auroraride/aurservd/internal/ent/rider"
)

// ReserveQuery is the builder for querying Reserve entities.
type ReserveQuery struct {
	config
	ctx          *QueryContext
	order        []OrderFunc
	inters       []Interceptor
	predicates   []predicate.Reserve
	withCabinet  *CabinetQuery
	withRider    *RiderQuery
	withCity     *CityQuery
	withBusiness *BusinessQuery
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReserveQuery builder.
func (rq *ReserveQuery) Where(ps ...predicate.Reserve) *ReserveQuery {
	rq.predicates = append(rq.predicates, ps...)
	return rq
}

// Limit the number of records to be returned by this query.
func (rq *ReserveQuery) Limit(limit int) *ReserveQuery {
	rq.ctx.Limit = &limit
	return rq
}

// Offset to start from.
func (rq *ReserveQuery) Offset(offset int) *ReserveQuery {
	rq.ctx.Offset = &offset
	return rq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rq *ReserveQuery) Unique(unique bool) *ReserveQuery {
	rq.ctx.Unique = &unique
	return rq
}

// Order specifies how the records should be ordered.
func (rq *ReserveQuery) Order(o ...OrderFunc) *ReserveQuery {
	rq.order = append(rq.order, o...)
	return rq
}

// QueryCabinet chains the current query on the "cabinet" edge.
func (rq *ReserveQuery) QueryCabinet() *CabinetQuery {
	query := (&CabinetClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reserve.Table, reserve.FieldID, selector),
			sqlgraph.To(cabinet.Table, cabinet.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, reserve.CabinetTable, reserve.CabinetColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRider chains the current query on the "rider" edge.
func (rq *ReserveQuery) QueryRider() *RiderQuery {
	query := (&RiderClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reserve.Table, reserve.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, reserve.RiderTable, reserve.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCity chains the current query on the "city" edge.
func (rq *ReserveQuery) QueryCity() *CityQuery {
	query := (&CityClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reserve.Table, reserve.FieldID, selector),
			sqlgraph.To(city.Table, city.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, reserve.CityTable, reserve.CityColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBusiness chains the current query on the "business" edge.
func (rq *ReserveQuery) QueryBusiness() *BusinessQuery {
	query := (&BusinessClient{config: rq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(reserve.Table, reserve.FieldID, selector),
			sqlgraph.To(business.Table, business.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, reserve.BusinessTable, reserve.BusinessColumn),
		)
		fromU = sqlgraph.SetNeighbors(rq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Reserve entity from the query.
// Returns a *NotFoundError when no Reserve was found.
func (rq *ReserveQuery) First(ctx context.Context) (*Reserve, error) {
	nodes, err := rq.Limit(1).All(setContextOp(ctx, rq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reserve.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rq *ReserveQuery) FirstX(ctx context.Context) *Reserve {
	node, err := rq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Reserve ID from the query.
// Returns a *NotFoundError when no Reserve ID was found.
func (rq *ReserveQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rq.Limit(1).IDs(setContextOp(ctx, rq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reserve.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rq *ReserveQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := rq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Reserve entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Reserve entity is found.
// Returns a *NotFoundError when no Reserve entities are found.
func (rq *ReserveQuery) Only(ctx context.Context) (*Reserve, error) {
	nodes, err := rq.Limit(2).All(setContextOp(ctx, rq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reserve.Label}
	default:
		return nil, &NotSingularError{reserve.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rq *ReserveQuery) OnlyX(ctx context.Context) *Reserve {
	node, err := rq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Reserve ID in the query.
// Returns a *NotSingularError when more than one Reserve ID is found.
// Returns a *NotFoundError when no entities are found.
func (rq *ReserveQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = rq.Limit(2).IDs(setContextOp(ctx, rq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reserve.Label}
	default:
		err = &NotSingularError{reserve.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rq *ReserveQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := rq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Reserves.
func (rq *ReserveQuery) All(ctx context.Context) ([]*Reserve, error) {
	ctx = setContextOp(ctx, rq.ctx, "All")
	if err := rq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Reserve, *ReserveQuery]()
	return withInterceptors[[]*Reserve](ctx, rq, qr, rq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rq *ReserveQuery) AllX(ctx context.Context) []*Reserve {
	nodes, err := rq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Reserve IDs.
func (rq *ReserveQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	ctx = setContextOp(ctx, rq.ctx, "IDs")
	if err := rq.Select(reserve.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rq *ReserveQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := rq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rq *ReserveQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rq.ctx, "Count")
	if err := rq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rq, querierCount[*ReserveQuery](), rq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rq *ReserveQuery) CountX(ctx context.Context) int {
	count, err := rq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rq *ReserveQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rq.ctx, "Exist")
	switch _, err := rq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rq *ReserveQuery) ExistX(ctx context.Context) bool {
	exist, err := rq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReserveQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rq *ReserveQuery) Clone() *ReserveQuery {
	if rq == nil {
		return nil
	}
	return &ReserveQuery{
		config:       rq.config,
		ctx:          rq.ctx.Clone(),
		order:        append([]OrderFunc{}, rq.order...),
		inters:       append([]Interceptor{}, rq.inters...),
		predicates:   append([]predicate.Reserve{}, rq.predicates...),
		withCabinet:  rq.withCabinet.Clone(),
		withRider:    rq.withRider.Clone(),
		withCity:     rq.withCity.Clone(),
		withBusiness: rq.withBusiness.Clone(),
		// clone intermediate query.
		sql:  rq.sql.Clone(),
		path: rq.path,
	}
}

// WithCabinet tells the query-builder to eager-load the nodes that are connected to
// the "cabinet" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *ReserveQuery) WithCabinet(opts ...func(*CabinetQuery)) *ReserveQuery {
	query := (&CabinetClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withCabinet = query
	return rq
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *ReserveQuery) WithRider(opts ...func(*RiderQuery)) *ReserveQuery {
	query := (&RiderClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withRider = query
	return rq
}

// WithCity tells the query-builder to eager-load the nodes that are connected to
// the "city" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *ReserveQuery) WithCity(opts ...func(*CityQuery)) *ReserveQuery {
	query := (&CityClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withCity = query
	return rq
}

// WithBusiness tells the query-builder to eager-load the nodes that are connected to
// the "business" edge. The optional arguments are used to configure the query builder of the edge.
func (rq *ReserveQuery) WithBusiness(opts ...func(*BusinessQuery)) *ReserveQuery {
	query := (&BusinessClient{config: rq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rq.withBusiness = query
	return rq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Reserve.Query().
//		GroupBy(reserve.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rq *ReserveQuery) GroupBy(field string, fields ...string) *ReserveGroupBy {
	rq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReserveGroupBy{build: rq}
	grbuild.flds = &rq.ctx.Fields
	grbuild.label = reserve.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Reserve.Query().
//		Select(reserve.FieldCreatedAt).
//		Scan(ctx, &v)
func (rq *ReserveQuery) Select(fields ...string) *ReserveSelect {
	rq.ctx.Fields = append(rq.ctx.Fields, fields...)
	sbuild := &ReserveSelect{ReserveQuery: rq}
	sbuild.label = reserve.Label
	sbuild.flds, sbuild.scan = &rq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReserveSelect configured with the given aggregations.
func (rq *ReserveQuery) Aggregate(fns ...AggregateFunc) *ReserveSelect {
	return rq.Select().Aggregate(fns...)
}

func (rq *ReserveQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rq); err != nil {
				return err
			}
		}
	}
	for _, f := range rq.ctx.Fields {
		if !reserve.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rq.path != nil {
		prev, err := rq.path(ctx)
		if err != nil {
			return err
		}
		rq.sql = prev
	}
	return nil
}

func (rq *ReserveQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Reserve, error) {
	var (
		nodes       = []*Reserve{}
		_spec       = rq.querySpec()
		loadedTypes = [4]bool{
			rq.withCabinet != nil,
			rq.withRider != nil,
			rq.withCity != nil,
			rq.withBusiness != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Reserve).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Reserve{config: rq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rq.modifiers) > 0 {
		_spec.Modifiers = rq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rq.withCabinet; query != nil {
		if err := rq.loadCabinet(ctx, query, nodes, nil,
			func(n *Reserve, e *Cabinet) { n.Edges.Cabinet = e }); err != nil {
			return nil, err
		}
	}
	if query := rq.withRider; query != nil {
		if err := rq.loadRider(ctx, query, nodes, nil,
			func(n *Reserve, e *Rider) { n.Edges.Rider = e }); err != nil {
			return nil, err
		}
	}
	if query := rq.withCity; query != nil {
		if err := rq.loadCity(ctx, query, nodes, nil,
			func(n *Reserve, e *City) { n.Edges.City = e }); err != nil {
			return nil, err
		}
	}
	if query := rq.withBusiness; query != nil {
		if err := rq.loadBusiness(ctx, query, nodes, nil,
			func(n *Reserve, e *Business) { n.Edges.Business = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rq *ReserveQuery) loadCabinet(ctx context.Context, query *CabinetQuery, nodes []*Reserve, init func(*Reserve), assign func(*Reserve, *Cabinet)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*Reserve)
	for i := range nodes {
		fk := nodes[i].CabinetID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(cabinet.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "cabinet_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rq *ReserveQuery) loadRider(ctx context.Context, query *RiderQuery, nodes []*Reserve, init func(*Reserve), assign func(*Reserve, *Rider)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*Reserve)
	for i := range nodes {
		fk := nodes[i].RiderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(rider.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rq *ReserveQuery) loadCity(ctx context.Context, query *CityQuery, nodes []*Reserve, init func(*Reserve), assign func(*Reserve, *City)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*Reserve)
	for i := range nodes {
		fk := nodes[i].CityID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(city.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "city_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rq *ReserveQuery) loadBusiness(ctx context.Context, query *BusinessQuery, nodes []*Reserve, init func(*Reserve), assign func(*Reserve, *Business)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*Reserve)
	for i := range nodes {
		if nodes[i].BusinessID == nil {
			continue
		}
		fk := *nodes[i].BusinessID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(business.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "business_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rq *ReserveQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rq.querySpec()
	if len(rq.modifiers) > 0 {
		_spec.Modifiers = rq.modifiers
	}
	_spec.Node.Columns = rq.ctx.Fields
	if len(rq.ctx.Fields) > 0 {
		_spec.Unique = rq.ctx.Unique != nil && *rq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rq.driver, _spec)
}

func (rq *ReserveQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   reserve.Table,
			Columns: reserve.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: reserve.FieldID,
			},
		},
		From:   rq.sql,
		Unique: true,
	}
	if unique := rq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := rq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reserve.FieldID)
		for i := range fields {
			if fields[i] != reserve.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rq *ReserveQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rq.driver.Dialect())
	t1 := builder.Table(reserve.Table)
	columns := rq.ctx.Fields
	if len(columns) == 0 {
		columns = reserve.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rq.sql != nil {
		selector = rq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rq.ctx.Unique != nil && *rq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rq.modifiers {
		m(selector)
	}
	for _, p := range rq.predicates {
		p(selector)
	}
	for _, p := range rq.order {
		p(selector)
	}
	if offset := rq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rq *ReserveQuery) Modify(modifiers ...func(s *sql.Selector)) *ReserveSelect {
	rq.modifiers = append(rq.modifiers, modifiers...)
	return rq.Select()
}

type ReserveQueryWith string

var (
	ReserveQueryWithCabinet  ReserveQueryWith = "Cabinet"
	ReserveQueryWithRider    ReserveQueryWith = "Rider"
	ReserveQueryWithCity     ReserveQueryWith = "City"
	ReserveQueryWithBusiness ReserveQueryWith = "Business"
)

func (rq *ReserveQuery) With(withEdges ...ReserveQueryWith) *ReserveQuery {
	for _, v := range withEdges {
		switch v {
		case ReserveQueryWithCabinet:
			rq.WithCabinet()
		case ReserveQueryWithRider:
			rq.WithRider()
		case ReserveQueryWithCity:
			rq.WithCity()
		case ReserveQueryWithBusiness:
			rq.WithBusiness()
		}
	}
	return rq
}

// ReserveGroupBy is the group-by builder for Reserve entities.
type ReserveGroupBy struct {
	selector
	build *ReserveQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rgb *ReserveGroupBy) Aggregate(fns ...AggregateFunc) *ReserveGroupBy {
	rgb.fns = append(rgb.fns, fns...)
	return rgb
}

// Scan applies the selector query and scans the result into the given value.
func (rgb *ReserveGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rgb.build.ctx, "GroupBy")
	if err := rgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReserveQuery, *ReserveGroupBy](ctx, rgb.build, rgb, rgb.build.inters, v)
}

func (rgb *ReserveGroupBy) sqlScan(ctx context.Context, root *ReserveQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rgb.fns))
	for _, fn := range rgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rgb.flds)+len(rgb.fns))
		for _, f := range *rgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReserveSelect is the builder for selecting fields of Reserve entities.
type ReserveSelect struct {
	*ReserveQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rs *ReserveSelect) Aggregate(fns ...AggregateFunc) *ReserveSelect {
	rs.fns = append(rs.fns, fns...)
	return rs
}

// Scan applies the selector query and scans the result into the given value.
func (rs *ReserveSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rs.ctx, "Select")
	if err := rs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReserveQuery, *ReserveSelect](ctx, rs.ReserveQuery, rs, rs.inters, v)
}

func (rs *ReserveSelect) sqlScan(ctx context.Context, root *ReserveQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rs.fns))
	for _, fn := range rs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rs *ReserveSelect) Modify(modifiers ...func(s *sql.Selector)) *ReserveSelect {
	rs.modifiers = append(rs.modifiers, modifiers...)
	return rs
}
