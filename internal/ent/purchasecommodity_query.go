// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/purchasecommodity"
)

// PurchaseCommodityQuery is the builder for querying PurchaseCommodity entities.
type PurchaseCommodityQuery struct {
	config
	ctx        *QueryContext
	order      []purchasecommodity.OrderOption
	inters     []Interceptor
	predicates []predicate.PurchaseCommodity
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PurchaseCommodityQuery builder.
func (pcq *PurchaseCommodityQuery) Where(ps ...predicate.PurchaseCommodity) *PurchaseCommodityQuery {
	pcq.predicates = append(pcq.predicates, ps...)
	return pcq
}

// Limit the number of records to be returned by this query.
func (pcq *PurchaseCommodityQuery) Limit(limit int) *PurchaseCommodityQuery {
	pcq.ctx.Limit = &limit
	return pcq
}

// Offset to start from.
func (pcq *PurchaseCommodityQuery) Offset(offset int) *PurchaseCommodityQuery {
	pcq.ctx.Offset = &offset
	return pcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pcq *PurchaseCommodityQuery) Unique(unique bool) *PurchaseCommodityQuery {
	pcq.ctx.Unique = &unique
	return pcq
}

// Order specifies how the records should be ordered.
func (pcq *PurchaseCommodityQuery) Order(o ...purchasecommodity.OrderOption) *PurchaseCommodityQuery {
	pcq.order = append(pcq.order, o...)
	return pcq
}

// First returns the first PurchaseCommodity entity from the query.
// Returns a *NotFoundError when no PurchaseCommodity was found.
func (pcq *PurchaseCommodityQuery) First(ctx context.Context) (*PurchaseCommodity, error) {
	nodes, err := pcq.Limit(1).All(setContextOp(ctx, pcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{purchasecommodity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) FirstX(ctx context.Context) *PurchaseCommodity {
	node, err := pcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PurchaseCommodity ID from the query.
// Returns a *NotFoundError when no PurchaseCommodity ID was found.
func (pcq *PurchaseCommodityQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = pcq.Limit(1).IDs(setContextOp(ctx, pcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{purchasecommodity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := pcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PurchaseCommodity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PurchaseCommodity entity is found.
// Returns a *NotFoundError when no PurchaseCommodity entities are found.
func (pcq *PurchaseCommodityQuery) Only(ctx context.Context) (*PurchaseCommodity, error) {
	nodes, err := pcq.Limit(2).All(setContextOp(ctx, pcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{purchasecommodity.Label}
	default:
		return nil, &NotSingularError{purchasecommodity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) OnlyX(ctx context.Context) *PurchaseCommodity {
	node, err := pcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PurchaseCommodity ID in the query.
// Returns a *NotSingularError when more than one PurchaseCommodity ID is found.
// Returns a *NotFoundError when no entities are found.
func (pcq *PurchaseCommodityQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = pcq.Limit(2).IDs(setContextOp(ctx, pcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{purchasecommodity.Label}
	default:
		err = &NotSingularError{purchasecommodity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := pcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PurchaseCommodities.
func (pcq *PurchaseCommodityQuery) All(ctx context.Context) ([]*PurchaseCommodity, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryAll)
	if err := pcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PurchaseCommodity, *PurchaseCommodityQuery]()
	return withInterceptors[[]*PurchaseCommodity](ctx, pcq, qr, pcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) AllX(ctx context.Context) []*PurchaseCommodity {
	nodes, err := pcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PurchaseCommodity IDs.
func (pcq *PurchaseCommodityQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if pcq.ctx.Unique == nil && pcq.path != nil {
		pcq.Unique(true)
	}
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryIDs)
	if err = pcq.Select(purchasecommodity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := pcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pcq *PurchaseCommodityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryCount)
	if err := pcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pcq, querierCount[*PurchaseCommodityQuery](), pcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) CountX(ctx context.Context) int {
	count, err := pcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pcq *PurchaseCommodityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryExist)
	switch _, err := pcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pcq *PurchaseCommodityQuery) ExistX(ctx context.Context) bool {
	exist, err := pcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PurchaseCommodityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pcq *PurchaseCommodityQuery) Clone() *PurchaseCommodityQuery {
	if pcq == nil {
		return nil
	}
	return &PurchaseCommodityQuery{
		config:     pcq.config,
		ctx:        pcq.ctx.Clone(),
		order:      append([]purchasecommodity.OrderOption{}, pcq.order...),
		inters:     append([]Interceptor{}, pcq.inters...),
		predicates: append([]predicate.PurchaseCommodity{}, pcq.predicates...),
		// clone intermediate query.
		sql:       pcq.sql.Clone(),
		path:      pcq.path,
		modifiers: append([]func(*sql.Selector){}, pcq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PurchaseCommodity.Query().
//		GroupBy(purchasecommodity.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pcq *PurchaseCommodityQuery) GroupBy(field string, fields ...string) *PurchaseCommodityGroupBy {
	pcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PurchaseCommodityGroupBy{build: pcq}
	grbuild.flds = &pcq.ctx.Fields
	grbuild.label = purchasecommodity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PurchaseCommodity.Query().
//		Select(purchasecommodity.FieldCreatedAt).
//		Scan(ctx, &v)
func (pcq *PurchaseCommodityQuery) Select(fields ...string) *PurchaseCommoditySelect {
	pcq.ctx.Fields = append(pcq.ctx.Fields, fields...)
	sbuild := &PurchaseCommoditySelect{PurchaseCommodityQuery: pcq}
	sbuild.label = purchasecommodity.Label
	sbuild.flds, sbuild.scan = &pcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PurchaseCommoditySelect configured with the given aggregations.
func (pcq *PurchaseCommodityQuery) Aggregate(fns ...AggregateFunc) *PurchaseCommoditySelect {
	return pcq.Select().Aggregate(fns...)
}

func (pcq *PurchaseCommodityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pcq); err != nil {
				return err
			}
		}
	}
	for _, f := range pcq.ctx.Fields {
		if !purchasecommodity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pcq.path != nil {
		prev, err := pcq.path(ctx)
		if err != nil {
			return err
		}
		pcq.sql = prev
	}
	return nil
}

func (pcq *PurchaseCommodityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PurchaseCommodity, error) {
	var (
		nodes = []*PurchaseCommodity{}
		_spec = pcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PurchaseCommodity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PurchaseCommodity{config: pcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(pcq.modifiers) > 0 {
		_spec.Modifiers = pcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (pcq *PurchaseCommodityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pcq.querySpec()
	if len(pcq.modifiers) > 0 {
		_spec.Modifiers = pcq.modifiers
	}
	_spec.Node.Columns = pcq.ctx.Fields
	if len(pcq.ctx.Fields) > 0 {
		_spec.Unique = pcq.ctx.Unique != nil && *pcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pcq.driver, _spec)
}

func (pcq *PurchaseCommodityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(purchasecommodity.Table, purchasecommodity.Columns, sqlgraph.NewFieldSpec(purchasecommodity.FieldID, field.TypeUint64))
	_spec.From = pcq.sql
	if unique := pcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pcq.path != nil {
		_spec.Unique = true
	}
	if fields := pcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, purchasecommodity.FieldID)
		for i := range fields {
			if fields[i] != purchasecommodity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pcq *PurchaseCommodityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pcq.driver.Dialect())
	t1 := builder.Table(purchasecommodity.Table)
	columns := pcq.ctx.Fields
	if len(columns) == 0 {
		columns = purchasecommodity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pcq.sql != nil {
		selector = pcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pcq.ctx.Unique != nil && *pcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pcq.modifiers {
		m(selector)
	}
	for _, p := range pcq.predicates {
		p(selector)
	}
	for _, p := range pcq.order {
		p(selector)
	}
	if offset := pcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pcq *PurchaseCommodityQuery) Modify(modifiers ...func(s *sql.Selector)) *PurchaseCommoditySelect {
	pcq.modifiers = append(pcq.modifiers, modifiers...)
	return pcq.Select()
}

type PurchaseCommodityQueryWith string

var ()

func (pcq *PurchaseCommodityQuery) With(withEdges ...PurchaseCommodityQueryWith) *PurchaseCommodityQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return pcq
}

// PurchaseCommodityGroupBy is the group-by builder for PurchaseCommodity entities.
type PurchaseCommodityGroupBy struct {
	selector
	build *PurchaseCommodityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pcgb *PurchaseCommodityGroupBy) Aggregate(fns ...AggregateFunc) *PurchaseCommodityGroupBy {
	pcgb.fns = append(pcgb.fns, fns...)
	return pcgb
}

// Scan applies the selector query and scans the result into the given value.
func (pcgb *PurchaseCommodityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcgb.build.ctx, ent.OpQueryGroupBy)
	if err := pcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PurchaseCommodityQuery, *PurchaseCommodityGroupBy](ctx, pcgb.build, pcgb, pcgb.build.inters, v)
}

func (pcgb *PurchaseCommodityGroupBy) sqlScan(ctx context.Context, root *PurchaseCommodityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pcgb.fns))
	for _, fn := range pcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pcgb.flds)+len(pcgb.fns))
		for _, f := range *pcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PurchaseCommoditySelect is the builder for selecting fields of PurchaseCommodity entities.
type PurchaseCommoditySelect struct {
	*PurchaseCommodityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pcs *PurchaseCommoditySelect) Aggregate(fns ...AggregateFunc) *PurchaseCommoditySelect {
	pcs.fns = append(pcs.fns, fns...)
	return pcs
}

// Scan applies the selector query and scans the result into the given value.
func (pcs *PurchaseCommoditySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcs.ctx, ent.OpQuerySelect)
	if err := pcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PurchaseCommodityQuery, *PurchaseCommoditySelect](ctx, pcs.PurchaseCommodityQuery, pcs, pcs.inters, v)
}

func (pcs *PurchaseCommoditySelect) sqlScan(ctx context.Context, root *PurchaseCommodityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pcs.fns))
	for _, fn := range pcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pcs *PurchaseCommoditySelect) Modify(modifiers ...func(s *sql.Selector)) *PurchaseCommoditySelect {
	pcs.modifiers = append(pcs.modifiers, modifiers...)
	return pcs
}
