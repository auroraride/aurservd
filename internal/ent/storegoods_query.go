// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/goods"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/store"
	"github.com/auroraride/aurservd/internal/ent/storegoods"
)

// StoreGoodsQuery is the builder for querying StoreGoods entities.
type StoreGoodsQuery struct {
	config
	ctx        *QueryContext
	order      []storegoods.OrderOption
	inters     []Interceptor
	predicates []predicate.StoreGoods
	withGoods  *GoodsQuery
	withStore  *StoreQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StoreGoodsQuery builder.
func (sgq *StoreGoodsQuery) Where(ps ...predicate.StoreGoods) *StoreGoodsQuery {
	sgq.predicates = append(sgq.predicates, ps...)
	return sgq
}

// Limit the number of records to be returned by this query.
func (sgq *StoreGoodsQuery) Limit(limit int) *StoreGoodsQuery {
	sgq.ctx.Limit = &limit
	return sgq
}

// Offset to start from.
func (sgq *StoreGoodsQuery) Offset(offset int) *StoreGoodsQuery {
	sgq.ctx.Offset = &offset
	return sgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sgq *StoreGoodsQuery) Unique(unique bool) *StoreGoodsQuery {
	sgq.ctx.Unique = &unique
	return sgq
}

// Order specifies how the records should be ordered.
func (sgq *StoreGoodsQuery) Order(o ...storegoods.OrderOption) *StoreGoodsQuery {
	sgq.order = append(sgq.order, o...)
	return sgq
}

// QueryGoods chains the current query on the "goods" edge.
func (sgq *StoreGoodsQuery) QueryGoods() *GoodsQuery {
	query := (&GoodsClient{config: sgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(storegoods.Table, storegoods.FieldID, selector),
			sqlgraph.To(goods.Table, goods.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, storegoods.GoodsTable, storegoods.GoodsColumn),
		)
		fromU = sqlgraph.SetNeighbors(sgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStore chains the current query on the "store" edge.
func (sgq *StoreGoodsQuery) QueryStore() *StoreQuery {
	query := (&StoreClient{config: sgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := sgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := sgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(storegoods.Table, storegoods.FieldID, selector),
			sqlgraph.To(store.Table, store.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, storegoods.StoreTable, storegoods.StoreColumn),
		)
		fromU = sqlgraph.SetNeighbors(sgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StoreGoods entity from the query.
// Returns a *NotFoundError when no StoreGoods was found.
func (sgq *StoreGoodsQuery) First(ctx context.Context) (*StoreGoods, error) {
	nodes, err := sgq.Limit(1).All(setContextOp(ctx, sgq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{storegoods.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sgq *StoreGoodsQuery) FirstX(ctx context.Context) *StoreGoods {
	node, err := sgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StoreGoods ID from the query.
// Returns a *NotFoundError when no StoreGoods ID was found.
func (sgq *StoreGoodsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = sgq.Limit(1).IDs(setContextOp(ctx, sgq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{storegoods.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sgq *StoreGoodsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := sgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StoreGoods entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StoreGoods entity is found.
// Returns a *NotFoundError when no StoreGoods entities are found.
func (sgq *StoreGoodsQuery) Only(ctx context.Context) (*StoreGoods, error) {
	nodes, err := sgq.Limit(2).All(setContextOp(ctx, sgq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{storegoods.Label}
	default:
		return nil, &NotSingularError{storegoods.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sgq *StoreGoodsQuery) OnlyX(ctx context.Context) *StoreGoods {
	node, err := sgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StoreGoods ID in the query.
// Returns a *NotSingularError when more than one StoreGoods ID is found.
// Returns a *NotFoundError when no entities are found.
func (sgq *StoreGoodsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = sgq.Limit(2).IDs(setContextOp(ctx, sgq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{storegoods.Label}
	default:
		err = &NotSingularError{storegoods.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sgq *StoreGoodsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := sgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StoreGoodsSlice.
func (sgq *StoreGoodsQuery) All(ctx context.Context) ([]*StoreGoods, error) {
	ctx = setContextOp(ctx, sgq.ctx, ent.OpQueryAll)
	if err := sgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StoreGoods, *StoreGoodsQuery]()
	return withInterceptors[[]*StoreGoods](ctx, sgq, qr, sgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sgq *StoreGoodsQuery) AllX(ctx context.Context) []*StoreGoods {
	nodes, err := sgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StoreGoods IDs.
func (sgq *StoreGoodsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if sgq.ctx.Unique == nil && sgq.path != nil {
		sgq.Unique(true)
	}
	ctx = setContextOp(ctx, sgq.ctx, ent.OpQueryIDs)
	if err = sgq.Select(storegoods.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sgq *StoreGoodsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := sgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sgq *StoreGoodsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sgq.ctx, ent.OpQueryCount)
	if err := sgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sgq, querierCount[*StoreGoodsQuery](), sgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sgq *StoreGoodsQuery) CountX(ctx context.Context) int {
	count, err := sgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sgq *StoreGoodsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sgq.ctx, ent.OpQueryExist)
	switch _, err := sgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sgq *StoreGoodsQuery) ExistX(ctx context.Context) bool {
	exist, err := sgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StoreGoodsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sgq *StoreGoodsQuery) Clone() *StoreGoodsQuery {
	if sgq == nil {
		return nil
	}
	return &StoreGoodsQuery{
		config:     sgq.config,
		ctx:        sgq.ctx.Clone(),
		order:      append([]storegoods.OrderOption{}, sgq.order...),
		inters:     append([]Interceptor{}, sgq.inters...),
		predicates: append([]predicate.StoreGoods{}, sgq.predicates...),
		withGoods:  sgq.withGoods.Clone(),
		withStore:  sgq.withStore.Clone(),
		// clone intermediate query.
		sql:       sgq.sql.Clone(),
		path:      sgq.path,
		modifiers: append([]func(*sql.Selector){}, sgq.modifiers...),
	}
}

// WithGoods tells the query-builder to eager-load the nodes that are connected to
// the "goods" edge. The optional arguments are used to configure the query builder of the edge.
func (sgq *StoreGoodsQuery) WithGoods(opts ...func(*GoodsQuery)) *StoreGoodsQuery {
	query := (&GoodsClient{config: sgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sgq.withGoods = query
	return sgq
}

// WithStore tells the query-builder to eager-load the nodes that are connected to
// the "store" edge. The optional arguments are used to configure the query builder of the edge.
func (sgq *StoreGoodsQuery) WithStore(opts ...func(*StoreQuery)) *StoreGoodsQuery {
	query := (&StoreClient{config: sgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	sgq.withStore = query
	return sgq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StoreGoods.Query().
//		GroupBy(storegoods.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sgq *StoreGoodsQuery) GroupBy(field string, fields ...string) *StoreGoodsGroupBy {
	sgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StoreGoodsGroupBy{build: sgq}
	grbuild.flds = &sgq.ctx.Fields
	grbuild.label = storegoods.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.StoreGoods.Query().
//		Select(storegoods.FieldCreatedAt).
//		Scan(ctx, &v)
func (sgq *StoreGoodsQuery) Select(fields ...string) *StoreGoodsSelect {
	sgq.ctx.Fields = append(sgq.ctx.Fields, fields...)
	sbuild := &StoreGoodsSelect{StoreGoodsQuery: sgq}
	sbuild.label = storegoods.Label
	sbuild.flds, sbuild.scan = &sgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StoreGoodsSelect configured with the given aggregations.
func (sgq *StoreGoodsQuery) Aggregate(fns ...AggregateFunc) *StoreGoodsSelect {
	return sgq.Select().Aggregate(fns...)
}

func (sgq *StoreGoodsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sgq); err != nil {
				return err
			}
		}
	}
	for _, f := range sgq.ctx.Fields {
		if !storegoods.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sgq.path != nil {
		prev, err := sgq.path(ctx)
		if err != nil {
			return err
		}
		sgq.sql = prev
	}
	return nil
}

func (sgq *StoreGoodsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StoreGoods, error) {
	var (
		nodes       = []*StoreGoods{}
		_spec       = sgq.querySpec()
		loadedTypes = [2]bool{
			sgq.withGoods != nil,
			sgq.withStore != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StoreGoods).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StoreGoods{config: sgq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(sgq.modifiers) > 0 {
		_spec.Modifiers = sgq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := sgq.withGoods; query != nil {
		if err := sgq.loadGoods(ctx, query, nodes, nil,
			func(n *StoreGoods, e *Goods) { n.Edges.Goods = e }); err != nil {
			return nil, err
		}
	}
	if query := sgq.withStore; query != nil {
		if err := sgq.loadStore(ctx, query, nodes, nil,
			func(n *StoreGoods, e *Store) { n.Edges.Store = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (sgq *StoreGoodsQuery) loadGoods(ctx context.Context, query *GoodsQuery, nodes []*StoreGoods, init func(*StoreGoods), assign func(*StoreGoods, *Goods)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*StoreGoods)
	for i := range nodes {
		fk := nodes[i].GoodsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(goods.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "goods_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (sgq *StoreGoodsQuery) loadStore(ctx context.Context, query *StoreQuery, nodes []*StoreGoods, init func(*StoreGoods), assign func(*StoreGoods, *Store)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*StoreGoods)
	for i := range nodes {
		fk := nodes[i].StoreID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(store.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "store_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (sgq *StoreGoodsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sgq.querySpec()
	if len(sgq.modifiers) > 0 {
		_spec.Modifiers = sgq.modifiers
	}
	_spec.Node.Columns = sgq.ctx.Fields
	if len(sgq.ctx.Fields) > 0 {
		_spec.Unique = sgq.ctx.Unique != nil && *sgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sgq.driver, _spec)
}

func (sgq *StoreGoodsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(storegoods.Table, storegoods.Columns, sqlgraph.NewFieldSpec(storegoods.FieldID, field.TypeUint64))
	_spec.From = sgq.sql
	if unique := sgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sgq.path != nil {
		_spec.Unique = true
	}
	if fields := sgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, storegoods.FieldID)
		for i := range fields {
			if fields[i] != storegoods.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if sgq.withGoods != nil {
			_spec.Node.AddColumnOnce(storegoods.FieldGoodsID)
		}
		if sgq.withStore != nil {
			_spec.Node.AddColumnOnce(storegoods.FieldStoreID)
		}
	}
	if ps := sgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sgq *StoreGoodsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sgq.driver.Dialect())
	t1 := builder.Table(storegoods.Table)
	columns := sgq.ctx.Fields
	if len(columns) == 0 {
		columns = storegoods.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sgq.sql != nil {
		selector = sgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sgq.ctx.Unique != nil && *sgq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range sgq.modifiers {
		m(selector)
	}
	for _, p := range sgq.predicates {
		p(selector)
	}
	for _, p := range sgq.order {
		p(selector)
	}
	if offset := sgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sgq *StoreGoodsQuery) Modify(modifiers ...func(s *sql.Selector)) *StoreGoodsSelect {
	sgq.modifiers = append(sgq.modifiers, modifiers...)
	return sgq.Select()
}

type StoreGoodsQueryWith string

var (
	StoreGoodsQueryWithGoods StoreGoodsQueryWith = "Goods"
	StoreGoodsQueryWithStore StoreGoodsQueryWith = "Store"
)

func (sgq *StoreGoodsQuery) With(withEdges ...StoreGoodsQueryWith) *StoreGoodsQuery {
	for _, v := range withEdges {
		switch v {
		case StoreGoodsQueryWithGoods:
			sgq.WithGoods()
		case StoreGoodsQueryWithStore:
			sgq.WithStore()
		}
	}
	return sgq
}

// StoreGoodsGroupBy is the group-by builder for StoreGoods entities.
type StoreGoodsGroupBy struct {
	selector
	build *StoreGoodsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sggb *StoreGoodsGroupBy) Aggregate(fns ...AggregateFunc) *StoreGoodsGroupBy {
	sggb.fns = append(sggb.fns, fns...)
	return sggb
}

// Scan applies the selector query and scans the result into the given value.
func (sggb *StoreGoodsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sggb.build.ctx, ent.OpQueryGroupBy)
	if err := sggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreGoodsQuery, *StoreGoodsGroupBy](ctx, sggb.build, sggb, sggb.build.inters, v)
}

func (sggb *StoreGoodsGroupBy) sqlScan(ctx context.Context, root *StoreGoodsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sggb.fns))
	for _, fn := range sggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sggb.flds)+len(sggb.fns))
		for _, f := range *sggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StoreGoodsSelect is the builder for selecting fields of StoreGoods entities.
type StoreGoodsSelect struct {
	*StoreGoodsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sgs *StoreGoodsSelect) Aggregate(fns ...AggregateFunc) *StoreGoodsSelect {
	sgs.fns = append(sgs.fns, fns...)
	return sgs
}

// Scan applies the selector query and scans the result into the given value.
func (sgs *StoreGoodsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sgs.ctx, ent.OpQuerySelect)
	if err := sgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StoreGoodsQuery, *StoreGoodsSelect](ctx, sgs.StoreGoodsQuery, sgs, sgs.inters, v)
}

func (sgs *StoreGoodsSelect) sqlScan(ctx context.Context, root *StoreGoodsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sgs.fns))
	for _, fn := range sgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sgs *StoreGoodsSelect) Modify(modifiers ...func(s *sql.Selector)) *StoreGoodsSelect {
	sgs.modifiers = append(sgs.modifiers, modifiers...)
	return sgs
}
