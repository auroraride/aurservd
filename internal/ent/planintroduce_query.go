// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/ebikebrand"
	"github.com/auroraride/aurservd/internal/ent/planintroduce"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// PlanIntroduceQuery is the builder for querying PlanIntroduce entities.
type PlanIntroduceQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	inters     []Interceptor
	predicates []predicate.PlanIntroduce
	withBrand  *EbikeBrandQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlanIntroduceQuery builder.
func (piq *PlanIntroduceQuery) Where(ps ...predicate.PlanIntroduce) *PlanIntroduceQuery {
	piq.predicates = append(piq.predicates, ps...)
	return piq
}

// Limit the number of records to be returned by this query.
func (piq *PlanIntroduceQuery) Limit(limit int) *PlanIntroduceQuery {
	piq.limit = &limit
	return piq
}

// Offset to start from.
func (piq *PlanIntroduceQuery) Offset(offset int) *PlanIntroduceQuery {
	piq.offset = &offset
	return piq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (piq *PlanIntroduceQuery) Unique(unique bool) *PlanIntroduceQuery {
	piq.unique = &unique
	return piq
}

// Order specifies how the records should be ordered.
func (piq *PlanIntroduceQuery) Order(o ...OrderFunc) *PlanIntroduceQuery {
	piq.order = append(piq.order, o...)
	return piq
}

// QueryBrand chains the current query on the "brand" edge.
func (piq *PlanIntroduceQuery) QueryBrand() *EbikeBrandQuery {
	query := (&EbikeBrandClient{config: piq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := piq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(planintroduce.Table, planintroduce.FieldID, selector),
			sqlgraph.To(ebikebrand.Table, ebikebrand.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, planintroduce.BrandTable, planintroduce.BrandColumn),
		)
		fromU = sqlgraph.SetNeighbors(piq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PlanIntroduce entity from the query.
// Returns a *NotFoundError when no PlanIntroduce was found.
func (piq *PlanIntroduceQuery) First(ctx context.Context) (*PlanIntroduce, error) {
	nodes, err := piq.Limit(1).All(newQueryContext(ctx, TypePlanIntroduce, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{planintroduce.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (piq *PlanIntroduceQuery) FirstX(ctx context.Context) *PlanIntroduce {
	node, err := piq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlanIntroduce ID from the query.
// Returns a *NotFoundError when no PlanIntroduce ID was found.
func (piq *PlanIntroduceQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = piq.Limit(1).IDs(newQueryContext(ctx, TypePlanIntroduce, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{planintroduce.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (piq *PlanIntroduceQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := piq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlanIntroduce entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlanIntroduce entity is found.
// Returns a *NotFoundError when no PlanIntroduce entities are found.
func (piq *PlanIntroduceQuery) Only(ctx context.Context) (*PlanIntroduce, error) {
	nodes, err := piq.Limit(2).All(newQueryContext(ctx, TypePlanIntroduce, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{planintroduce.Label}
	default:
		return nil, &NotSingularError{planintroduce.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (piq *PlanIntroduceQuery) OnlyX(ctx context.Context) *PlanIntroduce {
	node, err := piq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlanIntroduce ID in the query.
// Returns a *NotSingularError when more than one PlanIntroduce ID is found.
// Returns a *NotFoundError when no entities are found.
func (piq *PlanIntroduceQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = piq.Limit(2).IDs(newQueryContext(ctx, TypePlanIntroduce, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{planintroduce.Label}
	default:
		err = &NotSingularError{planintroduce.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (piq *PlanIntroduceQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := piq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlanIntroduces.
func (piq *PlanIntroduceQuery) All(ctx context.Context) ([]*PlanIntroduce, error) {
	ctx = newQueryContext(ctx, TypePlanIntroduce, "All")
	if err := piq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlanIntroduce, *PlanIntroduceQuery]()
	return withInterceptors[[]*PlanIntroduce](ctx, piq, qr, piq.inters)
}

// AllX is like All, but panics if an error occurs.
func (piq *PlanIntroduceQuery) AllX(ctx context.Context) []*PlanIntroduce {
	nodes, err := piq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlanIntroduce IDs.
func (piq *PlanIntroduceQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	ctx = newQueryContext(ctx, TypePlanIntroduce, "IDs")
	if err := piq.Select(planintroduce.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (piq *PlanIntroduceQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := piq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (piq *PlanIntroduceQuery) Count(ctx context.Context) (int, error) {
	ctx = newQueryContext(ctx, TypePlanIntroduce, "Count")
	if err := piq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, piq, querierCount[*PlanIntroduceQuery](), piq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (piq *PlanIntroduceQuery) CountX(ctx context.Context) int {
	count, err := piq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (piq *PlanIntroduceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = newQueryContext(ctx, TypePlanIntroduce, "Exist")
	switch _, err := piq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (piq *PlanIntroduceQuery) ExistX(ctx context.Context) bool {
	exist, err := piq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlanIntroduceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (piq *PlanIntroduceQuery) Clone() *PlanIntroduceQuery {
	if piq == nil {
		return nil
	}
	return &PlanIntroduceQuery{
		config:     piq.config,
		limit:      piq.limit,
		offset:     piq.offset,
		order:      append([]OrderFunc{}, piq.order...),
		predicates: append([]predicate.PlanIntroduce{}, piq.predicates...),
		withBrand:  piq.withBrand.Clone(),
		// clone intermediate query.
		sql:    piq.sql.Clone(),
		path:   piq.path,
		unique: piq.unique,
	}
}

// WithBrand tells the query-builder to eager-load the nodes that are connected to
// the "brand" edge. The optional arguments are used to configure the query builder of the edge.
func (piq *PlanIntroduceQuery) WithBrand(opts ...func(*EbikeBrandQuery)) *PlanIntroduceQuery {
	query := (&EbikeBrandClient{config: piq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	piq.withBrand = query
	return piq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlanIntroduce.Query().
//		GroupBy(planintroduce.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (piq *PlanIntroduceQuery) GroupBy(field string, fields ...string) *PlanIntroduceGroupBy {
	piq.fields = append([]string{field}, fields...)
	grbuild := &PlanIntroduceGroupBy{build: piq}
	grbuild.flds = &piq.fields
	grbuild.label = planintroduce.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.PlanIntroduce.Query().
//		Select(planintroduce.FieldCreatedAt).
//		Scan(ctx, &v)
func (piq *PlanIntroduceQuery) Select(fields ...string) *PlanIntroduceSelect {
	piq.fields = append(piq.fields, fields...)
	sbuild := &PlanIntroduceSelect{PlanIntroduceQuery: piq}
	sbuild.label = planintroduce.Label
	sbuild.flds, sbuild.scan = &piq.fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlanIntroduceSelect configured with the given aggregations.
func (piq *PlanIntroduceQuery) Aggregate(fns ...AggregateFunc) *PlanIntroduceSelect {
	return piq.Select().Aggregate(fns...)
}

func (piq *PlanIntroduceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range piq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, piq); err != nil {
				return err
			}
		}
	}
	for _, f := range piq.fields {
		if !planintroduce.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if piq.path != nil {
		prev, err := piq.path(ctx)
		if err != nil {
			return err
		}
		piq.sql = prev
	}
	return nil
}

func (piq *PlanIntroduceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlanIntroduce, error) {
	var (
		nodes       = []*PlanIntroduce{}
		_spec       = piq.querySpec()
		loadedTypes = [1]bool{
			piq.withBrand != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlanIntroduce).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlanIntroduce{config: piq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(piq.modifiers) > 0 {
		_spec.Modifiers = piq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, piq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := piq.withBrand; query != nil {
		if err := piq.loadBrand(ctx, query, nodes, nil,
			func(n *PlanIntroduce, e *EbikeBrand) { n.Edges.Brand = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (piq *PlanIntroduceQuery) loadBrand(ctx context.Context, query *EbikeBrandQuery, nodes []*PlanIntroduce, init func(*PlanIntroduce), assign func(*PlanIntroduce, *EbikeBrand)) error {
	ids := make([]uint64, 0, len(nodes))
	nodeids := make(map[uint64][]*PlanIntroduce)
	for i := range nodes {
		if nodes[i].BrandID == nil {
			continue
		}
		fk := *nodes[i].BrandID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	query.Where(ebikebrand.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "brand_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (piq *PlanIntroduceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := piq.querySpec()
	if len(piq.modifiers) > 0 {
		_spec.Modifiers = piq.modifiers
	}
	_spec.Node.Columns = piq.fields
	if len(piq.fields) > 0 {
		_spec.Unique = piq.unique != nil && *piq.unique
	}
	return sqlgraph.CountNodes(ctx, piq.driver, _spec)
}

func (piq *PlanIntroduceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   planintroduce.Table,
			Columns: planintroduce.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: planintroduce.FieldID,
			},
		},
		From:   piq.sql,
		Unique: true,
	}
	if unique := piq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := piq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, planintroduce.FieldID)
		for i := range fields {
			if fields[i] != planintroduce.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := piq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := piq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := piq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := piq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (piq *PlanIntroduceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(piq.driver.Dialect())
	t1 := builder.Table(planintroduce.Table)
	columns := piq.fields
	if len(columns) == 0 {
		columns = planintroduce.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if piq.sql != nil {
		selector = piq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if piq.unique != nil && *piq.unique {
		selector.Distinct()
	}
	for _, m := range piq.modifiers {
		m(selector)
	}
	for _, p := range piq.predicates {
		p(selector)
	}
	for _, p := range piq.order {
		p(selector)
	}
	if offset := piq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := piq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (piq *PlanIntroduceQuery) Modify(modifiers ...func(s *sql.Selector)) *PlanIntroduceSelect {
	piq.modifiers = append(piq.modifiers, modifiers...)
	return piq.Select()
}

// PlanIntroduceGroupBy is the group-by builder for PlanIntroduce entities.
type PlanIntroduceGroupBy struct {
	selector
	build *PlanIntroduceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pigb *PlanIntroduceGroupBy) Aggregate(fns ...AggregateFunc) *PlanIntroduceGroupBy {
	pigb.fns = append(pigb.fns, fns...)
	return pigb
}

// Scan applies the selector query and scans the result into the given value.
func (pigb *PlanIntroduceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypePlanIntroduce, "GroupBy")
	if err := pigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlanIntroduceQuery, *PlanIntroduceGroupBy](ctx, pigb.build, pigb, pigb.build.inters, v)
}

func (pigb *PlanIntroduceGroupBy) sqlScan(ctx context.Context, root *PlanIntroduceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pigb.fns))
	for _, fn := range pigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pigb.flds)+len(pigb.fns))
		for _, f := range *pigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlanIntroduceSelect is the builder for selecting fields of PlanIntroduce entities.
type PlanIntroduceSelect struct {
	*PlanIntroduceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pis *PlanIntroduceSelect) Aggregate(fns ...AggregateFunc) *PlanIntroduceSelect {
	pis.fns = append(pis.fns, fns...)
	return pis
}

// Scan applies the selector query and scans the result into the given value.
func (pis *PlanIntroduceSelect) Scan(ctx context.Context, v any) error {
	ctx = newQueryContext(ctx, TypePlanIntroduce, "Select")
	if err := pis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlanIntroduceQuery, *PlanIntroduceSelect](ctx, pis.PlanIntroduceQuery, pis, pis.inters, v)
}

func (pis *PlanIntroduceSelect) sqlScan(ctx context.Context, root *PlanIntroduceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pis.fns))
	for _, fn := range pis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pis *PlanIntroduceSelect) Modify(modifiers ...func(s *sql.Selector)) *PlanIntroduceSelect {
	pis.modifiers = append(pis.modifiers, modifiers...)
	return pis
}
