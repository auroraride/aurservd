// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/instructions"
	"github.com/auroraride/aurservd/internal/ent/predicate"
)

// InstructionsQuery is the builder for querying Instructions entities.
type InstructionsQuery struct {
	config
	ctx        *QueryContext
	order      []instructions.OrderOption
	inters     []Interceptor
	predicates []predicate.Instructions
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InstructionsQuery builder.
func (iq *InstructionsQuery) Where(ps ...predicate.Instructions) *InstructionsQuery {
	iq.predicates = append(iq.predicates, ps...)
	return iq
}

// Limit the number of records to be returned by this query.
func (iq *InstructionsQuery) Limit(limit int) *InstructionsQuery {
	iq.ctx.Limit = &limit
	return iq
}

// Offset to start from.
func (iq *InstructionsQuery) Offset(offset int) *InstructionsQuery {
	iq.ctx.Offset = &offset
	return iq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iq *InstructionsQuery) Unique(unique bool) *InstructionsQuery {
	iq.ctx.Unique = &unique
	return iq
}

// Order specifies how the records should be ordered.
func (iq *InstructionsQuery) Order(o ...instructions.OrderOption) *InstructionsQuery {
	iq.order = append(iq.order, o...)
	return iq
}

// First returns the first Instructions entity from the query.
// Returns a *NotFoundError when no Instructions was found.
func (iq *InstructionsQuery) First(ctx context.Context) (*Instructions, error) {
	nodes, err := iq.Limit(1).All(setContextOp(ctx, iq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{instructions.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iq *InstructionsQuery) FirstX(ctx context.Context) *Instructions {
	node, err := iq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Instructions ID from the query.
// Returns a *NotFoundError when no Instructions ID was found.
func (iq *InstructionsQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = iq.Limit(1).IDs(setContextOp(ctx, iq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{instructions.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iq *InstructionsQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := iq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Instructions entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Instructions entity is found.
// Returns a *NotFoundError when no Instructions entities are found.
func (iq *InstructionsQuery) Only(ctx context.Context) (*Instructions, error) {
	nodes, err := iq.Limit(2).All(setContextOp(ctx, iq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{instructions.Label}
	default:
		return nil, &NotSingularError{instructions.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iq *InstructionsQuery) OnlyX(ctx context.Context) *Instructions {
	node, err := iq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Instructions ID in the query.
// Returns a *NotSingularError when more than one Instructions ID is found.
// Returns a *NotFoundError when no entities are found.
func (iq *InstructionsQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = iq.Limit(2).IDs(setContextOp(ctx, iq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{instructions.Label}
	default:
		err = &NotSingularError{instructions.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iq *InstructionsQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := iq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InstructionsSlice.
func (iq *InstructionsQuery) All(ctx context.Context) ([]*Instructions, error) {
	ctx = setContextOp(ctx, iq.ctx, "All")
	if err := iq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Instructions, *InstructionsQuery]()
	return withInterceptors[[]*Instructions](ctx, iq, qr, iq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iq *InstructionsQuery) AllX(ctx context.Context) []*Instructions {
	nodes, err := iq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Instructions IDs.
func (iq *InstructionsQuery) IDs(ctx context.Context) (ids []uint64, err error) {
	if iq.ctx.Unique == nil && iq.path != nil {
		iq.Unique(true)
	}
	ctx = setContextOp(ctx, iq.ctx, "IDs")
	if err = iq.Select(instructions.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iq *InstructionsQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := iq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iq *InstructionsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iq.ctx, "Count")
	if err := iq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iq, querierCount[*InstructionsQuery](), iq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iq *InstructionsQuery) CountX(ctx context.Context) int {
	count, err := iq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iq *InstructionsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iq.ctx, "Exist")
	switch _, err := iq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iq *InstructionsQuery) ExistX(ctx context.Context) bool {
	exist, err := iq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InstructionsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iq *InstructionsQuery) Clone() *InstructionsQuery {
	if iq == nil {
		return nil
	}
	return &InstructionsQuery{
		config:     iq.config,
		ctx:        iq.ctx.Clone(),
		order:      append([]instructions.OrderOption{}, iq.order...),
		inters:     append([]Interceptor{}, iq.inters...),
		predicates: append([]predicate.Instructions{}, iq.predicates...),
		// clone intermediate query.
		sql:  iq.sql.Clone(),
		path: iq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Instructions.Query().
//		GroupBy(instructions.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iq *InstructionsQuery) GroupBy(field string, fields ...string) *InstructionsGroupBy {
	iq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InstructionsGroupBy{build: iq}
	grbuild.flds = &iq.ctx.Fields
	grbuild.label = instructions.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Instructions.Query().
//		Select(instructions.FieldCreatedAt).
//		Scan(ctx, &v)
func (iq *InstructionsQuery) Select(fields ...string) *InstructionsSelect {
	iq.ctx.Fields = append(iq.ctx.Fields, fields...)
	sbuild := &InstructionsSelect{InstructionsQuery: iq}
	sbuild.label = instructions.Label
	sbuild.flds, sbuild.scan = &iq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InstructionsSelect configured with the given aggregations.
func (iq *InstructionsQuery) Aggregate(fns ...AggregateFunc) *InstructionsSelect {
	return iq.Select().Aggregate(fns...)
}

func (iq *InstructionsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iq); err != nil {
				return err
			}
		}
	}
	for _, f := range iq.ctx.Fields {
		if !instructions.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iq.path != nil {
		prev, err := iq.path(ctx)
		if err != nil {
			return err
		}
		iq.sql = prev
	}
	return nil
}

func (iq *InstructionsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Instructions, error) {
	var (
		nodes = []*Instructions{}
		_spec = iq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Instructions).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Instructions{config: iq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(iq.modifiers) > 0 {
		_spec.Modifiers = iq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (iq *InstructionsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iq.querySpec()
	if len(iq.modifiers) > 0 {
		_spec.Modifiers = iq.modifiers
	}
	_spec.Node.Columns = iq.ctx.Fields
	if len(iq.ctx.Fields) > 0 {
		_spec.Unique = iq.ctx.Unique != nil && *iq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iq.driver, _spec)
}

func (iq *InstructionsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(instructions.Table, instructions.Columns, sqlgraph.NewFieldSpec(instructions.FieldID, field.TypeUint64))
	_spec.From = iq.sql
	if unique := iq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iq.path != nil {
		_spec.Unique = true
	}
	if fields := iq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, instructions.FieldID)
		for i := range fields {
			if fields[i] != instructions.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iq *InstructionsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iq.driver.Dialect())
	t1 := builder.Table(instructions.Table)
	columns := iq.ctx.Fields
	if len(columns) == 0 {
		columns = instructions.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iq.sql != nil {
		selector = iq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iq.ctx.Unique != nil && *iq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range iq.modifiers {
		m(selector)
	}
	for _, p := range iq.predicates {
		p(selector)
	}
	for _, p := range iq.order {
		p(selector)
	}
	if offset := iq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (iq *InstructionsQuery) Modify(modifiers ...func(s *sql.Selector)) *InstructionsSelect {
	iq.modifiers = append(iq.modifiers, modifiers...)
	return iq.Select()
}

type InstructionsQueryWith string

var ()

func (iq *InstructionsQuery) With(withEdges ...InstructionsQueryWith) *InstructionsQuery {
	for _, v := range withEdges {
		switch v {
		}
	}
	return iq
}

// InstructionsGroupBy is the group-by builder for Instructions entities.
type InstructionsGroupBy struct {
	selector
	build *InstructionsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (igb *InstructionsGroupBy) Aggregate(fns ...AggregateFunc) *InstructionsGroupBy {
	igb.fns = append(igb.fns, fns...)
	return igb
}

// Scan applies the selector query and scans the result into the given value.
func (igb *InstructionsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, igb.build.ctx, "GroupBy")
	if err := igb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InstructionsQuery, *InstructionsGroupBy](ctx, igb.build, igb, igb.build.inters, v)
}

func (igb *InstructionsGroupBy) sqlScan(ctx context.Context, root *InstructionsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(igb.fns))
	for _, fn := range igb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*igb.flds)+len(igb.fns))
		for _, f := range *igb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*igb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := igb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InstructionsSelect is the builder for selecting fields of Instructions entities.
type InstructionsSelect struct {
	*InstructionsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (is *InstructionsSelect) Aggregate(fns ...AggregateFunc) *InstructionsSelect {
	is.fns = append(is.fns, fns...)
	return is
}

// Scan applies the selector query and scans the result into the given value.
func (is *InstructionsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, is.ctx, "Select")
	if err := is.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InstructionsQuery, *InstructionsSelect](ctx, is.InstructionsQuery, is, is.inters, v)
}

func (is *InstructionsSelect) sqlScan(ctx context.Context, root *InstructionsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(is.fns))
	for _, fn := range is.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*is.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := is.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (is *InstructionsSelect) Modify(modifiers ...func(s *sql.Selector)) *InstructionsSelect {
	is.modifiers = append(is.modifiers, modifiers...)
	return is
}
