// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/auroraride/aurservd/internal/ent/order"
	"github.com/auroraride/aurservd/internal/ent/orderalter"
	"github.com/auroraride/aurservd/internal/ent/predicate"
	"github.com/auroraride/aurservd/internal/ent/rider"
)

// OrderAlterQuery is the builder for querying OrderAlter entities.
type OrderAlterQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.OrderAlter
	// eager-loading edges.
	withRider *RiderQuery
	withOrder *OrderQuery
	modifiers []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderAlterQuery builder.
func (oaq *OrderAlterQuery) Where(ps ...predicate.OrderAlter) *OrderAlterQuery {
	oaq.predicates = append(oaq.predicates, ps...)
	return oaq
}

// Limit adds a limit step to the query.
func (oaq *OrderAlterQuery) Limit(limit int) *OrderAlterQuery {
	oaq.limit = &limit
	return oaq
}

// Offset adds an offset step to the query.
func (oaq *OrderAlterQuery) Offset(offset int) *OrderAlterQuery {
	oaq.offset = &offset
	return oaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (oaq *OrderAlterQuery) Unique(unique bool) *OrderAlterQuery {
	oaq.unique = &unique
	return oaq
}

// Order adds an order step to the query.
func (oaq *OrderAlterQuery) Order(o ...OrderFunc) *OrderAlterQuery {
	oaq.order = append(oaq.order, o...)
	return oaq
}

// QueryRider chains the current query on the "rider" edge.
func (oaq *OrderAlterQuery) QueryRider() *RiderQuery {
	query := &RiderQuery{config: oaq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderalter.Table, orderalter.FieldID, selector),
			sqlgraph.To(rider.Table, rider.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orderalter.RiderTable, orderalter.RiderColumn),
		)
		fromU = sqlgraph.SetNeighbors(oaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrder chains the current query on the "order" edge.
func (oaq *OrderAlterQuery) QueryOrder() *OrderQuery {
	query := &OrderQuery{config: oaq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := oaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := oaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderalter.Table, orderalter.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orderalter.OrderTable, orderalter.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(oaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OrderAlter entity from the query.
// Returns a *NotFoundError when no OrderAlter was found.
func (oaq *OrderAlterQuery) First(ctx context.Context) (*OrderAlter, error) {
	nodes, err := oaq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderalter.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (oaq *OrderAlterQuery) FirstX(ctx context.Context) *OrderAlter {
	node, err := oaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderAlter ID from the query.
// Returns a *NotFoundError when no OrderAlter ID was found.
func (oaq *OrderAlterQuery) FirstID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = oaq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderalter.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (oaq *OrderAlterQuery) FirstIDX(ctx context.Context) uint64 {
	id, err := oaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderAlter entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderAlter entity is found.
// Returns a *NotFoundError when no OrderAlter entities are found.
func (oaq *OrderAlterQuery) Only(ctx context.Context) (*OrderAlter, error) {
	nodes, err := oaq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderalter.Label}
	default:
		return nil, &NotSingularError{orderalter.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (oaq *OrderAlterQuery) OnlyX(ctx context.Context) *OrderAlter {
	node, err := oaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderAlter ID in the query.
// Returns a *NotSingularError when more than one OrderAlter ID is found.
// Returns a *NotFoundError when no entities are found.
func (oaq *OrderAlterQuery) OnlyID(ctx context.Context) (id uint64, err error) {
	var ids []uint64
	if ids, err = oaq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderalter.Label}
	default:
		err = &NotSingularError{orderalter.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (oaq *OrderAlterQuery) OnlyIDX(ctx context.Context) uint64 {
	id, err := oaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderAlters.
func (oaq *OrderAlterQuery) All(ctx context.Context) ([]*OrderAlter, error) {
	if err := oaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return oaq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (oaq *OrderAlterQuery) AllX(ctx context.Context) []*OrderAlter {
	nodes, err := oaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderAlter IDs.
func (oaq *OrderAlterQuery) IDs(ctx context.Context) ([]uint64, error) {
	var ids []uint64
	if err := oaq.Select(orderalter.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (oaq *OrderAlterQuery) IDsX(ctx context.Context) []uint64 {
	ids, err := oaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (oaq *OrderAlterQuery) Count(ctx context.Context) (int, error) {
	if err := oaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return oaq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (oaq *OrderAlterQuery) CountX(ctx context.Context) int {
	count, err := oaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (oaq *OrderAlterQuery) Exist(ctx context.Context) (bool, error) {
	if err := oaq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return oaq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (oaq *OrderAlterQuery) ExistX(ctx context.Context) bool {
	exist, err := oaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderAlterQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (oaq *OrderAlterQuery) Clone() *OrderAlterQuery {
	if oaq == nil {
		return nil
	}
	return &OrderAlterQuery{
		config:     oaq.config,
		limit:      oaq.limit,
		offset:     oaq.offset,
		order:      append([]OrderFunc{}, oaq.order...),
		predicates: append([]predicate.OrderAlter{}, oaq.predicates...),
		withRider:  oaq.withRider.Clone(),
		withOrder:  oaq.withOrder.Clone(),
		// clone intermediate query.
		sql:    oaq.sql.Clone(),
		path:   oaq.path,
		unique: oaq.unique,
	}
}

// WithRider tells the query-builder to eager-load the nodes that are connected to
// the "rider" edge. The optional arguments are used to configure the query builder of the edge.
func (oaq *OrderAlterQuery) WithRider(opts ...func(*RiderQuery)) *OrderAlterQuery {
	query := &RiderQuery{config: oaq.config}
	for _, opt := range opts {
		opt(query)
	}
	oaq.withRider = query
	return oaq
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (oaq *OrderAlterQuery) WithOrder(opts ...func(*OrderQuery)) *OrderAlterQuery {
	query := &OrderQuery{config: oaq.config}
	for _, opt := range opts {
		opt(query)
	}
	oaq.withOrder = query
	return oaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderAlter.Query().
//		GroupBy(orderalter.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (oaq *OrderAlterQuery) GroupBy(field string, fields ...string) *OrderAlterGroupBy {
	grbuild := &OrderAlterGroupBy{config: oaq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := oaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return oaq.sqlQuery(ctx), nil
	}
	grbuild.label = orderalter.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.OrderAlter.Query().
//		Select(orderalter.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (oaq *OrderAlterQuery) Select(fields ...string) *OrderAlterSelect {
	oaq.fields = append(oaq.fields, fields...)
	selbuild := &OrderAlterSelect{OrderAlterQuery: oaq}
	selbuild.label = orderalter.Label
	selbuild.flds, selbuild.scan = &oaq.fields, selbuild.Scan
	return selbuild
}

func (oaq *OrderAlterQuery) prepareQuery(ctx context.Context) error {
	for _, f := range oaq.fields {
		if !orderalter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if oaq.path != nil {
		prev, err := oaq.path(ctx)
		if err != nil {
			return err
		}
		oaq.sql = prev
	}
	return nil
}

func (oaq *OrderAlterQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderAlter, error) {
	var (
		nodes       = []*OrderAlter{}
		_spec       = oaq.querySpec()
		loadedTypes = [2]bool{
			oaq.withRider != nil,
			oaq.withOrder != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*OrderAlter).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &OrderAlter{config: oaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(oaq.modifiers) > 0 {
		_spec.Modifiers = oaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, oaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := oaq.withRider; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*OrderAlter)
		for i := range nodes {
			fk := nodes[i].RiderID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(rider.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "rider_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Rider = n
			}
		}
	}

	if query := oaq.withOrder; query != nil {
		ids := make([]uint64, 0, len(nodes))
		nodeids := make(map[uint64][]*OrderAlter)
		for i := range nodes {
			fk := nodes[i].OrderID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(order.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Order = n
			}
		}
	}

	return nodes, nil
}

func (oaq *OrderAlterQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := oaq.querySpec()
	if len(oaq.modifiers) > 0 {
		_spec.Modifiers = oaq.modifiers
	}
	_spec.Node.Columns = oaq.fields
	if len(oaq.fields) > 0 {
		_spec.Unique = oaq.unique != nil && *oaq.unique
	}
	return sqlgraph.CountNodes(ctx, oaq.driver, _spec)
}

func (oaq *OrderAlterQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := oaq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (oaq *OrderAlterQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   orderalter.Table,
			Columns: orderalter.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint64,
				Column: orderalter.FieldID,
			},
		},
		From:   oaq.sql,
		Unique: true,
	}
	if unique := oaq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := oaq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderalter.FieldID)
		for i := range fields {
			if fields[i] != orderalter.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := oaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := oaq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := oaq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := oaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (oaq *OrderAlterQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(oaq.driver.Dialect())
	t1 := builder.Table(orderalter.Table)
	columns := oaq.fields
	if len(columns) == 0 {
		columns = orderalter.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if oaq.sql != nil {
		selector = oaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if oaq.unique != nil && *oaq.unique {
		selector.Distinct()
	}
	for _, m := range oaq.modifiers {
		m(selector)
	}
	for _, p := range oaq.predicates {
		p(selector)
	}
	for _, p := range oaq.order {
		p(selector)
	}
	if offset := oaq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := oaq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oaq *OrderAlterQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderAlterSelect {
	oaq.modifiers = append(oaq.modifiers, modifiers...)
	return oaq.Select()
}

// OrderAlterGroupBy is the group-by builder for OrderAlter entities.
type OrderAlterGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (oagb *OrderAlterGroupBy) Aggregate(fns ...AggregateFunc) *OrderAlterGroupBy {
	oagb.fns = append(oagb.fns, fns...)
	return oagb
}

// Scan applies the group-by query and scans the result into the given value.
func (oagb *OrderAlterGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := oagb.path(ctx)
	if err != nil {
		return err
	}
	oagb.sql = query
	return oagb.sqlScan(ctx, v)
}

func (oagb *OrderAlterGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range oagb.fields {
		if !orderalter.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := oagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (oagb *OrderAlterGroupBy) sqlQuery() *sql.Selector {
	selector := oagb.sql.Select()
	aggregation := make([]string, 0, len(oagb.fns))
	for _, fn := range oagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(oagb.fields)+len(oagb.fns))
		for _, f := range oagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(oagb.fields...)...)
}

// OrderAlterSelect is the builder for selecting fields of OrderAlter entities.
type OrderAlterSelect struct {
	*OrderAlterQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (oas *OrderAlterSelect) Scan(ctx context.Context, v interface{}) error {
	if err := oas.prepareQuery(ctx); err != nil {
		return err
	}
	oas.sql = oas.OrderAlterQuery.sqlQuery(ctx)
	return oas.sqlScan(ctx, v)
}

func (oas *OrderAlterSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := oas.sql.Query()
	if err := oas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oas *OrderAlterSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderAlterSelect {
	oas.modifiers = append(oas.modifiers, modifiers...)
	return oas
}
